# 戴森球计划量产量化计算器 - 整体架构与模块交互文档

## 目录

- [1. 项目概述](#1-项目概述)
- [2. 技术栈](#2-技术栈)
- [3. 模块架构](#3-模块架构)
- [4. 核心流程总览](#4-核心流程总览)
- [5. 模块交互详解](#5-模块交互详解)
- [6. 数据流向](#6-数据流向)
- [7. 关键设计模式](#7-关键设计模式)
- [8. 性能优化策略](#8-性能优化策略)

---

## 1. 项目概述

戴森球计划量产量化计算器是一个基于Web的自动化生产计算工具，用于计算游戏中生产某种产品所需的设备数量、原料消耗和增产剂需求，并能自动生成游戏内可导入的蓝图字符串。

### 1.1 核心功能

| 功能模块 | 描述 |
|---------|------|
| 需求计算 | 根据目标产量自动计算所需设备数量和原料 |
| 配方管理 | 支持多配方选择和切换 |
| 增产剂计算 | 支持加速/增产两种增产剂效果 |
| 蓝图生成 | 自动生成游戏内可导入的蓝图字符串 |
| 方案管理 | 支持保存/加载生产方案 |

---

## 2. 技术栈

```mermaid
graph TD
    subgraph 前端技术
        A[HTML5] --> A1[语义化标签]
        A --> A2[data-*属性]
        B[CSS3] --> B1[渐变效果]
        B --> B2[Flex布局]
        B --> B3[动画效果]
        C[JavaScript] --> C1[ES6+语法]
        C --> C2[Web Worker]
    end
    
    subgraph 框架库
        D[Vue.js 2.x] --> D1[响应式数据绑定]
        D --> D2[条件渲染]
        D --> D3[列表渲染]
        E[jQuery 2.2.4] --> E1[DOM操作]
        E --> E2[AJAX请求]
        E --> E3[事件处理]
    end
    
    subgraph 工具库
        F[pako.js] --> F1[Gzip压缩]
        G[jquery.cookie] --> G1[Cookie存储]
        H[jquery.tips] --> H1[提示框]
        I[cocoMessage] --> I1[消息提示]
    end
```

---

## 3. 模块架构

### 3.1 整体架构图

```mermaid
graph TB
    subgraph 表现层
        UI[index.html<br/>页面结构]
        CSS[style.css<br/>样式]
    end
    
    subgraph 业务逻辑层
        DATA[data.js<br/>核心计算]
        BP[blueprint.js<br/>蓝图生成]
        FACADE[blueprint.facade.js<br/>外观层]
        WORKER[blueprint.worker.js<br/>工作线程]
    end
    
    subgraph 数据层
        JSON[data.json<br/>图标资源]
        STORAGE[localStorage<br/>持久化存储]
    end
    
    subgraph 第三方库
        VUE[vue.min.js]
        JQ[jquery-2.2.4.min.js]
        PAKO[pako.js]
    end
    
    UI --> DATA
    UI --> BP
    DATA --> STORAGE
    DATA --> JSON
    BP --> PAKO
    FACADE --> WORKER
    FACADE --> BP
    WORKER --> BP
    UI --> VUE
    UI --> JQ
```

### 3.2 模块职责划分

| 模块 | 文件 | 职责 |
|------|------|------|
| **页面入口** | index.html | HTML结构、Vue模板、内联脚本 |
| **样式** | style.css | 全局样式、组件样式、动画效果 |
| **核心计算** | data.js | 配方数据、需求计算、设备配置 |
| **蓝图生成** | blueprint.js | 建筑布局、传送带网络、蓝图编码 |
| **外观层** | blueprint.facade.js | 异步接口、Worker管理 |
| **工作线程** | blueprint.worker.js | 后台计算、防止UI阻塞 |
| **图标资源** | data.json | Base64图标数据 |

---

## 4. 核心流程总览

### 4.1 应用启动流程

```mermaid
sequenceDiagram
    participant Browser as 浏览器
    participant HTML as index.html
    participant Data as data.js
    participant Vue as Vue实例
    participant AJAX as AJAX请求
    
    Browser->>HTML: 加载页面
    HTML->>HTML: 解析DOM结构
    HTML->>Data: 加载data.js
    Data->>Data: 定义配方数据
    Data->>Data: 定义能量/空间数据
    Data->>Vue: 创建Vue实例
    Vue->>Vue: 初始化data数据
    Vue->>Vue: 编译#result模板
    Data->>Data: f_initData()构建索引
    Data->>Data: f_fillData()填充选项
    Data->>Data: doSpeed1()计算速度
    Data->>Data: loadSetting()加载设置
    Data->>AJAX: 请求data.json
    AJAX-->>Data: 返回图标数据
    Data->>Data: f_initIcons()初始化图标
    Vue->>Browser: 首次渲染完成
```

### 4.2 用户操作流程

```mermaid
flowchart TD
    A[用户打开页面] --> B{选择操作}
    
    B -->|添加需求| C[点击添加按钮]
    C --> D[显示物品选择器]
    D --> E[选择物品]
    E --> F[输入产量/设备数]
    F --> G[addItem添加到xqs]
    G --> H[update_all触发计算]
    
    B -->|修改设置| I[选择设备类型]
    I --> J[选择增产剂]
    J --> K[saveSetting保存]
    K --> H
    
    B -->|生成蓝图| L[点击生成蓝图]
    L --> M[getRecipe收集配方]
    M --> N[BlueprintFacade.generateAsync]
    N --> O{Worker支持?}
    O -->|是| P[Worker后台计算]
    O -->|否| Q[主线程同步计算]
    P --> R[返回蓝图字符串]
    Q --> R
    R --> S[复制到剪贴板]
    
    H --> T[loadNumber递归计算]
    T --> U[checkResult优化结果]
    U --> V[Vue更新视图]
```

### 4.3 蓝图生成流程

```mermaid
flowchart TD
    subgraph 准备阶段
        A[generateBlueprint] --> B[getRecipe收集配方]
        B --> C[构建outputRecipe对象]
        C --> D[创建Blueprint实例]
    end
    
    subgraph 初始化阶段
        D --> E[init初始化]
        E --> F[mapRecipeID配方映射]
        F --> G{stackLayers > 1?}
        G -->|是| H[缩减num]
        G -->|否| I[保持num]
        H --> J[calculateBlueprintArea]
        I --> J
    end
    
    subgraph 建筑生成阶段
        J --> K[generateBuildings]
        K --> L[遍历subRecipes]
        L --> M[newProductionBuilding]
        M --> N[计算坐标]
        N --> O[创建建筑]
        O --> P[创建分拣器]
    end
    
    subgraph 传送带阶段
        P --> Q[generateConveyorBelts]
        Q --> R[计算物料流量]
        R --> S[生成传送带节点]
        S --> T[连接分拣器]
    end
    
    subgraph 增产剂阶段
        T --> U{使用增产剂?}
        U -->|是| V[generateConveyorBeltsForSprayCoater]
        U -->|否| W{stackLayers > 1?}
        V --> W
    end
    
    subgraph 堆叠阶段
        W -->|是| X[cloneToStackLayers]
        W -->|否| Y[toStr编码]
        X --> Y
    end
    
    subgraph 输出阶段
        Y --> Z[JSON序列化]
        Z --> AA[Gzip压缩]
        AA --> AB[Base64编码]
        AB --> AC[返回蓝图字符串]
    end
```

---

## 5. 模块交互详解

### 5.1 data.js 与 index.html 交互

```mermaid
sequenceDiagram
    participant HTML as index.html
    participant Data as data.js
    participant Vue as Vue实例
    
    Note over HTML,Data: 初始化阶段
    HTML->>Data: 加载脚本
    Data->>Vue: 创建Vue实例
    Vue->>HTML: 绑定#result
    
    Note over HTML,Data: 用户交互阶段
    HTML->>Data: 触发事件(如f_add)
    Data->>Data: 处理业务逻辑
    Data->>Vue: 更新app数据
    Vue->>HTML: 响应式更新DOM
    
    Note over HTML,Data: 设置持久化
    Data->>Data: saveSetting()
    Data->>HTML: localStorage存储
```

### 5.2 data.js 与 blueprint.js 交互

```mermaid
sequenceDiagram
    participant Data as data.js
    participant BP as blueprint.js
    participant Facade as blueprint.facade.js
    participant Worker as blueprint.worker.js
    
    Note over Data,BP: 蓝图生成触发
    Data->>Data: generateBlueprint()
    Data->>Data: getRecipe()收集配方
    Data->>Facade: BlueprintFacade.generateAsync()
    
    Note over Facade,Worker: 异步处理
    Facade->>Worker: postMessage(config)
    Worker->>BP: new Blueprint()
    Worker->>BP: init()
    Worker->>BP: generateBuildings()
    Worker->>BP: generateConveyorBelts()
    Worker->>BP: toStr()
    Worker-->>Facade: postMessage(result)
    Facade-->>Data: Promise.resolve(bpStr)
    
    Note over Data: 结果处理
    Data->>Data: 复制到剪贴板
```

### 5.3 Vue数据绑定交互

```mermaid
graph LR
    subgraph Vue实例
        A[app.xqs] --> B[需求列表渲染]
        C[app.items] --> D[结果表格渲染]
        E[app.total] --> F[合计显示]
        G[app.icons] --> H[图标显示]
    end
    
    subgraph 用户交互
        I[点击编辑] --> J[onClickNumber]
        J --> K[xps_editor_index]
        K --> L[显示编辑框]
        M[输入完成] --> N[submitEditorNumber]
        N --> O[更新xqs]
        O --> P[update_all]
    end
    
    subgraph 计算更新
        P --> Q[loadNumber]
        Q --> R[xh_list]
        R --> S[生成items]
        S --> C
    end
```

---

## 6. 数据流向

### 6.1 核心数据结构

```mermaid
graph TD
    subgraph 输入数据
        A[xqs<br/>需求列表] --> A1["[{item, number}]"]
        B[singleMake<br/>独立生产] --> B1["[{id, number}]"]
        C[ig_names<br/>排除列表] --> C1["[name, ...]"]
    end
    
    subgraph 配方数据
        D[data<br/>配方数组] --> D1["[{s, q, t, m, group}]"]
        E[recipeIndexByProduct<br/>产物索引] --> E1["{name: [id, ...]}"]
        F[recipeIndexByMaterial<br/>原料索引] --> F1["{name: [id, ...]}"]
    end
    
    subgraph 计算中间数据
        G[xh_list<br/>需求计算结果] --> G1["[{name, value, value2, accTotal}]"]
        H[out_list<br/>产出列表] --> H1["[{name, value}]"]
    end
    
    subgraph 输出数据
        I[items<br/>结果列表] --> I1["[{name, number1, number2, pf, m, ...}]"]
        J[total<br/>设备合计] --> J1["[{name, value, energy, space}]"]
        K[outputRecipe<br/>蓝图配方] --> K1["{subRecipes, targetItem, ...}"]
    end
    
    A --> G
    B --> G
    D --> G
    G --> H
    G --> I
    I --> J
    I --> K
```

### 6.2 配方数据流转

```mermaid
flowchart LR
    subgraph 配方定义
        A[data数组] --> B[配方对象]
        B --> C["s: 产物数组"]
        B --> D["q: 原料数组"]
        B --> E["t: 生产时间"]
        B --> F["m: 设备类型"]
    end
    
    subgraph 索引构建
        A --> G[f_initData]
        G --> H[recipeIndexByProduct]
        G --> I[recipeIndexByMaterial]
    end
    
    subgraph 配方查找
        J[find函数] --> K{settings_pf?}
        K -->|是| L[用户选择配方]
        K -->|否| M[索引查找]
        M --> H
        L --> N[返回配方]
        H --> N
    end
    
    subgraph 配方归一化
        N --> O[归一化处理]
        O --> P[抵消相同产物原料]
        P --> Q[移除归零项]
        Q --> R[返回归一化配方]
    end
```

---

## 7. 关键设计模式

### 7.1 外观模式 (Facade Pattern)

```mermaid
classDiagram
    class BlueprintFacade {
        -_isGenerating: boolean
        +generateAsync(title, iconId, recipe, config): Promise~string~
    }
    
    class Blueprint {
        +init(): void
        +generateBuildings(): void
        +generateConveyorBelts(): void
        +toStr(): string
    }
    
    class Worker {
        +postMessage(data): void
        +onmessage: Function
    }
    
    BlueprintFacade --> Blueprint : 创建实例
    BlueprintFacade --> Worker : 管理生命周期
```

**应用场景**：
- `blueprint.facade.js` 封装了蓝图生成的复杂性
- 提供统一的异步接口 `generateAsync()`
- 自动处理 Worker 生命周期和降级逻辑

### 7.2 观察者模式 (Observer Pattern)

```mermaid
classDiagram
    class Vue {
        +data: Object
        +methods: Object
        +computed: Object
    }
    
    class AppData {
        +xqs: Array
        +items: Array
        +total: Array
    }
    
    class DOM {
        +需求列表
        +结果表格
        +合计显示
    }
    
    Vue --> AppData : 响应式代理
    AppData --> DOM : 自动更新
```

**应用场景**：
- Vue.js 的响应式数据绑定
- 数据变化自动触发视图更新
- 用户交互自动更新数据

### 7.3 策略模式 (Strategy Pattern)

```mermaid
classDiagram
    class BuildingAreaCalculator {
        +calculate(subRecipe): AreaInfo
    }
    
    class SmelterStrategy {
        +calculate(): AreaInfo
    }
    
    class AssemblingStrategy {
        +calculate(): AreaInfo
    }
    
    class PlantStrategy {
        +calculate(): AreaInfo
    }
    
    class RefineryStrategy {
        +calculate(): AreaInfo
    }
    
    BuildingAreaCalculator --> SmelterStrategy
    BuildingAreaCalculator --> AssemblingStrategy
    BuildingAreaCalculator --> PlantStrategy
    BuildingAreaCalculator --> RefineryStrategy
```

**应用场景**：
- `calculateBuildingArea()` 根据建筑类型选择不同计算策略
- 增产剂效果计算根据类型选择不同公式

### 7.4 工厂模式 (Factory Pattern)

```mermaid
classDiagram
    class Blueprint {
        +getBuildingTemplate(): Building
        +newSprayCoater(offset, yaw): Building
        +newConveyorNode(...): Building
    }
    
    class Building {
        +index: number
        +localOffset: Array
        +itemId: number
        +modelIndex: number
    }
    
    Blueprint --> Building : 创建
```

**应用场景**：
- `getBuildingTemplate()` 创建建筑模板
- `newSprayCoater()` 创建喷涂机
- `newConveyorNode()` 创建传送带节点

---

## 8. 性能优化策略

### 8.1 索引优化

```mermaid
flowchart LR
    subgraph 优化前
        A[遍历data数组] --> B["O(n)复杂度"]
    end
    
    subgraph 优化后
        C[recipeIndexByProduct] --> D["O(1)查找"]
        E[recipeIndexByMaterial] --> F["O(1)查找"]
    end
    
    subgraph 效果
        G[配方查找] --> H["从O(n)优化到O(1)"]
    end
```

**实现代码**：
```javascript
// 构建索引
var recipeIndexByProduct = {};  // 产物名 → [配方索引数组]
var recipeIndexByMaterial = {}; // 原料名 → [配方索引数组]

function f_initData() {
    $(data).each(function (i, item) {
        // 构建产物索引
        for (var j = 0; j < item.s.length; j++) {
            var productName = item.s[j].name;
            if (!recipeIndexByProduct[productName]) {
                recipeIndexByProduct[productName] = [];
            }
            recipeIndexByProduct[productName].push(i);
        }
        // 构建原料索引
        if (item.q) {
            for (var j = 0; j < item.q.length; j++) {
                var materialName = item.q[j].name;
                if (!recipeIndexByMaterial[materialName]) {
                    recipeIndexByMaterial[materialName] = [];
                }
                recipeIndexByMaterial[materialName].push(i);
            }
        }
    });
}

// 使用索引查找
function getPfs(name) {
    var pfs = [];
    var indices = recipeIndexByProduct[name] || [];
    for (var i = 0; i < indices.length; i++) {
        var pf = $.extend(true, {}, data[indices[i]]);
        pfs.push(pf);
    }
    return pfs;
}
```

### 8.2 Web Worker 异步计算

```mermaid
sequenceDiagram
    participant Main as 主线程
    participant Facade as BlueprintFacade
    participant Worker as Worker线程
    
    Main->>Facade: generateAsync()
    Facade->>Facade: 检查_isGenerating
    Facade->>Worker: 创建Worker
    Facade->>Worker: postMessage(config)
    
    Note over Worker: 后台计算
    Worker->>Worker: init()
    Worker->>Worker: generateBuildings()
    Worker->>Worker: toStr()
    
    Worker-->>Facade: postMessage(result)
    Facade->>Facade: cleanup()
    Facade-->>Main: Promise.resolve()
    
    Note over Main: UI保持响应
```

**优势**：
- 防止UI阻塞
- 提升用户体验
- 支持大蓝图生成

### 8.3 数据缓存

```mermaid
flowchart TD
    A[用户设置] --> B{localStorage支持?}
    B -->|是| C[localStorage存储]
    B -->|否| D[Cookie存储]
    
    C --> E[下次加载读取]
    D --> E
    
    E --> F[恢复用户设置]
    F --> G[减少重复配置]
```

**缓存数据**：
| 缓存项 | 键名 | 说明 |
|--------|------|------|
| 设备设置 | machine_settings + version | 每个配方的设备选择 |
| 速度设置 | machine_settings_time + version | 自定义设备速度 |
| 配方设置 | machine_settings_pf + version | 用户选择的配方 |
| 方案列表 | settings_projects + version | 保存的生产方案 |

### 8.4 防抖与节流

```javascript
// 互斥锁防止重复点击
class BlueprintFacade {
    static _isGenerating = false;
    
    static generateAsync(title, iconId, recipe, config) {
        if (this._isGenerating) {
            return Promise.reject(new Error("ERR_ALREADY_RUNNING"));
        }
        this._isGenerating = true;
        
        return new Promise((resolve, reject) => {
            // ... 计算逻辑
        }).finally(() => {
            this._isGenerating = false;
        });
    }
}
```

---

## 附录：模块依赖关系图

```mermaid
graph TB
    subgraph 入口
        INDEX[index.html]
    end
    
    subgraph 核心模块
        DATA[data.js]
        BP[blueprint.js]
    end
    
    subgraph 辅助模块
        FACADE[blueprint.facade.js]
        WORKER[blueprint.worker.js]
    end
    
    subgraph 第三方库
        VUE[vue.min.js]
        JQ[jquery.min.js]
        PAKO[pako.js]
        COOKIE[jquery.cookie.js]
        TIPS[jquery.tips.js]
        COCO[cocoMessage.js]
    end
    
    subgraph 数据文件
        JSON[data.json]
        CSS[style.css]
    end
    
    INDEX --> DATA
    INDEX --> BP
    INDEX --> VUE
    INDEX --> JQ
    INDEX --> CSS
    
    DATA --> COOKIE
    DATA --> TIPS
    DATA --> COCO
    DATA --> JSON
    
    BP --> PAKO
    BP --> COCO
    
    FACADE --> BP
    FACADE --> WORKER
    
    WORKER --> BP
    WORKER --> PAKO
```
