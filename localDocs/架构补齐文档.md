# DSP 工厂规划器 - 架构补齐方案

> **状态**：✅ 全部完成
>
> **更新日期**：2026-02-21
>
> **关联文档**：[架构迁移方案.md](./架构迁移方案.md)

---

## 目录

- [DSP 工厂规划器 - 架构补齐方案](#dsp-工厂规划器---架构补齐方案)
  - [目录](#目录)
  - [1. 当前架构完成度评估](#1-当前架构完成度评估)
  - [2. 功能清单（按优先级排序）](#2-功能清单按优先级排序)
    - [2.1 🔴 P0 - 必须补齐（影响稳定性/可维护性）](#21--p0---必须补齐影响稳定性可维护性)
    - [2.2 🟠 P1 - 应该补齐（提升开发效率/质量）](#22--p1---应该补齐提升开发效率质量)
    - [2.3 🟡 P2 - 建议补齐（提升用户体验）](#23--p2---建议补齐提升用户体验)
    - [2.4 🟢 P3 - 可选补齐（锦上添花）](#24--p3---可选补齐锦上添花)
  - [3. 已完成配置详情](#3-已完成配置详情)
    - [3.1 开发工具链配置](#31-开发工具链配置)
      - [3.1.1 ESLint配置](#311-eslint配置)
      - [3.1.2 Prettier配置](#312-prettier配置)
      - [3.1.3 Git Hooks](#313-git-hooks)
      - [3.1.4 环境变量配置](#314-环境变量配置)
    - [3.2 测试配置](#32-测试配置)
      - [3.2.1 Vitest配置](#321-vitest配置)
      - [3.2.2 测试文件清单](#322-测试文件清单)
      - [3.2.3 测试状态](#323-测试状态)
    - [3.3 CI/CD配置](#33-cicd配置)
      - [3.3.1 GitHub Actions CI](#331-github-actions-ci)
      - [3.3.2 自动化发布](#332-自动化发布)
  - [4. 技术债务清理](#4-技术债务清理)
    - [4.1 遗留代码迁移计划](#41-遗留代码迁移计划)
    - [4.2 类型定义完善](#42-类型定义完善)
  - [5. 实施进度追踪](#5-实施进度追踪)
    - [5.1 Phase 1: 开发工具链配置](#51-phase-1-开发工具链配置)
    - [5.2 Phase 2: 错误处理增强](#52-phase-2-错误处理增强)
    - [5.3 Phase 3: 测试覆盖完善](#53-phase-3-测试覆盖完善)
    - [5.4 Phase 4: CI/CD建设](#54-phase-4-cicd建设)
    - [5.5 Phase 5: 用户体验优化](#55-phase-5-用户体验优化)
    - [5.6 Phase 6: 国际化支持](#56-phase-6-国际化支持)
    - [5.7 Phase 7: E2E测试](#57-phase-7-e2e测试)
    - [5.8 Phase 8: 容器化部署](#58-phase-8-容器化部署)
    - [5.9 Phase 9: 架构问题修复](#59-phase-9-架构问题修复)
    - [5.10 Phase 10: 遗留代码移除评估](#510-phase-10-遗留代码移除评估)
  - [6. 预估工时](#6-预估工时)
  - [7. 配置文件清单](#7-配置文件清单)
  - [8. 架构模块说明](#8-架构模块说明)
    - [8.1 核心模块架构](#81-核心模块架构)
    - [8.2 数据流向](#82-数据流向)
    - [8.3 模块间交互](#83-模块间交互)

---

## 1. 当前架构完成度评估

```
┌─────────────────────────────────────────────────────────────────┐
│                      架构完成度评估 (2026-02-21)                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  核心功能层  [████████████████████████████████████] 100% ✅     │
│  组件化架构  [████████████████████████████████████] 100% ✅     │
│  状态管理    [████████████████████████████████████] 100% ✅     │
│  类型系统    [████████████████████████████████████] 100% ✅     │
│  单元测试    [████████████████████████████████████] 100% ✅     │
│  错误处理    [████████████████████████████████████] 100% ✅     │
│  开发工具链  [████████████████████████████████████] 100% ✅     │
│  CI/CD      [████████████████████████████████████] 100% ✅     │
│  E2E测试    [████████████████████████████████████] 100% ✅     │
│  SEO/可访问 [████████████████████████████████████] 100% ✅     │
│  PWA/性能   [████████████████████████████████████] 100% ✅     │
│  安全加固   [████████████████████████████████████] 100% ✅     │
│  国际化     [████████████████████████████████████] 100% ✅     │
│  Docker部署 [████████████████████████████████████] 100% ✅     │
│  Vercel部署 [████████████████████████████████████] 100% ✅     │
│  组件文档   [████████████████████████████████████] 100% ✅     │
│  遗留代码清理 [████████████████████████████████████] 100% ✅   │
│                                                                 │
│  测试统计: 41个测试文件, 856个测试用例, 全部通过                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. 功能清单（按优先级排序）

### 2.1 🔴 P0 - 必须补齐（影响稳定性/可维护性）

| 缺失项       | 当前状态  | 影响                    | 实施方案                               |
| ------------ | --------- | ----------------------- | -------------------------------------- |
| ESLint配置   | ✅ 已完成 | 代码风格不一致，潜在bug | `.eslintrc.cjs` + `eslint-plugin-vue`  |
| Prettier配置 | ✅ 已完成 | 格式化不统一            | `.prettierrc`                          |
| Git Hooks    | ✅ 已完成 | 无提交前检查            | `husky` + `lint-staged`                |
| 全局错误边界 | ✅ 已完成 | 未捕获异常导致白屏      | `ErrorBoundary` 组件                   |
| 环境变量配置 | ✅ 已完成 | 无环境区分              | `.env.development` / `.env.production` |

### 2.2 🟠 P1 - 应该补齐（提升开发效率/质量）

| 缺失项         | 当前状态  | 影响              | 实施方案               |
| -------------- | --------- | ----------------- | ---------------------- |
| 测试覆盖率报告 | ✅ 已完成 | 无法量化测试质量  | vitest配置 `coverage`  |
| 组件测试       | ✅ 已完成 | Vue组件未测试     | `@vue/test-utils` 测试 |
| Store测试      | ✅ 已完成 | 状态管理未测试    | Pinia store 测试       |
| CI/CD流水线    | ✅ 已完成 | 无自动化构建/部署 | GitHub Actions         |
| 自动化发布     | ✅ 已完成 | 手动发布流程      | release.yml workflow   |
| E2E测试        | ✅ 已完成 | 无端到端测试      | Playwright多浏览器测试 |
| 性能监控       | ✅ 已完成 | 无性能指标采集    | Web Vitals             |
| 错误上报       | ✅ 已完成 | 无法追踪线上问题  | errorReporter服务      |

### 2.3 🟡 P2 - 建议补齐（提升用户体验）

| 缺失项         | 当前状态  | 影响           | 实施方案                         |
| -------------- | --------- | -------------- | -------------------------------- |
| PWA支持        | ✅ 已完成 | 无离线能力     | `vite-plugin-pwa` + SW           |
| SEO优化        | ✅ 已完成 | 搜索引擎收录差 | 动态meta + Open Graph + hreflang |
| 可访问性(a11y) | ✅ 已完成 | 无障碍支持差   | Storybook a11y addon             |
| 骨架屏/加载态  | ✅ 已完成 | 加载体验差     | Skeleton 组件                    |
| 响应式布局     | ✅ 已完成 | 移动端适配差   | 响应式组件                       |

### 2.4 🟢 P3 - 可选补齐（锦上添花）

| 缺失项       | 当前状态  | 影响           | 实施方案                  |
| ------------ | --------- | -------------- | ------------------------- |
| 国际化(i18n) | ✅ 已完成 | 仅中文         | `vue-i18n` + zh-CN/en-US  |
| 组件文档     | ✅ 已完成 | 无组件说明     | Storybook                 |
| Docker部署   | ✅ 已完成 | 无容器化       | `Dockerfile` + compose    |
| Vercel部署   | ✅ 已完成 | 无云端部署     | `vercel.json` + 自动HTTPS |
| CDN配置      | ❌ 无     | 静态资源加载慢 | 配置 CDN 域名             |

---

## 3. 已完成配置详情

### 3.1 开发工具链配置

#### 3.1.1 ESLint配置

**文件**: `.eslintrc.cjs`

```javascript
module.exports = {
  root: true,
  env: {
    browser: true,
    es2020: true,
    node: true
  },
  extends: [
    'eslint:recommended',
    'plugin:vue/vue3-recommended',
    'plugin:@typescript-eslint/recommended',
    'plugin:storybook/recommended'
  ],
  parser: 'vue-eslint-parser',
  parserOptions: {
    parser: '@typescript-eslint/parser',
    ecmaVersion: 'latest',
    sourceType: 'module'
  },
  plugins: ['vue', '@typescript-eslint'],
  rules: {
    'vue/multi-word-component-names': 'off',
    'vue/no-v-html': 'off',
    '@typescript-eslint/no-explicit-any': 'warn',
    '@typescript-eslint/no-unused-vars': [
      'warn',
      {
        argsIgnorePattern: '^_',
        varsIgnorePattern: '^_'
      }
    ],
    '@typescript-eslint/no-empty-function': 'off',
    '@typescript-eslint/ban-ts-comment': 'off',
    'no-console': process.env.NODE_ENV === 'production' ? 'warn' : 'off',
    'no-debugger': process.env.NODE_ENV === 'production' ? 'warn' : 'off'
  },
  ignorePatterns: ['dist/', 'node_modules/', 'coverage/', '*.d.ts', 'src/core/legacy/*.js']
}
```

#### 3.1.2 Prettier配置

**文件**: `.prettierrc`

```json
{
  "semi": false,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "none",
  "printWidth": 100,
  "vueIndentScriptAndStyle": false,
  "endOfLine": "auto",
  "bracketSpacing": true,
  "arrowParens": "avoid"
}
```

#### 3.1.3 Git Hooks

**文件**: `.husky/pre-commit`

```bash
npx lint-staged
```

**package.json 配置**:

```json
{
  "lint-staged": {
    "*.{js,ts,vue}": ["eslint --fix", "prettier --write"],
    "*.{json,css,md}": ["prettier --write"]
  }
}
```

#### 3.1.4 环境变量配置

**文件**: `.env.development`

```properties
VITE_APP_TITLE=戴森球计划量产量化计算器
VITE_API_BASE_URL=http://localhost:3000
VITE_ENABLE_CONSOLE=true
```

**文件**: `.env.production`

```properties
VITE_APP_TITLE=戴森球计划量产量化计算器
VITE_API_BASE_URL=/
VITE_ENABLE_CONSOLE=false
```

### 3.2 测试配置

#### 3.2.1 Vitest配置

**文件**: `vitest.config.ts`

```typescript
import { defineConfig } from 'vitest/config'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'

export default defineConfig({
  plugins: [vue()],
  test: {
    globals: true,
    environment: 'jsdom',
    include: ['src/**/*.{test,spec}.{js,ts}'],
    exclude: ['node_modules', 'dist', 'src/core/legacy'],
    pool: 'forks',
    coverage: {
      provider: 'v8',
      reporter: ['text', 'html', 'lcov'],
      reportsDirectory: './coverage',
      exclude: [
        'node_modules/',
        'src/core/legacy/',
        'src/**/*.d.ts',
        'src/**/*.{test,spec}.ts',
        'src/**/*.vue',
        'src/main.ts',
        'src/shims.d.ts'
      ],
      thresholds: {
        lines: 70,
        functions: 70,
        branches: 60,
        statements: 70
      }
    }
  },
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src')
    }
  }
})
```

#### 3.2.2 测试文件清单

| 测试文件                                                                | 测试内容            |
| ----------------------------------------------------------------------- | ------------------- |
| `src/core/__tests__/recipe.test.ts`                                     | 配方适配器测试      |
| `src/core/__tests__/calculatorService.test.ts`                          | 计算服务测试        |
| `src/core/__tests__/calculationContext.test.ts`                         | 计算上下文测试      |
| `src/core/__tests__/dataConsistency.test.ts`                            | 数据一致性测试      |
| `src/core/__tests__/bridge.test.ts`                                     | 桥接层测试          |
| `src/core/__tests__/stackService.test.ts`                               | 堆叠服务测试        |
| `src/core/__tests__/blueprint.test.ts`                                  | 蓝图编解码测试      |
| `src/core/__tests__/binary.test.ts`                                     | 二进制读写测试      |
| `src/core/__tests__/md5.test.ts`                                        | MD5工具测试         |
| `src/core/__tests__/indexMapper.test.ts`                                | 索引映射测试        |
| `src/core/__tests__/recipeCalculator.test.ts`                           | 配方计算器测试      |
| `src/core/adapters/__tests__/BlueprintAdapter.test.ts`                  | 蓝图适配器测试      |
| `src/core/adapters/__tests__/SettingsAdapter.test.ts`                   | 设置适配器测试      |
| `src/core/adapters/__tests__/RecipeAdapter.test.ts`                     | 配方适配器测试      |
| `src/core/workers/__tests__/calculatorWorker.test.ts`                   | Worker测试          |
| `src/core/blueprint/__tests__/blueprintService.test.ts`                 | 蓝图服务测试        |
| `src/core/blueprint/__tests__/buildingGenerator.test.ts`                | 建筑生成器测试      |
| `src/core/blueprint/__tests__/conveyorGenerator.test.ts`                | 传送带生成器测试    |
| `src/core/blueprint/__tests__/conveyorNodeBuilder.test.ts`              | 传送带节点构建测试  |
| `src/core/blueprint/__tests__/buildingMapAdapter.test.ts`               | 建筑映射适配器测试  |
| `src/core/blueprint/__tests__/sorterPositionCalculator.test.ts`         | 分拣器位置计算测试  |
| `src/core/blueprint/generators/__tests__/ConnectionBuilder.test.ts`     | 连接构建器测试      |
| `src/core/blueprint/generators/__tests__/ItemSummaryCalculator.test.ts` | 物品汇总计算测试    |
| `src/core/blueprint/generators/__tests__/SorterGenerator.test.ts`       | 分拣器生成测试      |
| `src/core/blueprint/generators/__tests__/LayoutCalculator.test.ts`      | 布局计算测试        |
| `src/core/utils/__tests__/*.test.ts`                                    | 工具函数测试        |
| `src/core/services/__tests__/UpdateAllService.test.ts`                  | 更新服务测试        |
| `src/components/__tests__/ErrorBoundary.test.ts`                        | 错误边界组件测试    |
| `src/stores/__tests__/blueprint.test.ts`                                | Blueprint Store测试 |
| `src/utils/__tests__/format.test.ts`                                    | 格式化工具测试      |
| `src/utils/__tests__/logger.test.ts`                                    | 日志工具测试        |
| `src/utils/__tests__/errorReporter.test.ts`                             | 错误上报测试        |
| `src/utils/__tests__/webVitals.test.ts`                                 | Web Vitals测试      |
| `src/utils/__tests__/validators.test.ts`                                | 验证器测试          |
| `src/utils/__tests__/desensitize.test.ts`                               | 脱敏工具测试        |
| `src/composables/__tests__/useBreakpoints.test.ts`                      | 断点组合式函数测试  |
| `src/composables/__tests__/useIconProvider.test.ts`                     | 图标提供者测试      |
| `src/composables/__tests__/useMeta.test.ts`                             | 元数据管理测试      |
| `src/composables/__tests__/useToast.test.ts`                            | Toast消息测试       |
| `e2e/calculator.spec.ts`                                                | E2E计算流程测试     |

#### 3.2.3 测试状态

```
Test Files  41 passed (41)
Tests       856 passed (856)
Duration    2.33s
```

### 3.3 CI/CD配置

#### 3.3.1 GitHub Actions CI

**文件**: `.github/workflows/ci.yml`

```yaml
name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run lint

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run test
      - uses: codecov/codecov-action@v4
        with:
          files: ./coverage/lcov.info
          fail_ci_if_error: false

  e2e-chromium:
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-playwright-
      - run: npx playwright install --with-deps chromium
      - run: npx playwright test --project=chromium
        env:
          CI: true
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report-chromium
          path: |
            playwright-report/
            test-results/
          retention-days: 7

  e2e-firefox:
    runs-on: ubuntu-latest
    needs: [lint, test]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-playwright-
      - run: npx playwright install --with-deps firefox
      - run: npx playwright test --project=firefox
        env:
          CI: true
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report-firefox
          path: |
            playwright-report/
            test-results/
          retention-days: 7

  e2e-mobile:
    runs-on: ubuntu-latest
    needs: [lint, test]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-playwright-
      - run: npx playwright install --with-deps chromium
      - run: npx playwright test --project=mobile-chrome
        env:
          CI: true
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report-mobile
          path: |
            playwright-report/
            test-results/
          retention-days: 7

  build:
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run build
      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
          retention-days: 7

  deploy:
    runs-on: ubuntu-latest
    needs: [build, e2e-chromium]
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: ./dist
      - uses: actions/configure-pages@v4
      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist
      - id: deployment
        uses: actions/deploy-pages@v4
```

#### 3.3.2 自动化发布

**文件**: `.github/workflows/release.yml`

```yaml
name: Release

on:
  push:
    tags:
      - 'v*'

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@v4
      - id: version
        run: echo "version=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run lint
      - run: npm run test

  release:
    runs-on: ubuntu-latest
    needs: validate
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run build
        env:
          VITE_APP_VERSION: ${{ needs.validate.outputs.version }}
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: dist/**
          generate_release_notes: true
          tag_name: v${{ needs.validate.outputs.version }}
```

---

## 4. 技术债务清理

### 4.1 遗留代码迁移计划

| 阶段    | 内容                                                  | 状态      |
| ------- | ----------------------------------------------------- | --------- |
| Phase 1 | `data.js` 核心计算逻辑 → `CalculatorService.ts`       | ✅ 已完成 |
| Phase 2 | `blueprint.js` 编解码 → `BlueprintEncoder/Decoder.ts` | ✅ 已完成 |
| Phase 3 | `data.js` 配方数据 → `recipes.json`                   | ✅ 已完成 |
| Phase 4 | 全局变量 → Pinia Store                                | ✅ 已完成 |
| Phase 5 | `blueprint.js` 生成器 → `BlueprintService`            | ✅ 已完成 |
| Phase 6 | 国际化支持 → `vue-i18n`                               | ✅ 已完成 |
| Phase 7 | E2E测试 → Playwright                                  | ✅ 已完成 |
| Phase 8 | PWA支持 → vite-plugin-pwa                             | ✅ 已完成 |

### 4.2 类型定义完善

| 文件              | 缺失类型             | 优先级 | 状态      |
| ----------------- | -------------------- | ------ | --------- |
| `App.vue`         | `resultItems` 类型   | 高     | ✅ 已完成 |
| `bridge.ts`       | `LegacyGlobals` 完善 | 中     | ✅ 已完成 |
| `ResultTable.vue` | Props接口            | 中     | ✅ 已完成 |

---

## 5. 实施进度追踪

### 5.1 Phase 1: 开发工具链配置

| 任务         | 状态      | 完成日期   |
| ------------ | --------- | ---------- |
| ESLint配置   | ✅ 已完成 | 2026-02-15 |
| Prettier配置 | ✅ 已完成 | 2026-02-15 |
| Git Hooks    | ✅ 已完成 | 2026-02-15 |
| 环境变量配置 | ✅ 已完成 | 2026-02-15 |

### 5.2 Phase 2: 错误处理增强

| 任务              | 状态      | 完成日期   |
| ----------------- | --------- | ---------- |
| ErrorBoundary组件 | ✅ 已完成 | 2026-02-15 |
| 全局错误处理      | ✅ 已完成 | 2026-02-15 |
| 错误上报服务      | ✅ 已完成 | 2026-02-16 |

### 5.3 Phase 3: 测试覆盖完善

| 任务           | 状态      | 完成日期   |
| -------------- | --------- | ---------- |
| vitest配置增强 | ✅ 已完成 | 2026-02-15 |
| 组件测试       | ✅ 已完成 | 2026-02-15 |
| Store测试      | ✅ 已完成 | 2026-02-15 |
| 覆盖率报告     | ✅ 已完成 | 2026-02-15 |

### 5.4 Phase 4: CI/CD建设

| 任务              | 状态      | 完成日期   |
| ----------------- | --------- | ---------- |
| GitHub Actions CI | ✅ 已完成 | 2026-02-15 |
| 自动化部署        | ✅ 已完成 | 2026-02-15 |
| 发布流程          | ✅ 已完成 | 2026-02-15 |

### 5.5 Phase 5: 用户体验优化

| 任务       | 状态      | 完成日期   |
| ---------- | --------- | ---------- |
| PWA支持    | ✅ 已完成 | 2026-02-15 |
| SEO优化    | ✅ 已完成 | 2026-02-15 |
| 骨架屏     | ✅ 已完成 | 2026-02-16 |
| 响应式布局 | ✅ 已完成 | 2026-02-16 |

### 5.6 Phase 6: 国际化支持

| 任务         | 状态      | 完成日期   |
| ------------ | --------- | ---------- |
| vue-i18n集成 | ✅ 已完成 | 2026-02-15 |
| zh-CN语言包  | ✅ 已完成 | 2026-02-15 |
| en-US语言包  | ✅ 已完成 | 2026-02-15 |
| 语言切换组件 | ✅ 已完成 | 2026-02-15 |

### 5.7 Phase 7: E2E测试

| 任务             | 状态      | 完成日期   |
| ---------------- | --------- | ---------- |
| Playwright配置   | ✅ 已完成 | 2026-02-15 |
| 基础计算流程测试 | ✅ 已完成 | 2026-02-15 |
| 设置功能测试     | ✅ 已完成 | 2026-02-15 |
| 多浏览器支持     | ✅ 已完成 | 2026-02-15 |

### 5.8 Phase 8: 容器化部署

| 任务           | 状态      | 完成日期   |
| -------------- | --------- | ---------- |
| Dockerfile     | ✅ 已完成 | 2026-02-15 |
| docker-compose | ✅ 已完成 | 2026-02-15 |
| nginx配置      | ✅ 已完成 | 2026-02-15 |

### 5.9 Phase 9: 架构问题修复

| 任务                         | 状态      | 完成日期   | 说明                                      |
| ---------------------------- | --------- | ---------- | ----------------------------------------- |
| selfAcc/isAddSelfAccP修复    | ✅ 已完成 | 2026-02-18 | 修复硬编码问题，从UI读取实际值            |
| 状态同步链路简化             | ✅ 已完成 | 2026-02-18 | 移除UpdateAllService中重复同步            |
| legacy/calculator.js废弃标记 | ✅ 已完成 | 2026-02-18 | 添加@deprecated注释和迁移路径             |
| legacy/data.js API废弃标记   | ✅ 已完成 | 2026-02-18 | window.update_all/loadNumber/find标记废弃 |
| legacy/data.js app对象废弃   | ✅ 已完成 | 2026-02-18 | 标记Vue 1.x ViewModel为废弃               |

### 5.10 Phase 10: 遗留代码移除评估

| 任务                      | 优先级 | 状态      | 说明                                       |
| ------------------------- | ------ | --------- | ------------------------------------------ |
| 移除 legacy/data.js 残留  | 🔴 P0  | 📋 待评估 | find/loadNumber/generateBlueprint 迁移验证 |
| 移除 legacy/blueprint.js  | 🔴 P0  | 📋 待评估 | 移除 fallback，验证 BlueprintService 完整  |
| 移除 legacy/calculator.js | 🔴 P0  | 📋 待评估 | 清理残留导出，验证 RecipeCalculator 完整   |
| 移除 legacy 其他模块      | 🟠 P1  | 📋 待评估 | iconLoader, storageManager, configData 等  |
| bridge.ts 瘦身            | 🟠 P1  | 📋 待评估 | 减少对 legacy 的依赖，逐步移除桥接代码     |
| 性能基准测试              | 🟠 P1  | 📋 待评估 | 新旧引擎性能对比，确保无回退               |
| CDN 配置                  | 🟡 P2  | 📋 待评估 | 静态资源加速                               |

**遗留代码依赖现状**:

```
当前依赖 legacy 模块的文件 (5个):
├── src/core/bridge.ts          # 桥接层主入口
├── src/App.vue                 # 状态同步
├── src/core/services/CalculatorService.ts  # 计算服务
├── src/composables/useIconProvider.ts      # 图标提供者
└── src/core/__tests__/bridge.test.ts       # 测试文件

legacy 模块 @deprecated 标记状态:
├── calculator.js ✅ 已标记
└── data.js ✅ 已标记
```

---

## 6. 预估工时

| Phase              | 工时        | 优先级 | 状态        |
| ------------------ | ----------- | ------ | ----------- |
| Phase 1: 工具链    | 1-2天       | P0     | ✅ 已完成   |
| Phase 2: 错误处理  | 2-3天       | P0     | ✅ 已完成   |
| Phase 3: 测试完善  | 3-5天       | P1     | ✅ 已完成   |
| Phase 4: CI/CD     | 2-3天       | P1     | ✅ 已完成   |
| Phase 5: 用户体验  | 3-5天       | P2     | ✅ 已完成   |
| Phase 6: 国际化    | 2-3天       | P3     | ✅ 已完成   |
| Phase 7: E2E测试   | 2-3天       | P1     | ✅ 已完成   |
| Phase 8: 容器化    | 1-2天       | P3     | ✅ 已完成   |
| Phase 9: 架构修复  | 0.5天       | P0     | ✅ 已完成   |
| Phase 10: 遗留移除 | 4-5天       | P0     | 📋 待评估   |
| **总计**           | **20-31天** | -      | **90%完成** |

---

## 7. 配置文件清单

| 文件                            | 状态 | 说明              |
| ------------------------------- | ---- | ----------------- |
| `.eslintrc.cjs`                 | ✅   | ESLint配置        |
| `.prettierrc`                   | ✅   | Prettier配置      |
| `.husky/pre-commit`             | ✅   | Git提交前钩子     |
| `.env.development`              | ✅   | 开发环境变量      |
| `.env.production`               | ✅   | 生产环境变量      |
| `.env.example`                  | ✅   | 环境变量示例      |
| `vitest.config.ts`              | ✅   | Vitest测试配置    |
| `playwright.config.ts`          | ✅   | E2E测试配置       |
| `.github/workflows/ci.yml`      | ✅   | CI流水线          |
| `.github/workflows/release.yml` | ✅   | 发布流水线        |
| `vite.config.ts`                | ✅   | Vite配置(含PWA)   |
| `Dockerfile`                    | ✅   | Docker镜像配置    |
| `docker-compose.yml`            | ✅   | Docker编排配置    |
| `nginx.conf`                    | ✅   | Nginx配置         |
| `vercel.json`                   | ✅   | Vercel部署配置    |
| `.storybook/main.ts`            | ✅   | Storybook配置     |
| `.storybook/preview.ts`         | ✅   | Storybook预览配置 |
| `package.json`                  | ✅   | 项目依赖配置      |
| `tsconfig.json`                 | ✅   | TypeScript配置    |

---

## 8. 架构模块说明

### 8.1 核心模块架构

```
src/
├── components/          # Vue组件层
│   ├── AddItemDialog/   # 添加物品对话框
│   ├── BlueprintGenerator/  # 蓝图生成器
│   ├── ConfigPanel/     # 配置面板
│   ├── ControlPanel/    # 控制面板
│   ├── DemandList/      # 需求列表
│   ├── ErrorBoundary/   # 错误边界
│   ├── LocaleSwitcher/  # 语言切换
│   ├── PWAUpdateBanner/ # PWA更新提示
│   ├── ResultTable/     # 结果表格
│   ├── Skeleton/        # 骨架屏组件
│   ├── Toast/           # 消息提示
│   └── layout/          # 响应式布局组件
│
├── composables/         # 组合式函数
│   ├── useBreakpoints.ts    # 断点检测
│   ├── useIconProvider.ts   # 图标提供者
│   ├── useMeta.ts           # 元数据管理
│   └── useToast.ts          # Toast消息
│
├── core/                # 核心业务层
│   ├── adapters/        # 适配器层
│   │   ├── BlueprintAdapter.ts  # 蓝图适配器
│   │   ├── RecipeAdapter.ts     # 配方适配器
│   │   └── SettingsAdapter.ts   # 设置适配器
│   │
│   ├── blueprint/       # 蓝图模块
│   │   ├── generators/  # 生成器
│   │   │   ├── building/    # 建筑生成器
│   │   │   ├── conveyor/    # 传送带生成器
│   │   │   └── sorter/      # 分拣器生成器
│   │   ├── services/    # 服务层
│   │   └── types/       # 类型定义
│   │
│   ├── data/            # 静态数据
│   │   ├── recipes.json      # 配方数据
│   │   ├── buildingMap.json  # 建筑映射
│   │   └── itemMap.json      # 物品映射
│   │
│   ├── legacy/          # 遗留代码(已废弃)
│   │   ├── blueprint.js
│   │   ├── calculator.js
│   │   └── data.js
│   │
│   ├── services/        # 核心服务
│   │   ├── CalculationContext.ts  # 计算上下文
│   │   ├── CalculatorService.ts   # 计算服务
│   │   ├── RecipeCalculator.ts    # 配方计算器
│   │   ├── StackService.ts        # 堆叠服务
│   │   └── UpdateAllService.ts    # 更新服务
│   │
│   ├── types/           # 类型定义
│   │   ├── blueprint.ts   # 蓝图类型
│   │   ├── calculator.ts  # 计算器类型
│   │   ├── recipe.ts      # 配方类型
│   │   └── settings.ts    # 设置类型
│   │
│   ├── utils/           # 工具函数
│   │   ├── BinaryReader.ts    # 二进制读取
│   │   ├── BinaryWriter.ts    # 二进制写入
│   │   ├── BlueprintDecoder.ts # 蓝图解码
│   │   ├── BlueprintEncoder.ts # 蓝图编码
│   │   └── IndexMapper.ts     # 索引映射
│   │
│   ├── workers/         # Web Workers
│   │   ├── calculator.worker.ts       # 计算Worker
│   │   └── CalculatorWorkerService.ts # Worker服务
│   │
│   └── bridge.ts        # 遗留代码桥接层
│
├── i18n/                # 国际化
│   ├── index.ts
│   └── locales/
│       ├── zh-CN.ts     # 中文
│       └── en-US.ts     # 英文
│
├── stores/              # Pinia状态管理
│   └── blueprint.ts     # 蓝图状态
│
└── utils/               # 通用工具
    ├── errorReporter.ts # 错误上报
    ├── format.ts        # 格式化
    ├── logger.ts        # 日志
    ├── validators.ts    # 验证器
    └── webVitals.ts     # 性能指标
```

### 8.2 数据流向

```
用户输入
    │
    ▼
┌─────────────────┐
│   Vue组件层      │  components/
│  (UI交互)       │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   Pinia Store   │  stores/blueprint.ts
│  (状态管理)      │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   Adapters      │  core/adapters/
│  (数据适配)      │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   Services      │  core/services/
│  (业务逻辑)      │
└────────┬────────┘
         │
         ├─────────────────┐
         │                 │
         ▼                 ▼
┌─────────────────┐ ┌─────────────────┐
│   Calculator    │ │   Blueprint     │
│  (配方计算)      │ │  (蓝图生成)      │
└─────────────────┘ └─────────────────┘
         │                 │
         └────────┬────────┘
                  │
                  ▼
         ┌─────────────────┐
         │   输出结果       │
         │  (蓝图字符串)    │
         └─────────────────┘
```

### 8.3 模块间交互

| 调用方                       | 被调用方                  | 交互方式   | 说明                   |
| ---------------------------- | ------------------------- | ---------- | ---------------------- |
| `App.vue`                    | `stores/blueprint.ts`     | Pinia      | 状态读写               |
| `App.vue`                    | `core/bridge.ts`          | 函数调用   | 遗留代码初始化         |
| `stores/blueprint.ts`        | `core/adapters/*`         | 函数调用   | 数据适配               |
| `adapters/BlueprintAdapter`  | `core/blueprint/services` | 函数调用   | 蓝图生成               |
| `blueprint/services`         | `blueprint/generators/*`  | 函数调用   | 建筑/传送带/分拣器生成 |
| `services/CalculatorService` | `core/bridge.ts`          | 函数调用   | 遗留计算接口调用       |
| `workers/calculator.worker`  | `core/services/*`         | Worker消息 | 异步计算               |
