# 重构后部署方案

> **状态**：✅ 已完成 - GitHub Pages 自动部署
>
> **更新日期**：2026-02-15
>
> **关联文档**：[架构迁移方案.md](./架构迁移方案.md)

## 1. 部署架构对比

### 1.1 重构前部署流程

```
开发环境 → git push → GitHub main分支 → 服务器监听 → 自动部署
                ↓
         直接部署源码
         (index.html + Scripts/ + img/ + quote/)
```

**特点**：

- 纯静态HTML项目，无需构建
- main分支直接包含可部署资源
- 服务器拉取源码即可运行

### 1.2 重构后部署流程（当前方案）

```
开发环境 → git push → GitHub main分支 → CI触发 → 构建测试 → 部署GitHub Pages
                ↓
         源码 (src/ + 配置文件)
                ↓
         CI流水线 (lint → test → build)
                ↓
         构建产物 (dist/)
                ↓
         自动部署到 GitHub Pages
```

**特点**：

- Vue 3 + TypeScript + Vite 项目
- GitHub Actions 自动构建和部署
- GitHub Pages 免费托管
- 零服务器成本

---

## 2. 当前部署方案：GitHub Pages + GitHub Actions

### 2.1 方案概述

本项目采用 **GitHub Pages + GitHub Actions** 自动部署方案：

| 组件     | 说明                                |
| -------- | ----------------------------------- |
| 托管平台 | GitHub Pages                        |
| CI/CD    | GitHub Actions                      |
| 构建工具 | Vite                                |
| 部署触发 | push 到 main/master 分支            |
| 部署地址 | `https://<username>.github.io/DSQ/` |

### 2.2 CI/CD 流水线

项目已配置完整的 CI/CD 流水线：

```
┌─────────────────────────────────────────────────────────────────┐
│                    GitHub Actions 流水线                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Push to main/master                                            │
│         │                                                       │
│         ▼                                                       │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐         │
│  │    lint     │    │    test     │    │    build    │         │
│  │  ESLint检查  │    │  359个测试   │    │  Vite构建   │         │
│  └──────┬──────┘    └──────┬──────┘    └──────┬──────┘         │
│         │                  │                  │                 │
│         └──────────────────┼──────────────────┘                 │
│                            │                                    │
│                            ▼                                    │
│                   ┌─────────────┐                               │
│                   │   deploy    │                               │
│                   │ GitHub Pages│                               │
│                   └─────────────┘                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.3 CI 配置文件

**文件**: `.github/workflows/ci.yml`

```yaml
name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run lint

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run test
      - uses: codecov/codecov-action@v4
        with:
          files: ./coverage/lcov.info
          fail_ci_if_error: false

  build:
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run build
      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
          retention-days: 7

  deploy:
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: ./dist
      - uses: actions/configure-pages@v4
      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist
      - id: deployment
        uses: actions/deploy-pages@v4
```

### 2.4 Release 配置文件

**文件**: `.github/workflows/release.yml`

```yaml
name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run build
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: dist/**
          generate_release_notes: true
```

### 2.5 部署触发条件

| 事件 | 触发条件         | 执行内容                     |
| ---- | ---------------- | ---------------------------- |
| Push | main/master 分支 | lint → test → build → deploy |
| PR   | main/master 分支 | lint → test → build (不部署) |
| Tag  | v\* 格式标签     | build → 创建 GitHub Release  |

---

## 3. 构建配置

### 3.1 构建命令

```bash
npm install        # 安装依赖
npm run build      # 构建（vue-tsc类型检查 + vite build）
npm run preview    # 本地预览构建产物
```

### 3.2 Vite 配置

**文件**: `vite.config.ts`

```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src')
    }
  },
  server: {
    port: 3000,
    host: true
  },
  build: {
    target: 'esnext',
    outDir: 'dist',
    assetsDir: 'assets',
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['vue', 'pinia'],
          pako: ['pako']
        }
      }
    },
    minify: 'esbuild',
    esbuild: {
      drop: ['console', 'debugger']
    }
  },
  optimizeDeps: {
    include: ['vue', 'pinia']
  }
})
```

### 3.3 构建产物结构

```
dist/
├── index.html              # 入口HTML
├── assets/
│   ├── index-[hash].js     # 主JS包
│   ├── index-[hash].css    # 主CSS包
│   ├── vendor-[hash].js    # Vue + Pinia
│   └── pako-[hash].js      # pako压缩库
└── favicon.ico             # 网站图标
```

### 3.4 构建优化配置

| 配置项       | 值               | 说明                 |
| ------------ | ---------------- | -------------------- |
| target       | esnext           | 现代浏览器优化       |
| minify       | esbuild          | 快速压缩             |
| manualChunks | vendor/pako      | 代码分割             |
| drop         | console/debugger | 生产环境移除调试代码 |

---

## 4. package.json 脚本

```json
{
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint . --ext .vue,.js,.jsx,.cjs,.mjs,.ts,.tsx --fix",
    "format": "prettier --write \"src/**/*.{js,ts,vue,json,css}\"",
    "prepare": "husky"
  }
}
```

---

## 5. .gitignore 配置

```gitignore
# Dependencies
node_modules/

# Build output
dist/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Environment
.env
.env.local
.env.*.local

# Test coverage
coverage/
```

---

## 6. 迁移步骤（已完成）

### 6.1 准备阶段 ✅

1. ✅ 备份当前main分支
2. ✅ 确认所有功能已迁移完成
3. ✅ 配置CI/CD流水线

### 6.2 配置CI/CD ✅

1. ✅ 添加 `.github/workflows/ci.yml`
2. ✅ 添加 `.github/workflows/release.yml`
3. ✅ 配置 GitHub Pages
4. ✅ 测试构建流程通过

### 6.3 切换部署 ✅

1. ✅ 合并重构代码到main分支
2. ✅ 触发CI构建
3. ✅ 验证部署结果

---

## 7. 回滚方案

### 7.1 代码回滚

```bash
git revert HEAD        # 回滚最近一次提交
git reset --hard HEAD~1  # 强制回滚（谨慎使用）
```

### 7.2 部署回滚

- GitHub Actions 支持重新部署历史版本
- GitHub Pages 保留部署历史，可快速回滚

---

## 8. 监控与告警

### 8.1 部署状态监控

- GitHub Actions 构建状态徽章
- 部署失败邮件通知

### 8.2 运行时监控

- 前端错误上报（可选接入 Sentry）
- 性能监控（可选接入 Web Vitals）

---

## 9. 部署方案对比总结

| 维度     | 重构前     | 重构后         |
| -------- | ---------- | -------------- |
| 构建方式 | 无需构建   | Vite构建       |
| 部署内容 | 源码       | 构建产物       |
| CI/CD    | 无         | GitHub Actions |
| 托管平台 | 自建服务器 | GitHub Pages   |
| 部署触发 | 服务器监听 | push自动触发   |
| 回滚方式 | git reset  | CI重新部署     |
| 成本     | 服务器费用 | 免费           |

---

## 10. 备选部署方案

### 10.1 Vercel 部署（可选）

如需更强大的CDN和预览功能，可切换到Vercel：

| 特性         | 说明                     |
| ------------ | ------------------------ |
| 零配置构建   | 自动检测 Vite/Vue 项目   |
| Git 集成     | push 自动触发部署        |
| Preview 部署 | 每个分支自动生成预览 URL |
| 全球 CDN     | 自动分发到边缘节点       |
| 免费额度     | 个人项目免费             |

### 10.2 自建服务器部署（可选）

如需自建服务器，可使用以下配置：

**Nginx 配置示例**：

```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /var/www/dsq;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

---

## 11. 部署检查清单

### 11.1 首次部署检查

- [x] GitHub 仓库配置完成
- [x] `.github/workflows/ci.yml` 配置完成
- [x] `.github/workflows/release.yml` 配置完成
- [x] `vite.config.ts` 构建配置完成
- [x] 构建命令测试通过：`npm run build`
- [x] GitHub Pages 启用
- [x] 首次部署成功

### 11.2 持续部署检查

- [ ] lint 检查通过
- [ ] 359个测试全部通过
- [ ] 构建产物生成成功
- [ ] GitHub Pages 部署成功
- [ ] 线上功能验证通过

---

## 12. 常见问题

### Q1: 构建失败怎么办？

1. 检查 GitHub Actions 日志
2. 本地运行 `npm run build` 复现问题
3. 检查 TypeScript 类型错误：`npx vue-tsc --noEmit`

### Q2: 如何回滚到上一个版本？

1. 在 GitHub Actions 页面找到历史成功部署
2. 点击 "Re-run jobs" 重新部署
3. 或使用 `git revert` 回滚代码后重新推送

### Q3: 如何添加自定义域名？

1. 在 GitHub 仓库 Settings → Pages → Custom domain
2. 添加 CNAME 记录指向 `username.github.io`
3. 等待 DNS 生效

---

## 13. 总结

本项目已完成从传统静态部署到现代化 CI/CD 部署的迁移：

```
┌─────────────────────────────────────────────────────────────────┐
│                    部署架构演进                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  重构前                          重构后                          │
│  ────────                        ────────                        │
│  纯静态HTML                      Vue 3 + TypeScript              │
│  无构建步骤                      Vite 构建                        │
│  服务器监听部署                  GitHub Actions 自动部署          │
│  自建服务器                      GitHub Pages 免费托管            │
│  手动回滚                        CI 一键回滚                      │
│                                                                 │
│                        ↓ 迁移完成 ↓                              │
│                                                                 │
│  ✅ 零服务器成本                                                 │
│  ✅ 自动化CI/CD                                                  │
│  ✅ 代码质量保障 (lint + 359个测试)                               │
│  ✅ 快速回滚能力                                                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```
