# DSP 工厂规划器 - 架构补齐方案

> **状态**：✅ P0/P1 已完成
>
> **完成日期**：2026-02-14
>
> **关联文档**：[架构迁移方案.md](./架构迁移方案.md)

---

## 1. 当前架构完成度评估

```
┌─────────────────────────────────────────────────────────────────┐
│                      架构完成度评估                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  核心功能层  [████████████████████████████████████] 100%        │
│  组件化架构  [████████████████████████████████████] 100%        │
│  状态管理    [██████████████████████████████████░░]  90%        │
│  类型系统    [██████████████████████████████████░░]  90%        │
│  单元测试    [████████████████████████████░░░░░░░░]  70%        │
│  错误处理    [████████████████████████░░░░░░░░░░░░]  60%        │
│  开发工具链  [████████████████░░░░░░░░░░░░░░░░░░░░]  40%        │
│  CI/CD      [░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░]   0%        │
│  E2E测试    [░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░]   0%        │
│  SEO/可访问 [████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░]  20%        │
│  PWA/性能   [████████████░░░░░░░░░░░░░░░░░░░░░░░░░]  30%        │
│  安全加固   [████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░]  20%        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. 缺失功能清单（按优先级排序）

### 2.1 🔴 P0 - 必须补齐（影响稳定性/可维护性）

| 缺失项       | 当前状态 | 影响                    | 实施方案                                   |
| ------------ | -------- | ----------------------- | ------------------------------------------ |
| ESLint配置   | ❌ 无    | 代码风格不一致，潜在bug | 添加 `.eslintrc.cjs` + `eslint-plugin-vue` |
| Prettier配置 | ❌ 无    | 格式化不统一            | 添加 `.prettierrc`                         |
| Git Hooks    | ❌ 无    | 无提交前检查            | 添加 `husky` + `lint-staged`               |
| 全局错误边界 | ❌ 无    | 未捕获异常导致白屏      | 添加 `ErrorBoundary` 组件                  |
| 环境变量配置 | ❌ 无    | 无环境区分              | 添加 `.env.*` 文件                         |

### 2.2 🟠 P1 - 应该补齐（提升开发效率/质量）

| 缺失项         | 当前状态 | 影响              | 实施方案                    |
| -------------- | -------- | ----------------- | --------------------------- |
| 测试覆盖率报告 | ❌ 无    | 无法量化测试质量  | vitest配置 `coverage`       |
| 组件测试       | ❌ 无    | Vue组件未测试     | 添加 `@vue/test-utils` 测试 |
| CI/CD流水线    | ❌ 无    | 无自动化构建/部署 | 添加 GitHub Actions         |
| 性能监控       | ❌ 无    | 无性能指标采集    | 添加 Web Vitals             |
| 错误上报       | ❌ 无    | 无法追踪线上问题  | 集成 Sentry 或自建          |

### 2.3 🟡 P2 - 建议补齐（提升用户体验）

| 缺失项         | 当前状态 | 影响           | 实施方案                  |
| -------------- | -------- | -------------- | ------------------------- |
| PWA支持        | ❌ 无    | 无离线能力     | 添加 `vite-plugin-pwa`    |
| SEO优化        | ⚠️ 基础  | 搜索引擎收录差 | 添加动态meta + Open Graph |
| 可访问性(a11y) | ❌ 无    | 无障碍支持差   | 添加 `aria-*` 属性        |
| 骨架屏/加载态  | ⚠️ 简单  | 加载体验差     | 添加 Skeleton 组件        |
| 响应式布局     | ⚠️ 部分  | 移动端适配差   | 添加媒体查询              |

### 2.4 🟢 P3 - 可选补齐（锦上添花）

| 缺失项       | 当前状态 | 影响           | 实施方案                    |
| ------------ | -------- | -------------- | --------------------------- |
| 国际化(i18n) | ❌ 无    | 仅中文         | 添加 `vue-i18n`             |
| 组件文档     | ❌ 无    | 无组件说明     | 添加 Storybook 或 VitePress |
| Docker部署   | ❌ 无    | 无容器化       | 添加 `Dockerfile`           |
| CDN配置      | ❌ 无    | 静态资源加载慢 | 配置 CDN 域名               |

---

## 3. 详细实施方案

### 3.1 Phase 1: 开发工具链配置（1-2天）

#### 3.1.1 ESLint配置

**新增文件**: `.eslintrc.cjs`

```javascript
module.exports = {
  root: true,
  env: {
    browser: true,
    es2020: true,
    node: true
  },
  extends: [
    'eslint:recommended',
    'plugin:vue/vue3-recommended',
    'plugin:@typescript-eslint/recommended'
  ],
  parser: 'vue-eslint-parser',
  parserOptions: {
    parser: '@typescript-eslint/parser',
    ecmaVersion: 'latest',
    sourceType: 'module'
  },
  plugins: ['vue', '@typescript-eslint'],
  rules: {
    'vue/multi-word-component-names': 'off',
    '@typescript-eslint/no-explicit-any': 'warn',
    '@typescript-eslint/no-unused-vars': ['warn', { argsIgnorePattern: '^_' }]
  },
  ignorePatterns: ['dist/', 'node_modules/', 'src/core/legacy/*.js']
}
```

**新增依赖**:

```bash
npm install -D eslint @typescript-eslint/parser @typescript-eslint/eslint-plugin eslint-plugin-vue
```

#### 3.1.2 Prettier配置

**新增文件**: `.prettierrc`

```json
{
  "semi": false,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "none",
  "printWidth": 100,
  "vueIndentScriptAndStyle": true
}
```

**新增依赖**:

```bash
npm install -D prettier eslint-config-prettier
```

#### 3.1.3 Git Hooks

**新增依赖**:

```bash
npm install -D husky lint-staged
npx husky init
```

**新增文件**: `.husky/pre-commit`

```bash
npx lint-staged
```

**修改 `package.json`**:

```json
{
  "lint-staged": {
    "*.{js,ts,vue}": ["eslint --fix", "prettier --write"],
    "*.{json,md,css}": ["prettier --write"]
  }
}
```

#### 3.1.4 环境变量配置

**新增文件**: `.env.development`

```properties
VITE_APP_TITLE=戴森球计划量产量化计算器
VITE_API_BASE_URL=http://localhost:3000
VITE_ENABLE_CONSOLE=true
```

**新增文件**: `.env.production`

```properties
VITE_APP_TITLE=戴森球计划量产量化计算器
VITE_API_BASE_URL=/
VITE_ENABLE_CONSOLE=false
```

**修改 `vite.config.ts`**:

```typescript
export default defineConfig({
  define: {
    __APP_VERSION__: JSON.stringify(process.env.npm_package_version)
  }
})
```

---

### 3.2 Phase 2: 错误处理增强（2-3天）

#### 3.2.1 全局错误边界组件

**新增文件**: `src/components/ErrorBoundary.vue`

```vue
<template>
  <slot v-if="!error" />
  <div v-else class="error-boundary">
    <div class="error-content">
      <h2>出错了</h2>
      <p>{{ error.message }}</p>
      <button @click="resetError">重试</button>
    </div>
  </div>
</template>

<script setup lang="ts">
import { onErrorCaptured, ref } from 'vue'
import { logger } from '@/utils/logger'

const error = ref<Error | null>(null)

onErrorCaptured((e: Error) => {
  error.value = e
  logger.error('[ErrorBoundary]', e)
  return false
})

function resetError() {
  error.value = null
}
</script>

<style scoped>
.error-boundary {
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 400px;
}
.error-content {
  text-align: center;
  padding: 40px;
  background: #fff;
  border-radius: 8px;
  box-shadow: 0 2px 12px rgba(0, 0, 0, 0.1);
}
</style>
```

**修改 `App.vue`**:

```vue
<template>
  <ErrorBoundary>
    <div id="app">
      <!-- 原有内容 -->
    </div>
  </ErrorBoundary>
</template>
```

#### 3.2.2 全局错误处理

**修改 `main.ts`**:

```typescript
app.config.errorHandler = (err, instance, info) => {
  logger.error('[Vue Error]', err, info)
}

app.config.warnHandler = (msg, instance, trace) => {
  logger.warn('[Vue Warn]', msg, trace)
}

window.addEventListener('unhandledrejection', event => {
  logger.error('[Unhandled Rejection]', event.reason)
})
```

#### 3.2.3 错误上报服务（可选）

**新增文件**: `src/utils/errorReporter.ts`

```typescript
interface ErrorReport {
  message: string
  stack?: string
  url: string
  timestamp: number
  userAgent: string
}

export function reportError(error: Error): void {
  const report: ErrorReport = {
    message: error.message,
    stack: error.stack,
    url: window.location.href,
    timestamp: Date.now(),
    userAgent: navigator.userAgent
  }

  if (import.meta.env.PROD) {
    fetch('/api/errors', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(report)
    }).catch(() => {})
  }
}
```

---

### 3.3 Phase 3: 测试覆盖完善（3-5天）

#### 3.3.1 测试配置增强

**新增文件**: `vitest.config.ts`

```typescript
import { defineConfig } from 'vitest/config'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'

export default defineConfig({
  plugins: [vue()],
  test: {
    globals: true,
    environment: 'jsdom',
    coverage: {
      provider: 'v8',
      reporter: ['text', 'html', 'lcov'],
      exclude: [
        'node_modules/',
        'src/core/legacy/',
        'src/**/*.d.ts',
        'src/**/*.test.ts',
        'src/**/*.spec.ts'
      ],
      thresholds: {
        lines: 70,
        functions: 70,
        branches: 60,
        statements: 70
      }
    }
  },
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src')
    }
  }
})
```

**新增依赖**:

```bash
npm install -D @vue/test-utils jsdom @vitest/coverage-v8
```

#### 3.3.2 组件测试示例

**新增文件**: `src/components/__tests__/ResultTable.test.ts`

```typescript
import { describe, it, expect } from 'vitest'
import { mount } from '@vue/test-utils'
import ResultTable from '../ResultTable/ResultTable.vue'

describe('ResultTable', () => {
  it('renders empty state when no items', () => {
    const wrapper = mount(ResultTable, {
      props: {
        items: [],
        isLoading: false,
        error: null,
        hideSource: false
      }
    })
    expect(wrapper.text()).toContain('暂无数据')
  })

  it('shows loading state', () => {
    const wrapper = mount(ResultTable, {
      props: {
        items: [],
        isLoading: true,
        error: null,
        hideSource: false
      }
    })
    expect(wrapper.find('.loading').exists()).toBe(true)
  })

  it('displays error message', () => {
    const wrapper = mount(ResultTable, {
      props: {
        items: [],
        isLoading: false,
        error: '计算失败',
        hideSource: false
      }
    })
    expect(wrapper.text()).toContain('计算失败')
  })
})
```

#### 3.3.3 Store测试示例

**新增文件**: `src/stores/__tests__/blueprint.test.ts`

```typescript
import { describe, it, expect, beforeEach } from 'vitest'
import { setActivePinia, createPinia } from 'pinia'
import { useBlueprintStore } from '../blueprint'

describe('BlueprintStore', () => {
  beforeEach(() => {
    setActivePinia(createPinia())
  })

  it('adds demand correctly', () => {
    const store = useBlueprintStore()
    store.addDemand('ironOre', 100)
    expect(store.demandList).toHaveLength(1)
    expect(store.demandList[0]).toEqual({ name: 'ironOre', num: 100 })
  })

  it('updates existing demand', () => {
    const store = useBlueprintStore()
    store.addDemand('ironOre', 100)
    store.addDemand('ironOre', 50)
    expect(store.demandList).toHaveLength(1)
    expect(store.demandList[0].num).toBe(150)
  })

  it('creates valid snapshot', () => {
    const store = useBlueprintStore()
    store.addDemand('ironOre', 100)
    const snapshot = store.createSnapshot()
    expect(snapshot.demandVersion).toBe(1)
    expect(snapshot.demandList).toHaveLength(1)
  })
})
```

---

### 3.4 Phase 4: CI/CD建设（2-3天）

#### 3.4.1 GitHub Actions配置

**新增文件**: `.github/workflows/ci.yml`

```yaml
name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run lint

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run test
      - uses: codecov/codecov-action@v4
        with:
          files: ./coverage/lcov.info

  build:
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run build
      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  deploy:
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
```

#### 3.4.2 自动化发布

**新增文件**: `.github/workflows/release.yml`

```yaml
name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run build
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: dist/**
```

---

### 3.5 Phase 5: 用户体验优化（持续）

#### 3.5.1 PWA支持

**新增依赖**:

```bash
npm install -D vite-plugin-pwa
```

**修改 `vite.config.ts`**:

```typescript
import { VitePWA } from 'vite-plugin-pwa'

export default defineConfig({
  plugins: [
    vue(),
    VitePWA({
      registerType: 'autoUpdate',
      manifest: {
        name: '戴森球计划量产量化计算器',
        short_name: 'DSP计算器',
        theme_color: '#1a1a2e',
        icons: [
          { src: '/icon-192.png', sizes: '192x192', type: 'image/png' },
          { src: '/icon-512.png', sizes: '512x512', type: 'image/png' }
        ]
      }
    })
  ]
})
```

#### 3.5.2 SEO优化

**修改 `index.html`**:

```html
<head>
  <meta name="description" content="《戴森球计划》自动化量产计算器，全自动计算产能需求" />
  <meta property="og:title" content="戴森球计划量产量化计算器" />
  <meta property="og:description" content="全自动计算量产产品的具体产能需求" />
  <meta property="og:type" content="website" />
  <meta property="og:locale" content="zh_CN" />
  <link rel="canonical" href="https://your-domain.com/" />
</head>
```

#### 3.5.3 骨架屏组件

**新增文件**: `src/components/Skeleton/SkeletonTable.vue`

```vue
<template>
  <div class="skeleton-table">
    <div class="skeleton-header">
      <div class="skeleton-row">
        <div v-for="i in 6" :key="i" class="skeleton-cell header"></div>
      </div>
    </div>
    <div class="skeleton-body">
      <div v-for="i in 5" :key="i" class="skeleton-row">
        <div v-for="j in 6" :key="j" class="skeleton-cell"></div>
      </div>
    </div>
  </div>
</template>

<style scoped>
.skeleton-table {
  width: 100%;
}
.skeleton-row {
  display: flex;
  gap: 12px;
  padding: 12px 0;
}
.skeleton-cell {
  flex: 1;
  height: 20px;
  background: linear-gradient(90deg, #f0f0f0 25%, #e0e0e0 50%, #f0f0f0 75%);
  background-size: 200% 100%;
  animation: shimmer 1.5s infinite;
  border-radius: 4px;
}
.skeleton-cell.header {
  height: 24px;
  background: #e0e0e0;
  animation: none;
}
@keyframes shimmer {
  0% {
    background-position: 200% 0;
  }
  100% {
    background-position: -200% 0;
  }
}
</style>
```

---

## 4. 技术债务清理

### 4.1 遗留代码迁移计划

| 阶段    | 内容                                                  | 状态      |
| ------- | ----------------------------------------------------- | --------- |
| Phase 1 | `data.js` 核心计算逻辑 → `CalculatorService.ts`       | ✅ 已完成 |
| Phase 2 | `blueprint.js` 编解码 → `BlueprintEncoder/Decoder.ts` | ✅ 已完成 |
| Phase 3 | `data.js` 配方数据 → `recipeMap.json`                 | ⏳ 待开始 |
| Phase 4 | 全局变量 → Pinia Store                                | ⏳ 待开始 |

### 4.2 类型定义完善

| 文件              | 缺失类型             | 优先级 |
| ----------------- | -------------------- | ------ |
| `App.vue`         | `resultItems` 类型   | 高     |
| `bridge.ts`       | `LegacyGlobals` 完善 | 中     |
| `ResultTable.vue` | Props接口            | 中     |

---

## 5. 实施进度追踪

### 5.1 Phase 1: 开发工具链配置

| 任务         | 状态      | 完成日期   |
| ------------ | --------- | ---------- |
| ESLint配置   | ✅ 已完成 | 2026-02-14 |
| Prettier配置 | ✅ 已完成 | 2026-02-14 |
| Git Hooks    | ✅ 已完成 | 2026-02-14 |
| 环境变量配置 | ✅ 已完成 | 2026-02-14 |

### 5.2 Phase 2: 错误处理增强

| 任务              | 状态      | 完成日期   |
| ----------------- | --------- | ---------- |
| ErrorBoundary组件 | ✅ 已完成 | 2026-02-14 |
| 全局错误处理      | ✅ 已完成 | 2026-02-14 |
| 错误上报服务      | 🔲 待开始 | -          |

### 5.3 Phase 3: 测试覆盖完善

| 任务           | 状态      | 完成日期   |
| -------------- | --------- | ---------- |
| vitest配置增强 | ✅ 已完成 | 2026-02-14 |
| 组件测试       | ✅ 已完成 | 2026-02-14 |
| Store测试      | ✅ 已完成 | 2026-02-14 |
| 覆盖率报告     | ✅ 已完成 | 2026-02-14 |

### 5.4 Phase 4: CI/CD建设

| 任务              | 状态      | 完成日期   |
| ----------------- | --------- | ---------- |
| GitHub Actions CI | ✅ 已完成 | 2026-02-14 |
| 自动化部署        | ✅ 已完成 | 2026-02-14 |
| 发布流程          | ✅ 已完成 | 2026-02-14 |

### 5.5 Phase 5: 用户体验优化

| 任务       | 状态      | 完成日期 |
| ---------- | --------- | -------- |
| PWA支持    | 🔲 待开始 | -        |
| SEO优化    | 🔲 待开始 | -        |
| 骨架屏     | 🔲 待开始 | -        |
| 响应式布局 | 🔲 待开始 | -        |

---

## 6. 预估工时

| Phase             | 工时        | 优先级 | 状态             |
| ----------------- | ----------- | ------ | ---------------- |
| Phase 1: 工具链   | 1-2天       | P0     | ✅ 已完成        |
| Phase 2: 错误处理 | 2-3天       | P0     | ✅ 已完成        |
| Phase 3: 测试完善 | 3-5天       | P1     | ✅ 已完成        |
| Phase 4: CI/CD    | 2-3天       | P1     | ✅ 已完成        |
| Phase 5: 用户体验 | 持续        | P2     | 🔲 待开始        |
| **总计**          | **10-15天** | -      | **P0/P1 已完成** |

---

## 7. 参考资源

- [Vue 3 官方文档](https://vuejs.org/)
- [Vitest 配置指南](https://vitest.dev/config/)
- [Vue Test Utils](https://test-utils.vuejs.org/)
- [GitHub Actions 文档](https://docs.github.com/en/actions)
- [Vite PWA Plugin](https://vite-pwa-org.netlify.app/)
