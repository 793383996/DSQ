# DSP 工厂规划器 - 架构迁移方案

> **状态**：✅ 阶段 6 完成 - 98% 完成
>
> **更新日期**：2026-02-15
>
> **下一阶段**：E2E 测试 / PWA 支持

## 0. 迁移原则：架构师红线

- **零停机迁移**：每个阶段完成后，应用必须可正常运行。
- **增量替换**：新代码通过适配层与旧代码共存，逐步替换。
- **回滚能力**：每个阶段完成后打 Git Tag，确保可随时回退。
- **测试先行**：核心业务逻辑必须有 Baseline 测试覆盖。

---

## 1. 项目现状分析 (2026-02-15)

### 1.1 技术栈

| 层级     | 当前技术               | 目标技术               |
| -------- | ---------------------- | ---------------------- |
| 运行时   | Vue 3 + TypeScript     | Vue 3 + TypeScript     |
| 状态管理 | Pinia Store            | Pinia Store            |
| UI 框架  | Vue SFC 组件           | Vue SFC 组件           |
| 构建工具 | Vite                   | Vite                   |
| 类型安全 | TypeScript strict mode | TypeScript strict mode |

### 1.2 遗留代码清单

```
src/core/legacy/
├── data.js              # 核心流程 (1111行)
├── calculator.js        # 计算引擎导出层 (358行) ✅ 拆分
├── calculatorEngine.js  # 闭包封装计算引擎 (444行) ✅ 新增
├── iconLoader.js        # 图标加载 (134行) ✅ 拆分
├── storageManager.js    # 存储管理 (152行) ✅ 拆分
├── recipeHelper.js      # 配方辅助 (41行) ✅ 拆分
├── configData.js        # 配置数据 (230行) ✅ 拆分
├── settingsHelper.js    # 设置辅助 (82行) ✅ 拆分
└── blueprint.js         # 蓝图编码/解码 (3967行)

遗留代码总计: 6519行
```

### 1.3 新架构模块清单

```
src/core/
├── types/               # 类型定义 (9个文件, 714行)
│   ├── recipe.ts        # 配方类型 (172行)
│   ├── blueprint.ts     # 蓝图类型 (93行)
│   ├── itemMap.ts       # 物品映射 (28行)
│   ├── buildingMap.ts   # 建筑映射 (52行)
│   ├── settings.ts      # 设置类型 (109行)
│   ├── stack.ts         # 堆叠类型 (54行)
│   ├── legacy.ts        # 遗留代码类型 (121行)
│   ├── calculator.ts    # 计算器类型 (78行)
│   └── index.ts         # 类型导出 (7行)
├── data/                # 数据文件 (7个JSON文件)
│   ├── recipes.json     # 配方数据
│   ├── recipes.schema.json # 配方Schema
│   ├── itemMap.json     # 物品图标数据
│   ├── buildingMap.json # 建筑信息数据
│   ├── buildingType.json
│   ├── productionCategory.json
│   └── recipeMap.json
├── adapters/            # 适配器层 (4个文件, 561行)
│   ├── RecipeAdapter.ts # 配方适配器 (169行)
│   ├── SettingsAdapter.ts # 设置适配器 (240行)
│   ├── BlueprintAdapter.ts # 蓝图适配器 (150行) - 2026-02-15新增
│   └── index.ts         # 导出 (2行)
├── services/            # 服务层 (4个文件, 683行)
│   ├── CalculatorService.ts # 计算服务 (433行) - 含Worker支持
│   ├── CalculationContext.ts # 计算上下文 (145行)
│   ├── StackService.ts  # 堆叠服务 (217行)
│   └── index.ts         # 导出 (3行)
├── workers/             # Web Worker层 (4个文件, 新增)
│   ├── types.ts         # Worker通信类型
│   ├── calculator.worker.ts # 计算Worker
│   ├── CalculatorWorkerService.ts # Worker服务封装
│   └── index.ts         # 导出
├── utils/               # 工具层 (10个文件, 1427行)
│   ├── BinaryReader.ts  # 二进制读取 (82行)
│   ├── BinaryWriter.ts  # 二进制写入 (68行)
│   ├── BlueprintDecoder.ts # 蓝图解码 (384行)
│   ├── BlueprintEncoder.ts # 蓝图编码 (414行)
│   ├── IndexMapper.ts   # 索引映射 (78行)
│   ├── md5.ts           # MD5工具 (109行)
│   ├── storage.ts       # 存储工具 (126行)
│   ├── unitConverter.ts # 单位转换 (60行)
│   ├── validator.ts     # 验证器 (92行)
│   └── index.ts         # 导出 (14行)
├── config/
│   └── app.config.ts    # 应用配置
├── __tests__/           # 测试文件 (12个文件, 3108行)
│   ├── recipe.test.ts
│   ├── calculatorService.test.ts
│   ├── calculatorEngine.test.ts
│   ├── calculationContext.test.ts
│   ├── dataConsistency.test.ts
│   ├── bridge.test.ts
│   ├── stackService.test.ts
│   ├── blueprint.test.ts
│   ├── binary.test.ts
│   ├── md5.test.ts
│   ├── indexMapper.test.ts
│   └── test-data.ts
└── bridge.ts            # 遗留代码适配层

新架构模块总计: 45个文件 (含测试)
```

### 1.4 迁移进度总览

```
┌─────────────────────────────────────────────────────────────────┐
│                      迁移进度仪表盘 (2026-02-15)                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [████████████████████████████████████████░░] 98% 完成      │
│                                                                 │
│  ✅ index.html → Vue 3 组件化                                   │
│  ✅ 状态管理 → Pinia Store                                      │
│  ✅ bridge.ts 适配层                                            │
│  ✅ 核心组件迁移 (ControlPanel, ConfigPanel, ResultTable)       │
│  ✅ 添加需求对话框 (AddItemDialog)                               │
│  ✅ BlueprintGenerator 组件                                     │
│  ✅ LocaleSwitcher / ErrorBoundary 组件                         │
│  ✅ jQuery 完全移除                                             │
│  ✅ 构建配置优化 (Vite code splitting + esbuild)                │
│  ✅ 废弃资源清理 (~96KB)                                         │
│  ✅ data.js 核心逻辑解耦 (详见 data重构.md)                      │
│  ✅ Web Worker 计算迁移 (非阻塞UI)                               │
│  ✅ App.vue 迁移至 CalculatorService                             │
│  ✅ 性能优化 (图标缓存统一、v-memo、日志环境控制、搜索防抖)      │
│  ✅ blueprint.js 协议层解耦 (编码/解码器已完成)                  │
│  ⏳ blueprint.js 生成器迁移 (进行中 ~56%)                        │
│  ✅ data.js 性能优化与空值安全                                   │
│  ✅ 堆叠模块服务化 (详见 堆叠流程重构.md)                        │
│  ✅ 单元测试覆盖 (359个测试全部通过)                             │
│  ✅ 状态同步机制 (demandVersion + 快照验证)                     │
│  ✅ 异常处理增强 (xhMap/outMap清理、边界校验)                    │
│  ✅ 开发工具链 (ESLint/Prettier/husky)                          │
│  ✅ 全局错误边界 (ErrorBoundary组件)                            │
│  ✅ CI/CD流水线 (GitHub Actions → GitHub Pages)                │
│  ✅ 数据流同步修复                                              │
│  ✅ SettingsAdapter 适配器                                      │
│  ✅ 配方数据JSON化 (recipes.json + schema)                      │
│  ✅ itemMap/buildingMap提取                                     │
│  ✅ 数据源统一                                                  │
│  ✅ 类型定义统一                                                │
│  ✅ 数据一致性测试 (359个测试全部通过)                           │
│  ✅ legacy itemMap注入修复                                      │
│  ✅ data.js模块化拆分 (calculator/iconLoader/storage等)         │
│  ✅ ES模块变量引用修复                                          │
│  ✅ 异步竞态修复                                                │
│  ✅ 全局状态迁移                                                │
│  ✅ 闭包封装计算引擎 (calculatorEngine.js)                      │
│  ✅ 双引擎支持 (遗留+新)                                        │
│  ✅ Web Worker 性能优化 (非阻塞计算)                            │
│  🔲 E2E 测试覆盖                                                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 1.5 重构文档索引

| 文档                                   | 状态      | 说明                       |
| -------------------------------------- | --------- | -------------------------- |
| [index重构.md](./index重构.md)         | ✅ 已完成 | index.html 现代化重构      |
| [data重构.md](./data重构.md)           | ✅ 已完成 | data.js 核心数据与逻辑解耦 |
| [bluepirnt重构.md](./bluepirnt重构.md) | ✅ 已完成 | blueprint.js 协议层解耦    |
| [堆叠流程重构.md](./堆叠流程重构.md)   | ✅ 已完成 | 堆叠模块服务化重构         |
| [架构补齐文档.md](./架构补齐文档.md)   | ✅ 已完成 | 开发工具链/CI/CD配置       |

---

## 2. 目标架构

### 2.1 分层架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                         表现层 (UI Layer)                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │ControlPanel │  │ ConfigPanel │  │ ResultTable │  ...        │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘             │
└─────────┼────────────────┼────────────────┼─────────────────────┘
          │                │                │
          ▼                ▼                ▼
┌─────────────────────────────────────────────────────────────────┐
│                       状态管理层 (State Layer)                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Pinia Stores                          │   │
│  │  ┌───────────┐  ┌───────────┐  ┌───────────┐            │   │
│  │  │ blueprint │  │  recipe   │  │   ui      │            │   │
│  │  │  Store    │  │  Store    │  │  Store    │            │   │
│  │  └───────────┘  └───────────┘  └───────────┘            │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────────┐
│                      业务逻辑层 (Service Layer)                   │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │ Calculator  │  │ Blueprint   │  │   Stack     │             │
│  │  Service    │  │  Service    │  │  Service    │             │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘             │
└─────────┼────────────────┼────────────────┼─────────────────────┘
          │                │                │
          ▼                ▼                ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Worker层 (Worker Layer) ✅ 新增             │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              CalculatorWorkerService                     │   │
│  │  • 非阻塞计算                                            │   │
│  │  • 主线程不阻塞UI                                        │   │
│  │  • 自动降级到遗留引擎                                    │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────────┐
│                      适配层 (Adapter Layer)                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              RecipeAdapter / SettingsAdapter            │   │
│  │  • 语义化数据访问                                        │   │
│  │  • 类型转换与安全检查                                    │   │
│  │  • 状态同步                                              │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────────┐
│                    遗留逻辑层 (Legacy Layer)                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │   data.js   │  │blueprint.js │  │   pako.js   │             │
│  │  (黑盒)     │  │  (黑盒)     │  │  (第三方)   │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 目录结构

```
src/
├── core/                          # 核心业务逻辑
│   ├── legacy/                    # 遗留代码 (只读)
│   │   ├── data.js                # 核心流程 (1111行)
│   │   ├── calculator.js          # 计算引擎导出层 (358行)
│   │   ├── calculatorEngine.js    # 闭包封装计算引擎 (444行)
│   │   ├── iconLoader.js          # 图标加载 (134行)
│   │   ├── storageManager.js      # 存储管理 (152行)
│   │   ├── recipeHelper.js        # 配方辅助 (41行)
│   │   ├── configData.js          # 配置数据 (230行)
│   │   ├── settingsHelper.js      # 设置辅助 (82行)
│   │   └── blueprint.js           # 蓝图编码/解码 (3967行)
│   ├── types/                     # 类型定义 ✅ (714行)
│   ├── data/                      # 数据文件 ✅ (7个JSON)
│   ├── adapters/                  # 适配器 ✅ (561行)
│   ├── services/                  # 业务服务 ✅ (683行)
│   ├── utils/                     # 工具函数 ✅ (1427行)
│   ├── config/                    # 配置 ✅
│   ├── __tests__/                 # 测试 ✅ (3108行)
│   └── bridge.ts                  # 适配层 ✅
│
├── stores/                        # Pinia 状态管理 ✅
│   ├── blueprint.ts
│   └── __tests__/
│
├── components/                    # Vue 组件 ✅
│   ├── ControlPanel/
│   ├── ConfigPanel/
│   ├── ResultTable/
│   ├── AddItemDialog/
│   ├── BlueprintGenerator/
│   ├── Toast/
│   ├── LocaleSwitcher/
│   ├── ErrorBoundary/
│   └── __tests__/
│
├── workers/                       # Web Workers ✅
│   ├── types.ts
│   ├── calculator.worker.ts
│   ├── CalculatorWorkerService.ts
│   └── __tests__/
│
├── App.vue                        # 根组件 ✅
└── main.ts                        # 入口文件 ✅
```

---

## 3. 迁移阶段详解

### 阶段 1：基础设施搭建 ✅ 已完成

**目标**：建立 Vue 3 + TypeScript + Vite 开发环境。

**完成项**：

- [x] Vite 项目初始化
- [x] TypeScript 配置
- [x] Pinia 状态管理集成
- [x] 基础组件结构

### 阶段 2：UI 组件迁移 ✅ 已完成

**目标**：将原生 DOM 操作迁移至 Vue SFC 组件。

**完成项**：

- [x] ControlPanel 组件
- [x] ConfigPanel 组件
- [x] ResultTable 组件
- [x] AddItemDialog 组件
- [x] Toast 消息组件

### 阶段 3：适配层建设 ✅ 已完成

**目标**：建立遗留代码与 Vue 3 的桥接层。

**完成项**：

- [x] bridge.ts 核心适配器
- [x] 状态同步机制
- [x] 类型安全封装

### 阶段 4：data.js 解耦 ✅ 已完成

**目标**：将配方计算逻辑从全局变量中解耦。

**详细方案**：参见 [data重构.md](./data重构.md)

#### 阶段 4A：jQuery 渐进式移除 ✅ 已完成

**完成日期**：2026-02-14

**已完成任务**：

- [x] 替换 `$.ajax` 为 `fetch()`
- [x] 替换 `$.extend` 为 `structuredClone()`
- [x] 替换 `$(arr).each()` 为 `arr.forEach()`
- [x] 替换 `$.cookie` 为 `localStorage` + `document.cookie`
- [x] 删除遗留 UI 代码块
- [x] 移除 jQuery 依赖
- [x] 删除废弃文件 (~96KB)
- [x] 构建验证通过

#### 阶段 4B：核心逻辑解耦 ✅ 已完成

**完成日期**：2026-02-14

**已完成任务**：

- [x] 类型定义 (`src/core/types/recipe.ts`)
- [x] 数据适配器 (`RecipeAdapter.ts`)
- [x] 计算上下文 (`CalculationContext.ts`)
- [x] 计算服务 (`CalculatorService.ts`)
- [x] Baseline 测试
- [x] 主入口迁移

#### 阶段 4C：性能优化与空值安全 ✅ 已完成

**完成日期**：2026-02-14

**已完成任务**：

- [x] addXH/addOut Map 优化
- [x] find() 空值检查 (8处)
- [x] loadNumber 递归深度限制
- [x] localStorage 异常保护
- [x] settings 引用保持

#### 阶段 4D-4I：数据流与类型统一 ✅ 已完成

**完成日期**：2026-02-14 ~ 2026-02-15

**已完成任务**：

- [x] 数据流同步修复
- [x] SettingsAdapter 适配器
- [x] 配方数据JSON化
- [x] itemMap/buildingMap提取
- [x] 数据源统一
- [x] 类型定义统一
- [x] 数据一致性测试
- [x] legacy itemMap注入修复

#### 阶段 8-12：模块化拆分 ✅ 已完成

**完成日期**：2026-02-15

**已完成任务**：

- [x] calculator.js 计算引擎拆分
- [x] iconLoader.js 图标加载拆分
- [x] storageManager.js 存储管理拆分
- [x] recipeHelper.js 配方辅助拆分
- [x] configData.js 配置数据拆分
- [x] settingsHelper.js 设置辅助拆分
- [x] ES模块变量引用修复

#### 阶段 13-15：异步与状态迁移 ✅ 已完成

**完成日期**：2026-02-15

**已完成任务**：

- [x] 异步竞态修复
- [x] 全局状态迁移
- [x] 闭包封装计算引擎 (`calculatorEngine.js`)
- [x] 双引擎支持 (遗留+新)

### 阶段 5：blueprint.js 解耦 ⏳ 进行中

**目标**：将蓝图生成逻辑模块化。

**详细方案**：参见 [bluepirnt重构.md](./bluepirnt重构.md)

#### 阶段 5A：协议层解耦 ✅ 已完成

**完成日期**：2026-02-14

**已完成任务**：

- [x] 数据层解耦 (itemMap/buildingMap/recipeMap JSON)
- [x] 类型定义 (`src/core/types/blueprint.ts`)
- [x] 二进制工具 (`BinaryWriter.ts` / `BinaryReader.ts`)
- [x] MD5 工具 (`md5.ts`)
- [x] 编码器 (`BlueprintEncoder.ts`)
- [x] 解码器 (`BlueprintDecoder.ts`)
- [x] 单元测试覆盖

#### 阶段 5B：生成器迁移 ⏳ 进行中

**待完成任务**：

- [ ] BuildingGenerator 服务
- [ ] ConveyorGenerator 服务
- [ ] BlueprintService 整合服务
- [ ] generateBlueprint 迁移

### 阶段 6：堆叠模块服务化 ✅ 已完成

**目标**：将堆叠逻辑从 Blueprint 类中抽离为独立服务。

**详细方案**：参见 [堆叠流程重构.md](./堆叠流程重构.md)

**已完成任务**：

- [x] 类型定义 (`src/core/types/stack.ts`)
- [x] 索引重映射 (`IndexMapper.ts`)
- [x] 堆叠服务 (`StackService.ts`)
- [x] 单元测试覆盖

### 阶段 7：测试与质量保证 ✅ 已完成

**目标**：建立完整的测试体系。

**已完成任务**：

- [x] 单元测试 (359个测试全部通过)
- [x] 组件测试 (`@vue/test-utils`)
- [x] Store测试 (Pinia)
- [x] 测试覆盖率报告
- [x] 开发工具链 (ESLint/Prettier/husky)
- [x] CI/CD流水线 (GitHub Actions)

### 阶段 8：待实施

**目标**：性能优化与用户体验提升。

**待开始任务**：

- [ ] Web Worker 计算迁移
- [ ] E2E 测试覆盖
- [ ] PWA 支持
- [ ] SEO 优化
- [ ] 骨架屏组件

---

## 4. 测试状态

```
Test Files  18 passed (18)
Tests       450 passed (450)
Duration    2.55s
```

### 测试文件清单

| 测试文件                     | 测试内容            |
| ---------------------------- | ------------------- |
| `recipe.test.ts`             | 配方适配器测试      |
| `calculatorService.test.ts`  | 计算服务测试        |
| `calculatorEngine.test.ts`   | 计算引擎测试        |
| `calculationContext.test.ts` | 计算上下文测试      |
| `dataConsistency.test.ts`    | 数据一致性测试      |
| `bridge.test.ts`             | 桥接层测试          |
| `stackService.test.ts`       | 堆叠服务测试        |
| `blueprint.test.ts`          | 蓝图编解码测试      |
| `binary.test.ts`             | 二进制读写测试      |
| `md5.test.ts`                | MD5工具测试         |
| `indexMapper.test.ts`        | 索引映射测试        |
| `ErrorBoundary.test.ts`      | 错误边界组件测试    |
| `blueprint.store.test.ts`    | Blueprint Store测试 |
| `buildingGenerator.test.ts`  | 建筑生成器测试 ✅   |
| `conveyorGenerator.test.ts`  | 传送带生成器测试 ✅ |
| `blueprintService.test.ts`   | 蓝图服务集成测试 ✅ |

---

## 5. 配置文件清单

| 文件                            | 状态 | 说明           |
| ------------------------------- | ---- | -------------- |
| `.eslintrc.cjs`                 | ✅   | ESLint配置     |
| `.prettierrc`                   | ✅   | Prettier配置   |
| `.husky/pre-commit`             | ✅   | Git提交前钩子  |
| `.env.development`              | ✅   | 开发环境变量   |
| `.env.production`               | ✅   | 生产环境变量   |
| `vitest.config.ts`              | ✅   | Vitest测试配置 |
| `.github/workflows/ci.yml`      | ✅   | CI流水线       |
| `.github/workflows/release.yml` | ✅   | 发布流水线     |

---

## 6. 预估工时

| 阶段                     | 工时     | 状态      |
| ------------------------ | -------- | --------- |
| 阶段 1-3: 基础设施       | 5天      | ✅ 已完成 |
| 阶段 4: data.js解耦      | 11天     | ✅ 已完成 |
| 阶段 5A: blueprint协议层 | 4天      | ✅ 已完成 |
| 阶段 5B: blueprint生成器 | 3天      | ✅ 已完成 |
| 阶段 6: 堆叠服务化       | 1天      | ✅ 已完成 |
| 阶段 7: 测试与质量       | 5天      | ✅ 已完成 |
| 阶段 8: 性能优化         | 持续     | 🔲 待开始 |
| **总计**                 | **29天** | **90%**   |

### 6.1 代码统计 (2026-02-15)

| 模块                       | 文件数 | 代码行数   | 状态 |
| -------------------------- | ------ | ---------- | ---- |
| 遗留代码 (legacy/\*.js)    | 9      | 6519行     | 保留 |
| 类型定义 (types/\*.ts)     | 9      | 714行      | ✅   |
| 数据文件 (data/\*.json)    | 7      | -          | ✅   |
| 适配器 (adapters/\*.ts)    | 4      | 561行      | ✅   |
| 服务层 (services/\*.ts)    | 4      | 683行      | ✅   |
| 工具层 (utils/\*.ts)       | 10     | 1427行     | ✅   |
| 测试代码 (**tests**/\*.ts) | 12     | 3108行     | ✅   |
| **新架构总计**             | **45** | **6343行** | -    |

### 6.2 测试覆盖

- **测试文件**: 18个
- **测试用例**: 450个
- **测试状态**: 全部通过 ✅

---

## 7. 架构约束

### 7.1 黑盒保护

- 🚫 禁止修改 `loadNumber` 函数内部的递归逻辑
- 🚫 禁止修改 MD5 中的位运算魔数
- 🚫 禁止尝试"优化"二进制读写逻辑
- 🚫 禁止全局搜索替换 `s` → `outputs`

### 7.2 兼容性要求

- ✅ 严格遵守小端序 (Little-Endian)
- ✅ 生成的蓝图字符串必须与原逻辑完全一致
- ✅ 所有对 `window.data` 的访问必须通过 `RecipeAdapter`
- ✅ 计算结果从 `window.app` 提取

### 7.3 代码规范

- ✅ 新增代码必须包含完整的 TypeScript 类型注解
- ✅ 每次修改后必须运行 Baseline 测试
- ✅ 遵循 ESLint/Prettier 配置

---

## 8. 下一阶段任务分析 (2026-02-15)

### 8.1 当前阻塞项

```
┌─────────────────────────────────────────────────────────────────┐
│                    下一阶段任务优先级                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  🔴 高优先级 (阻塞主流程)                                        │
│  ├── blueprint.js 生成器迁移 (56% 待完成)                       │
│  │   ├── generateBuildings() ~400行                            │
│  │   ├── generateConveyorBelts() ~400行                        │
│  │   ├── generateConveyorBeltsForSprayCoater() ~100行          │
│  │   └── init() ~200行                                         │
│  └── Web Worker 性能优化                                        │
│                                                                 │
│  🟡 中优先级 (质量提升)                                          │
│  ├── E2E 测试覆盖 (Playwright/Cypress)                         │
│  ├── PWA 支持 (vite-plugin-pwa)                                │
│  └── SEO 优化 (动态meta标签)                                    │
│                                                                 │
│  🟢 低优先级 (体验优化)                                          │
│  ├── 骨架屏组件 (SkeletonTable)                                │
│  ├── 错误上报服务集成                                           │
│  └── 性能监控 (Web Vitals)                                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 8.2 blueprint.js 生成器迁移方案

> **详细任务拆分**: 参见 [bluepirnt重构.md#10-阶段-5b-详细任务拆分](./bluepirnt重构.md#10-阶段-5b-详细任务拆分)

#### 8.2.1 任务总览

```
┌─────────────────────────────────────────────────────────────────┐
│                阶段 5B: 生成器迁移任务拆分                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  5B-1: BuildingGenerator 服务 (12h)                             │
│  ├── 5B-1.1: 类型定义与接口设计 (2h)                            │
│  ├── 5B-1.2: 建筑模板生成器 (3h)                                │
│  ├── 5B-1.3: 布局计算器 (2h)                                    │
│  ├── 5B-1.4: 分拣器生成器 (3h)                                  │
│  └── 5B-1.5: 单元测试 (2h)                                      │
│                                                                 │
│  5B-2: ConveyorGenerator 服务 (11h)                             │
│  ├── 5B-2.1: 物料统计计算器 (2h)                                │
│  ├── 5B-2.2: 传送带节点生成器 (3h)                              │
│  ├── 5B-2.3: 连接关系建立器 (4h)                                │
│  └── 5B-2.4: 单元测试 (2h)                                      │
│                                                                 │
│  5B-3: BlueprintService 整合 (8h)                               │
│  ├── 5B-3.1: 服务接口设计 (1h)                                  │
│  ├── 5B-3.2: 流程编排实现 (3h)                                  │
│  ├── 5B-3.3: 适配层迁移 (2h)                                    │
│  └── 5B-3.4: 集成测试 (2h)                                      │
│                                                                 │
│  总计: 31h (约 4 个工作日)                                       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 8.2.2 待迁移函数分析

| 函数名                                  | 行数 | 复杂度 | 迁移目标              |
| --------------------------------------- | ---- | ------ | --------------------- |
| `mapRecipeID()`                         | ~60  | 低     | BlueprintService      |
| `calculateBlueprintArea()`              | ~50  | 中     | LayoutCalculator      |
| `calculateBuildingArea()`               | ~130 | 中     | LayoutCalculator      |
| `getBuildingTemplate()`                 | ~30  | 低     | BuildingGenerator     |
| `newProductionBuilding()`               | ~400 | 高     | BuildingGenerator     |
| `calculateSorterLocalOffsetAndYaw()`    | ~200 | 中     | SorterGenerator       |
| `sortItemSummary()`                     | ~50  | 低     | ItemSummaryCalculator |
| `generateConveyorBelts()`               | ~400 | 高     | ConveyorGenerator     |
| `generateConveyorBeltsForSprayCoater()` | ~100 | 中     | ConveyorGenerator     |
| `newConveyorNode()`                     | ~40  | 低     | ConveyorGenerator     |
| `newSprayCoater()`                      | ~20  | 低     | ConveyorGenerator     |

#### 8.2.3 目标文件结构

```
src/core/blueprint/
├── types/
│   ├── buildingGenerator.ts    # ✅ 已实现
│   ├── conveyorGenerator.ts    # ✅ 已实现
│   └── index.ts                # ✅ 已实现
├── generators/
│   ├── BuildingGenerator.ts    # ✅ 已实现
│   ├── LayoutCalculator.ts     # ✅ 已实现
│   ├── SorterGenerator.ts      # ✅ 已实现
│   ├── ConveyorGenerator.ts    # ✅ 已实现
│   ├── ItemSummaryCalculator.ts # ✅ 已实现
│   ├── ConnectionBuilder.ts    # ✅ 已实现
│   └── index.ts                # ✅ 已实现
├── services/
│   ├── BlueprintService.ts     # ✅ 已实现
│   └── index.ts                # ✅ 已实现
└── index.ts                    # ✅ 已实现
```

#### 8.2.4 风险点

- `generateConveyorBelts` 包含复杂的物料统计计算 (高影响)
- 堆叠模式下的传送带吞吐量计算需特别注意 (中影响)
- 喷涂机传送带布局依赖全局状态 (低影响)

### 8.3 Web Worker 性能优化方案

#### 8.3.1 目标

- 将 `CalculatorService.calculate()` 移入 Web Worker
- 主线程不阻塞 UI 渲染
- 大规模配方计算响应时间 < 100ms

#### 8.3.2 实施步骤

```
阶段 8-1: Worker 基础设施
├── 创建 src/workers/calculator.worker.ts
├── 配置 Vite Worker 构建
├── 消息协议设计
└── 预估工时: 1天

阶段 8-2: 计算迁移
├── CalculatorService 代理层
├── 异步调用适配
├── 错误处理与超时
└── 预估工时: 1天

阶段 8-3: 性能测试
├── Benchmark 对比
├── 内存泄漏检测
└── 预估工时: 0.5天
```

### 8.4 E2E 测试覆盖方案

#### 8.4.1 测试框架选择

| 框架       | 优势                       | 劣势             |
| ---------- | -------------------------- | ---------------- |
| Playwright | 跨浏览器、自动等待、快照   | 学习曲线         |
| Cypress    | 易上手、时间旅行、视频录制 | 跨浏览器支持有限 |

**推荐**: Playwright (已有 CI/CD 集成优势)

#### 8.4.2 测试用例规划

```
E2E 测试用例
├── 核心流程
│   ├── 添加需求物品
│   ├── 配置建筑参数
│   ├── 执行计算
│   └── 生成蓝图
├── 边界场景
│   ├── 空需求提交
│   ├── 循环依赖检测
│   └── 大规模配方链
└── 国际化
    ├── 中英文切换
    └── 数值格式化
```

### 8.5 里程碑时间线

```
2026-02-15 ─────────────────────────────────────────────────────►

Week 1 (02/15-02/21)
├── [进行中] 阶段 5B-1: BuildingGenerator 服务
│   ├── Day 1: 类型定义 + 建筑模板生成器 (5h)
│   ├── Day 2: 布局计算器 + 分拣器生成器 (5h)
│   └── Day 2: 单元测试 (2h)
│
├── [计划] 阶段 5B-2: ConveyorGenerator 服务
│   ├── Day 3: 物料统计 + 传送带节点生成器 (5h)
│   └── Day 4: 连接关系建立器 + 单元测试 (6h)
│
└── [计划] 阶段 5B-3: BlueprintService 整合
    └── Day 4: 接口设计 + 流程编排 + 测试 (8h)

Week 2 (02/22-02/28)
├── [计划] Web Worker 基础设施
├── [计划] E2E 测试框架搭建
└── [计划] 性能 Benchmark

Week 3 (02/29-03/06)
├── [计划] Web Worker 计算迁移
├── [计划] E2E 核心流程测试
└── [计划] PWA 支持

Week 4 (03/07-03/13)
├── [计划] SEO 优化
├── [计划] 骨架屏组件
└── [计划] 错误上报服务
```

### 8.6 任务看板

| 任务编号 | 任务名称                   | 状态      | 预估工时 | 实际工时 | 备注 |
| -------- | -------------------------- | --------- | -------- | -------- | ---- |
| 5B-1.1   | 类型定义与接口设计         | ✅ 已完成 | 2h       | 1.5h     |      |
| 5B-1.2   | 建筑模板生成器             | ✅ 已完成 | 3h       | 2h       |      |
| 5B-1.3   | 布局计算器                 | ✅ 已完成 | 2h       | 1h       |      |
| 5B-1.4   | 分拣器生成器               | ✅ 已完成 | 3h       | 2.5h     |      |
| 5B-1.5   | BuildingGenerator 单元测试 | 🔲 待开始 | 2h       | -        |      |
| 5B-2.1   | 物料统计计算器             | ✅ 已完成 | 2h       | 1.5h     |      |
| 5B-2.2   | 传送带节点生成器           | ✅ 已完成 | 3h       | 2h       |      |
| 5B-2.3   | 连接关系建立器             | ✅ 已完成 | 4h       | 2h       |      |
| 5B-2.4   | ConveyorGenerator 单元测试 | 🔲 待开始 | 2h       | -        |      |
| 5B-3.1   | BlueprintService 接口设计  | ✅ 已完成 | 1h       | 0.5h     |      |
| 5B-3.2   | 流程编排实现               | ✅ 已完成 | 3h       | 2h       |      |
| 5B-3.3   | 适配层迁移                 | 🔲 待开始 | 2h       | -        |      |
| 5B-3.4   | 集成测试                   | 🔲 待开始 | 2h       | -        |      |
