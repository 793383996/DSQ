# DSP 工厂规划器 - 架构迁移方案

## 0. 迁移原则：架构师红线

- **零停机迁移**：每个阶段完成后，应用必须可正常运行。
- **增量替换**：新代码通过适配层与旧代码共存，逐步替换。
- **回滚能力**：每个阶段完成后打 Git Tag，确保可随时回退。
- **测试先行**：核心业务逻辑必须有 Baseline 测试覆盖。

---

## 1. 项目现状分析 (2026-02-13)

### 1.1 技术栈

| 层级     | 当前技术                 | 目标技术               |
| -------- | ------------------------ | ---------------------- |
| 运行时   | 浏览器原生 JS            | Vue 3 + TypeScript     |
| 状态管理 | 全局变量 (`window.data`) | Pinia Store            |
| UI 框架  | 原生 DOM 操作            | Vue SFC 组件           |
| 构建工具 | 无 (直接引入)            | Vite                   |
| 类型安全 | 无                       | TypeScript strict mode |

### 1.2 遗留代码清单

```
public/
├── js/
│   ├── data.js          # 配方数据与计算逻辑 (~2000 行)
│   ├── blueprint.js     # 蓝图生成 (~800 行)
│   └── md5.js           # MD5 算法 (内置于 blueprint.js)
└── index.html           # 入口页面 (已迁移至 Vue)
```

### 1.3 迁移进度总览

```
┌─────────────────────────────────────────────────────────────────┐
│                      迁移进度仪表盘                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [███████████████████████████████████████] 99% 完成        │
│                                                                 │
│  ✅ index.html → Vue 3 组件化 (详见 index重构.md)               │
│  ✅ 状态管理 → Pinia Store                                      │
│  ✅ bridge.ts 适配层                                            │
│  ✅ 核心组件迁移 (ControlPanel, ConfigPanel, ResultTable)       │
│  ✅ 添加需求对话框 (AddItemDialog)                               │
│  ✅ jQuery 完全移除 (阶段 4A)                                    │
│  ✅ 构建配置优化 (Vite code splitting + esbuild)                │
│  ✅ 废弃资源清理 (~96KB)                                         │
│  ✅ data.js 核心逻辑解耦 (阶段 4B, 详见 data重构.md)             │
│  ✅ App.vue 迁移至 CalculatorService                             │
│  ✅ 性能优化 (图标缓存统一、v-memo、日志环境控制、搜索防抖)      │
│  ✅ blueprint.js 协议层解耦 (阶段 5, 编码/解码器已完成)         │
│  ✅ data.js 性能优化与空值安全 (阶段 4C)                        │
│  ✅ 堆叠模块服务化 (阶段 5A, 详见 堆叠流程重构.md)              │
│  ✅ 单元测试覆盖 (148个测试全部通过)                            │
│  ✅ 状态同步机制 (demandVersion + 快照验证)                     │
│  ✅ 异常处理增强 (xhMap/outMap清理、边界校验)                   │
│  ✅ 开发工具链 (ESLint/Prettier/husky)                          │
│  ✅ 全局错误边界 (ErrorBoundary组件)                            │
│  ✅ CI/CD流水线 (GitHub Actions)                                │
│  ⏳ Web Worker 性能优化                                          │
│  🔲 E2E 测试覆盖                                                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 1.4 重构文档索引

| 文档                                   | 状态      | 说明                       |
| -------------------------------------- | --------- | -------------------------- |
| [index重构.md](./index重构.md)         | ✅ 已完成 | index.html 现代化重构      |
| [data重构.md](./data重构.md)           | ✅ 已完成 | data.js 核心数据与逻辑解耦 |
| [bluepirnt重构.md](./bluepirnt重构.md) | ✅ 已完成 | blueprint.js 协议层解耦    |
| [堆叠流程重构.md](./堆叠流程重构.md)   | ✅ 已完成 | 堆叠模块服务化重构         |

---

## 2. 目标架构

### 2.1 分层架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                         表现层 (UI Layer)                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │ControlPanel │  │ ConfigPanel │  │ ResultTable │  ...        │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘             │
└─────────┼────────────────┼────────────────┼─────────────────────┘
          │                │                │
          ▼                ▼                ▼
┌─────────────────────────────────────────────────────────────────┐
│                       状态管理层 (State Layer)                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Pinia Stores                          │   │
│  │  ┌───────────┐  ┌───────────┐  ┌───────────┐            │   │
│  │  │ blueprint │  │  recipe   │  │   ui      │            │   │
│  │  │  Store    │  │  Store    │  │  Store    │            │   │
│  │  └───────────┘  └───────────┘  └───────────┘            │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────────┐
│                      业务逻辑层 (Service Layer)                   │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │ Calculator  │  │ Blueprint   │  │   Recipe    │             │
│  │  Service    │  │  Service    │  │  Service    │             │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘             │
└─────────┼────────────────┼────────────────┼─────────────────────┘
          │                │                │
          ▼                ▼                ▼
┌─────────────────────────────────────────────────────────────────┐
│                      适配层 (Adapter Layer)                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                      bridge.ts                          │   │
│  │  • 遗留代码调用封装                                       │   │
│  │  • 类型转换与安全检查                                     │   │
│  │  • 状态同步                                              │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────────┐
│                    遗留逻辑层 (Legacy Layer)                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │   data.js   │  │blueprint.js │  │   pako.js   │             │
│  │  (黑盒)     │  │  (黑盒)     │  │  (第三方)   │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 目录结构规划

```
src/
├── core/                          # 核心业务逻辑
│   ├── legacy/                    # 遗留代码 (只读)
│   │   ├── data.js
│   │   ├── blueprint.js
│   │   └── pako.js
│   ├── bridge.ts                  # 适配层 ✅
│   ├── types/                     # 类型定义
│   │   ├── recipe.ts
│   │   ├── blueprint.ts
│   │   └── index.ts
│   ├── services/                  # 业务服务
│   │   ├── CalculatorService.ts
│   │   ├── BlueprintService.ts
│   │   └── RecipeService.ts
│   └── utils/                     # 工具函数
│       ├── Crypto.ts              # MD5 封装
│       └── BlueprintBuffer.ts     # 二进制读写
│
├── stores/                        # Pinia 状态管理
│   ├── blueprint.ts               # ✅ 已完成
│   ├── recipe.ts                  # 待实现
│   └── ui.ts                      # 待实现
│
├── components/                    # Vue 组件
│   ├── ControlPanel/              # ✅ 已完成
│   ├── ConfigPanel/               # ✅ 已完成
│   ├── ResultTable/               # ✅ 已完成
│   ├── AddItemDialog/             # ✅ 已完成
│   └── Toast/                     # ✅ 已完成
│
├── workers/                       # Web Workers
│   └── calculator.worker.ts       # 计算密集型任务
│
├── App.vue                        # 根组件
└── main.ts                        # 入口文件
```

---

## 3. 迁移阶段详解

### 阶段 1：基础设施搭建 ✅ 已完成

**目标**：建立 Vue 3 + TypeScript + Vite 开发环境。

**完成项**：

- [x] Vite 项目初始化
- [x] TypeScript 配置
- [x] Pinia 状态管理集成
- [x] 基础组件结构

### 阶段 2：UI 组件迁移 ✅ 已完成

**目标**：将原生 DOM 操作迁移至 Vue SFC 组件。

**完成项**：

- [x] ControlPanel 组件
- [x] ConfigPanel 组件
- [x] ResultTable 组件
- [x] AddItemDialog 组件
- [x] Toast 消息组件

### 阶段 3：适配层建设 ✅ 已完成

**目标**：建立遗留代码与 Vue 3 的桥接层。

**完成项**：

- [x] bridge.ts 核心适配器
- [x] 状态同步机制
- [x] 类型安全封装

### 阶段 4：data.js 解耦 ⏳ 进行中

**目标**：将配方计算逻辑从全局变量中解耦。

**详细方案**：参见 [data重构.md](./data重构.md)

#### 阶段 4A：jQuery 渐进式移除 ✅ 已完成

**目标**：移除 jQuery 依赖，保持架构简洁。

**完成日期**：2026-02-14

**已完成任务**：

- [x] 替换 `$.ajax` 为 `fetch()`
- [x] 替换 `$.extend` 为 `structuredClone()`
- [x] 替换 `$(arr).each()` 为 `arr.forEach()`
- [x] 替换 `$.cookie` 为 `localStorage` + `document.cookie`
- [x] 删除遗留 UI 代码块 (L4523-L6040)
- [x] 移除 jQuery import
- [x] 从 package.json 移除 jQuery 依赖
- [x] 删除废弃文件 (jquery-2.2.4.min.js, jquery.cookie.min.js, jquery.tips.js, vue.min.js)
- [x] 构建验证通过

**优化成果**：

- 构建产物优化：主业务代码 gzip 减少 31 KB
- 废弃资源清理：~96 KB
- Vite 配置优化：code splitting + esbuild minification

#### 阶段 4B：核心逻辑解耦 ✅ 已完成

**目标**：将配方计算逻辑服务化，消除全局变量污染。

**关键任务**：

- [x] **类型定义** - 创建 `src/core/types/recipe.ts`
  - [x] `IRecipe` 配方接口 (语义化 s/q/t/m 字段)
  - [x] `IRecipeItem` 物品项接口
  - [x] `ICalculationResult` 计算结果接口
  - [x] `IDemand` 需求项接口
  - [x] `IRawRecipe` 原始配方数据格式
- [x] **数据适配器** - 创建 `src/core/adapters/RecipeAdapter.ts`
  - [x] 从原始数据加载配方
  - [x] 构建产物/原料索引
  - [x] 提供语义化查询接口
- [x] **计算上下文** - 创建 `src/core/services/CalculationContext.ts`
  - [x] 替代 xh_list, out_list, items 全局变量
  - [x] 递归深度监控
  - [x] 计算耗时统计
  - [x] 填充消耗/产出数据
- [x] **计算服务** - 创建 `src/core/services/CalculatorService.ts`
  - [x] 封装 `update_all` 逻辑 (黑盒保护)
  - [x] 提供异步计算接口
  - [x] 结果提取与封装 (从 window.app)
  - [x] 修复原 bridge.runCalculation 调用错误
- [x] **Baseline 测试** - 创建 `src/core/__tests__/recipe.test.ts`
  - [x] RecipeAdapter 单元测试
  - [x] CalculatorService 集成测试
  - [x] 回归测试用例
- [x] **主入口迁移** - App.vue 切换至 CalculatorService
  - [x] 移除对 bridge.runCalculation 的依赖
  - [x] 使用 calculatorService.calculate()
  - [x] 结果处理使用 items + items2

**架构约束**：

- 🚫 禁止修改 `loadNumber` 函数内部的递归逻辑
- 🚫 禁止全局搜索替换 `s` → `outputs`，会破坏递归链
- ✅ 所有对 `window.data` 的访问必须通过 `RecipeAdapter`
- ✅ 计算结果从 `window.app` 提取，不是 `window.items`

#### 阶段 4C：性能优化与空值安全 ✅ 已完成

**目标**：优化计算性能，增强边界条件处理。

**完成日期**：2026-02-14

**已完成任务**：

- [x] **addXH/addOut Map 优化** - O(n) → O(1) 查找
  - [x] 添加 `xhMap` / `outMap` 辅助索引
  - [x] `update_all()` 重置时同步清空 Map
- [x] **find() 空值检查** - 8处关键位置
  - [x] `getXhs()` - `item.id` 访问前检查
  - [x] `doMergeMul()` - `item.s` 访问前检查
  - [x] `mergeMul()` - `item.id` 访问前检查
  - [x] `checkResult()` - `item/info` 空值检查
  - [x] 结果生成循环 - `item/info` 空值检查
- [x] **loadNumber 递归深度限制**
  - [x] 添加 `loadNumberDepth` 计数器
  - [x] 最大深度 `maxLoadNumberDepth = 200`
  - [x] 超限警告提示循环依赖
- [x] **localStorage 异常保护**
  - [x] `saveData()` / `getData()` 添加 try-catch
  - [x] 隐私模式静默失败，使用 cookie 降级
- [x] **settings 引用保持**
  - [x] `loadSetting()` 使用属性复制而非对象替换
  - [x] 避免 `window.settings` 引用断开

**优化成果**：

- 高频查找操作性能提升 (Map 索引)
- 边界条件崩溃风险消除 (8处空值检查)
- 递归溢出保护 (深度限制)
- 隐私模式兼容 (localStorage 异常保护)

### 阶段 5：blueprint.js 解耦 ✅ 已完成

**目标**：将蓝图生成逻辑模块化。

**详细方案**：参见 [bluepirnt重构.md](./bluepirnt重构.md)

**关键任务**：

- [x] **数据层解耦** - 提取静态数据为 JSON
  - [x] `itemMap.json` 物品映射
  - [x] `buildingMap.json` 建筑映射
  - [x] `recipeMap.json` 配方映射
  - [x] `productionCategory.json` 生产类别
  - [x] `buildingType.json` 建筑类型
  - [x] `src/core/types/blueprint.ts` 类型定义
- [x] **二进制工具** - 创建 `src/core/utils/BinaryWriter.ts` / `BinaryReader.ts`
  - [x] 小端序 (Little-Endian) 读写器
  - [x] Float32 精度保证
  - [x] 单元测试覆盖
- [x] **MD5 工具** - 创建 `src/core/utils/md5.ts`
  - [x] 从 blueprint.js 迁移 MD5 实现
  - [x] 单元测试覆盖
- [x] **解析器/编码器** - 创建 `BlueprintDecoder.ts` / `BlueprintEncoder.ts`
  - [x] 蓝图字符串解析
  - [x] 蓝图数据编码
- [x] **单元测试** - 确保编解码兼容性
  - [x] 往返测试 (round-trip)

**架构约束**：

- 🚫 禁止重写 MD5 中的位运算魔数
- 🚫 禁止尝试"优化"二进制读写逻辑
- ✅ 严格遵守小端序 (Little-Endian)
- ✅ 生成的蓝图字符串必须与原逻辑完全一致

### 阶段 5A：堆叠模块服务化 ✅ 已完成

**目标**：将堆叠逻辑从 Blueprint 类中抽离为独立服务。

**完成日期**：2026-02-14

**详细方案**：参见 [堆叠流程重构.md](./堆叠流程重构.md)

**已完成任务**：

- [x] **类型定义** - 创建 `src/core/types/stack.ts`
  - [x] `IStackConfig` 堆叠配置
  - [x] `ICloneFilter` 克隆过滤规则
  - [x] `ICloneResult` 克隆结果
  - [x] `ISubRecipe` 子配方接口
  - [x] `IItemSummary` 物料汇总接口
  - [x] `ISorterMap` 分拣器映射接口
- [x] **堆叠服务** - 创建 `src/core/services/StackService.ts`
  - [x] Phase 1: `reduceBuildingNum()` - num 缩减
  - [x] Phase 3: `scaleItemSummary()` / `scaleSorters()` - rate 放大
  - [x] Phase 4: `cloneToStackLayers()` - 建筑克隆
  - [x] `getSortersPerNode()` - 分拣器分配策略
  - [x] `createCloneFilter()` / `isCloneable()` - 克隆过滤
- [x] **Index 重映射** - 创建 `src/core/utils/IndexMapper.ts`
  - [x] `buildIndexMap()` - 索引映射构建
  - [x] `remapInputIndex()` - 输入索引重映射
  - [x] `remapOutputIndex()` - 输出索引重映射
  - [x] `applyZOffset()` - Z轴偏移应用
  - [x] `cloneBuildingWithRemap()` - 建筑深拷贝与重映射
- [x] **单元测试** - 创建 `src/core/__tests__/stackService.test.ts`
  - [x] 43个测试用例全部通过
  - [x] 深拷贝验证 (parameters/yaw)
  - [x] indexMap按层存储验证
  - [x] 参数边界校验验证

**核心逻辑**：

```
Phase 1: init() 缩减 num（除Lab外）
  → 60个设备 → ceil(60/4) = 15个设备/层
  → Lab.num 不缩减，保持完整

Phase 2: generateBuildings() 单层布局
  → 15个设备 + 15个分拣器在z=0

Phase 3: generateConveyorBelts() rate放大
  → rate × stackLayers（支撑所有层总产能）

Phase 4: cloneToStackLayers() 克隆
  → 15设备 × 3份 → z=10,20,30
  → Lab不克隆
```

### 阶段 6：性能优化 🔲 计划中

**目标**：将计算密集型任务移至 Web Worker。

**关键任务**：

- [ ] 创建 `calculator.worker.ts`
- [ ] Worker 通信协议设计
- [ ] 主线程 Worker 调度器
- [ ] 进度反馈机制

### 阶段 7：测试覆盖 ✅ 已完成

**目标**：建立完整的测试体系。

**完成日期**：2026-02-14

**已完成任务**：

- [x] 单元测试 (Vitest) - 148个测试通过
- [x] 组件测试 (Vue Test Utils) - ErrorBoundary/Store测试
- [x] 测试覆盖率报告配置 - vitest coverage v8
- [ ] E2E 测试 (Playwright) - 待后续实施

### 阶段 8：开发工具链 ✅ 已完成

**目标**：建立完整的开发工具链和CI/CD流程。

**完成日期**：2026-02-14

**已完成任务**：

- [x] ESLint配置 - `.eslintrc.cjs`
- [x] Prettier配置 - `.prettierrc`
- [x] Git Hooks - `husky` + `lint-staged`
- [x] 环境变量配置 - `.env.development` / `.env.production`
- [x] 全局错误边界 - `ErrorBoundary.vue`
- [x] 全局错误处理 - `main.ts` errorHandler
- [x] CI/CD流水线 - GitHub Actions
- [x] 自动化部署 - GitHub Pages

---

## 4. 风险评估与缓解策略

### 4.1 高风险项

| 风险           | 影响            | 缓解策略                 |
| -------------- | --------------- | ------------------------ |
| 计算逻辑错误   | 生产结果不正确  | Baseline 测试 + 人工验证 |
| 蓝图格式不兼容 | 游戏无法识别    | 字节级对比测试           |
| 性能退化       | 用户体验下降    | 性能基准测试             |
| 状态不同步     | UI 与数据不一致 | 严格的单向数据流         |

### 4.2 技术债务清单

```
┌─────────────────────────────────────────────────────────────────┐
│                      技术债务追踪                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  🔴 高优先级                                                    │
│  ├── data.js 全局变量污染                                       │
│  ├── blueprint.js MD5 魔数未注释                                │
│  └── 缺少类型定义导致运行时错误                                  │
│                                                                 │
│  🟡 中优先级                                                    │
│  ├── 计算逻辑阻塞主线程                                         │
│  ├── 组件间直接引用遗留代码                                      │
│  └── 缺少错误边界处理                                           │
│                                                                 │
│  🟢 低优先级                                                    │
│  ├── 代码风格不统一                                             │
│  ├── 缺少开发文档                                               │
│  └── 未使用的遗留代码残留                                        │
│                                                                 │
│  ✅ 已解决 (2026-02-14)                                         │
│  ├── pako 全局访问问题 → bridge.ts 挂载 window.pako             │
│  ├── Window 接口缺少 pako 类型 → 已添加声明                      │
│  ├── find() 空值崩溃 → 8处空值检查                               │
│  ├── 递归深度溢出 → loadNumberDepth 限制 max=200                │
│  ├── 隐私模式崩溃 → localStorage try-catch 保护                 │
│  └── settings 引用断开 → 属性复制替代对象替换                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4.3 pako 依赖分析 (2026-02-14)

**现状**：

- ~~`src/core/legacy/pako.js`~~ - 已删除 (死代码)
- ~~`Scripts/pako.js`~~ - 已删除 (参考文档)
- `npm pako@2.1.0` - 实际使用，通过 `import('pako')` 加载

**调用链**：

```
BlueprintGenerator.vue
  → window.generateBlueprint()     (data.js)
    → new Blueprint()              (data.js)
      → pako.default.gzip()        (blueprint.js:4017)
```

**修复**：

- bridge.ts 添加 `(window as any).pako = pakoMod` 挂载全局
- Window 接口添加 `pako: any` 类型声明
- 删除死代码文件 `src/core/legacy/pako.js`, `Scripts/pako.js`

### 4.4 已修复问题 (2026-02-14)

| 问题                          | 严重程度 | 修复方案                                        |
| ----------------------------- | -------- | ----------------------------------------------- |
| `window.xqs` 格式读取错误     | 高       | App.vue 兼容 `{ item: { name }, number }` 格式  |
| `window.settings` 类型错误    | 高       | 改为 `Record<string, {...}>` 配方级设置         |
| `isLegacyDataLoaded` 检查错误 | 致命     | 检查 `update_all/find/data` 而非 `isDataLoaded` |
| `syncStateToLegacy` 覆盖问题  | 高       | 移除对 `window.settings` 的赋值                 |
| Store 过度调用 syncToLegacy   | 中       | 移除 store 方法中的冗余同步调用                 |

**关键发现**：

- `window.data` (配方数据) 与 `window.game_data` (图标资源) 是两套独立数据
- `window.isDataLoaded` 仅表示图标加载完成，不代表计算引擎就绪
- `window.xqs` 格式为 `{ item: { name }, number }`，非 `{ name, number }`

---

## 5. 回滚策略

### 5.1 Git Tag 规范

每个阶段完成后打 Tag：

```bash
# 阶段完成后
git tag -a v0.4.0-data-decouple -m "阶段 4: data.js 解耦完成"
git push origin v0.4.0-data-decouple

# 回滚命令
git checkout v0.3.0-bridge-complete
```

### 5.2 功能开关

关键新功能通过环境变量控制：

```typescript
// src/config/features.ts
export const FEATURES = {
  USE_NEW_CALCULATOR: import.meta.env.VITE_USE_NEW_CALCULATOR === 'true',
  USE_WEB_WORKER: import.meta.env.VITE_USE_WEB_WORKER === 'true',
  ENABLE_DEBUG_PANEL: import.meta.env.DEV
}

// 使用示例
import { FEATURES } from '@/config/features'

if (FEATURES.USE_NEW_CALCULATOR) {
  result = CalculatorService.calculate(demands)
} else {
  result = bridge.calculate(demands)
}
```

---

## 6. 性能基准

### 6.1 关键指标

| 指标               | 当前值 | 目标值 | 测量方法        |
| ------------------ | ------ | ------ | --------------- |
| 首屏加载时间       | ~500ms | <300ms | Lighthouse      |
| 计算耗时 (100项)   | ~50ms  | <30ms  | Performance API |
| 蓝图生成 (500建筑) | ~100ms | <50ms  | Performance API |
| 内存占用           | ~50MB  | <30MB  | Chrome DevTools |

### 6.2 性能监控

```typescript
// src/utils/performance.ts
export function measurePerformance<T>(name: string, fn: () => T): T {
  const start = performance.now()
  const result = fn()
  const duration = performance.now() - start

  if (duration > 16) {
    // 超过一帧
    console.warn(`[Performance] ${name} took ${duration.toFixed(2)}ms`)
  }

  return result
}

// 使用示例
const result = measurePerformance('calculate', () => {
  return bridge.calculate(demands)
})
```

---

## 7. 协作规范

### 7.1 分支策略

```
main (生产)
  │
  ├── develop (开发主分支)
  │     │
  │     ├── feature/data-decouple
  │     ├── feature/blueprint-decouple
  │     └── feature/web-worker
  │
  └── hotfix/* (紧急修复)
```

### 7.2 提交规范

```
[模块]：[总结标题]
[版本]：[###]
[方案]：[###]
[需求Id/bugId]：[###]
[影响范围]：[###]
[测试建议]：[###]
```

### 7.3 代码审查清单

- [ ] 是否修改了遗留代码的核心逻辑？
- [ ] 新代码是否有完整的类型定义？
- [ ] 是否添加了必要的错误处理？
- [ ] 性能敏感代码是否有基准测试？
- [ ] 是否更新了相关文档？

---

## 8. 下一步行动

### 8.1 立即执行

1. **完成 data.js 类型定义** - 创建 `src/core/types/recipe.ts`
2. **实现 RecipeDataAdapter** - 封装遗留数据访问
3. **创建 CalculatorService** - 抽象计算逻辑

### 8.2 本周计划

1. 完成 data.js 解耦 (阶段 4B)
2. 开始 blueprint.js 类型定义 (阶段 5 前置)
3. 建立 Baseline 测试用例

### 8.3 本月目标

1. 完成所有核心逻辑解耦
2. 实现 Web Worker 计算
3. 达到 80% 测试覆盖率

---

## 8.1 阶段 4A 完成记录 (2026-02-14)

### 已完成工作

| 任务             | 状态 | 说明                                                         |
| ---------------- | ---- | ------------------------------------------------------------ |
| jQuery API 替换  | ✅   | $.ajax → fetch, $.extend → structuredClone, $.each → forEach |
| 遗留 UI 代码删除 | ✅   | 删除 L4523-L6040 死代码                                      |
| jQuery 依赖移除  | ✅   | package.json 已移除                                          |
| 废弃文件清理     | ✅   | jquery-\*.js, vue.min.js, 废弃图片                           |
| Vite 构建优化    | ✅   | code splitting + esbuild minify                              |

### 构建产物对比

| 指标            | 优化前 | 优化后 | 变化           |
| --------------- | ------ | ------ | -------------- |
| index.js (gzip) | 43 KB  | 12 KB  | -72%           |
| vendor.js       | -      | 31 KB  | 新增独立 chunk |
| 废弃文件        | ~96 KB | 0 KB   | -100%          |

---

## 9. 附录：关键接口定义

### 9.1 计算服务接口

```typescript
interface ICalculatorService {
  calculate(demands: IDemandItem[]): ICalculationResult
  validate(demands: IDemandItem[]): IValidationResult
  getRecipe(itemId: string): IRecipe | null
  getMachineType(recipeId: string): string | null
}
```

### 9.2 蓝图服务接口

```typescript
interface IBlueprintService {
  generate(result: ICalculationResult): string
  parse(blueprintStr: string): IBlueprintData
  validate(blueprintStr: string): boolean
}
```

### 9.3 状态管理接口

```typescript
interface IBlueprintStore {
  demands: IDemandItem[]
  excludes: string[]
  settings: ISettings
  result: ICalculationResult | null

  addDemand(item: IDemandItem): void
  removeDemand(id: string): void
  updateDemand(id: string, updates: Partial<IDemandItem>): void
  calculate(): Promise<void>
  generateBlueprint(): string | null
}
```

---

## 10. 遗留类详细拆分计划

### 10.1 `blueprint.js` 类拆分方案

#### 10.1.1 当前结构分析

| 模块                  | 行数  | 职责             | 复杂度   |
| --------------------- | ----- | ---------------- | -------- |
| `itemMap`             | ~200  | 物品映射表       | 低       |
| `buildingMap`         | ~150  | 建筑映射表       | 低       |
| `productionCategory`  | ~10   | 生产类别枚举     | 低       |
| `buildingType`        | ~10   | 建筑类型枚举     | 低       |
| `Blueprint` 类        | ~1500 | 蓝图生成核心逻辑 | 高       |
| `generateBlueprint()` | ~200  | 蓝图生成入口     | 中       |
| `getRecipe()`         | ~300  | 配方解析         | 中       |
| MD5 实现              | ~300  | 哈希计算         | 高(黑盒) |
| 二进制编解码          | ~800  | 协议层           | 高(黑盒) |
| 参数解析器            | ~400  | 建筑参数处理     | 中       |

**总行数**: ~4000行

#### 10.1.2 拆分目标结构

```
src/core/blueprint/
├── types/
│   ├── item.ts              # IItemMapping, ItemMap
│   ├── building.ts          # IBuildingMapping, BuildingType, ProductionCategory
│   ├── blueprint.ts         # IBlueprintData, IBlueprintBuilding
│   ├── recipe.ts            # ISubRecipe, IRecipeItem
│   └── index.ts             # 统一导出
├── data/
│   ├── itemMap.json         # 物品映射数据 ✅
│   ├── buildingMap.json     # 建筑映射数据 ✅
│   ├── productionCategory.json # 生产类别 ✅
│   └── buildingType.json    # 建筑类型 ✅
├── utils/
│   ├── BinaryReader.ts      # 二进制读取器 ✅
│   ├── BinaryWriter.ts      # 二进制写入器 ✅
│   ├── md5.ts               # MD5 哈希 ✅
│   └── IndexMapper.ts       # 索引重映射 ✅
├── parsers/
│   ├── BlueprintDecoder.ts  # 蓝图解码器 ✅
│   ├── BlueprintEncoder.ts  # 蓝图编码器 ✅
│   ├── ParameterParser.ts   # 建筑参数解析
│   └── RecipeParser.ts      # 配方解析器
├── services/
│   ├── BlueprintService.ts  # 蓝图生成服务
│   ├── StackService.ts      # 堆叠服务 ✅
│   ├── BuildingGenerator.ts # 建筑生成器
│   └── ConveyorGenerator.ts # 传送带生成器
└── index.ts                 # 模块导出
```

#### 10.1.3 拆分步骤

| 步骤 | 任务                                           | 优先级 | 风险     | 依赖       |
| ---- | ---------------------------------------------- | ------ | -------- | ---------- |
| 1    | 提取 `itemMap` → `itemMap.json`                | 高     | 低       | 无         |
| 2    | 提取 `buildingMap` → `buildingMap.json`        | 高     | 低       | 无         |
| 3    | 提取 `productionCategory` / `buildingType`     | 高     | 低       | 无         |
| 4    | 创建类型定义 `types/*.ts`                      | 高     | 低       | 步骤1-3    |
| 5    | 提取 MD5 → `utils/md5.ts`                      | 中     | 高(黑盒) | 无         |
| 6    | 创建 `BinaryReader/Writer`                     | 中     | 高(黑盒) | 无         |
| 7    | 创建 `BlueprintDecoder/Encoder`                | 中     | 高(黑盒) | 步骤5-6    |
| 8    | 提取参数解析器 → `parsers/ParameterParser.ts`  | 中     | 中       | 步骤4      |
| 9    | 提取 `getRecipe()` → `parsers/RecipeParser.ts` | 中     | 中       | 步骤4      |
| 10   | 创建 `BuildingGenerator`                       | 低     | 中       | 步骤4,8    |
| 11   | 创建 `ConveyorGenerator`                       | 低     | 中       | 步骤4,10   |
| 12   | 创建 `BlueprintService` 整合层                 | 低     | 中       | 步骤7-11   |
| 13   | 删除原 `blueprint.js`                          | 低     | 高       | 步骤12验证 |

#### 10.1.4 关键模块详解

**A. ParameterParser (参数解析器)**

```typescript
// src/core/blueprint/parsers/ParameterParser.ts

export interface IParameterParser {
  encodedSize(params: unknown): number
  encode(params: unknown, buffer: DataView): void
  decode(buffer: DataView): unknown
}

export class ParameterParserRegistry {
  private parsers: Map<number, IParameterParser> = new Map()

  register(itemId: number, parser: IParameterParser): void
  getParser(itemId: number): IParameterParser
  parse(itemId: number, buffer: DataView): unknown
  encode(itemId: number, params: unknown, buffer: DataView): number
}

// 内置解析器
export const stationParamParser: IParameterParser // 物流站
export const labParamParser: IParameterParser // 研究站
export const splitterParamParser: IParameterParser // 分流器
export const inserterParamParser: IParameterParser // 分拣器
export const beltParamParser: IParameterParser // 传送带
export const storageParamParser: IParameterParser // 储物仓
export const tankParamParser: IParameterParser // 储液罐
export const ejectorParamParser: IParameterParser // 弹射器
export const MonitorParamParser: IParameterParser // 监测器
```

**B. BuildingGenerator (建筑生成器)**

```typescript
// src/core/blueprint/services/BuildingGenerator.ts

export interface IBuildingGeneratorConfig {
  conveyorBeltStackLayer: number
  onlyConveyorBeltMk3: boolean
  onlySorterMk3: boolean
  useSorterMk4: boolean
  selfSpray: boolean
  generateTeslaTower: boolean
  teslaTowerInterval: number
  teslaTowerLineInterval: number
}

export class BuildingGenerator {
  private config: IBuildingGeneratorConfig
  private buildings: IBlueprintBuilding[] = []
  private indexCounter: number = 0

  generateFromRecipe(recipe: ISubRecipe): IBlueprintBuilding[]
  generateAssemblingMachine(recipe: ISubRecipe): IBlueprintBuilding
  generateSmelter(recipe: ISubRecipe): IBlueprintBuilding
  generateLab(recipe: ISubRecipe): IBlueprintBuilding
  generateSorter(building: IBlueprintBuilding, config: ISorterConfig): IBlueprintBuilding
  generateTeslaTower(position: ICoordinate): IBlueprintBuilding
  generateSprayCoater(position: ICoordinate): IBlueprintBuilding

  getBuildings(): IBlueprintBuilding[]
  reset(): void
}
```

**C. ConveyorGenerator (传送带生成器)**

```typescript
// src/core/blueprint/services/ConveyorGenerator.ts

export interface IConveyorConfig {
  transportSpeed: number
  stackLayer: number
  maxSorterNumOneBelt: number
}

export class ConveyorGenerator {
  private buildings: IBlueprintBuilding[] = []

  generateBelt(from: ICoordinate, to: ICoordinate, itemId: number): IBlueprintBuilding[]
  connectBuildings(source: IBlueprintBuilding, target: IBlueprintBuilding): void
  optimizeBeltLayout(): void

  getBuildings(): IBlueprintBuilding[]
  reset(): void
}
```

#### 10.1.5 风险评估

| 风险项           | 级别 | 缓解措施                            |
| ---------------- | ---- | ----------------------------------- |
| 二进制协议不兼容 | 高   | Baseline测试覆盖所有编解码场景      |
| 参数解析遗漏     | 中   | 完整枚举所有itemId，添加unknown处理 |
| 坐标计算偏差     | 中   | Float32精度测试，容差校验           |
| 分拣器连接错误   | 中   | 索引重映射单元测试                  |

---

### 10.2 `data.js` 类拆分方案

#### 10.2.1 当前结构分析

| 模块            | 行数  | 职责         | 复杂度     |
| --------------- | ----- | ------------ | ---------- |
| `data` 数组     | ~3000 | 配方数据     | 低(纯数据) |
| `loadNumber()`  | ~200  | 递归计算核心 | 高(黑盒)   |
| `find()`        | ~50   | 配方查找     | 中         |
| `update_all()`  | ~300  | 全局状态更新 | 高(黑盒)   |
| `f_add()`       | ~50   | 添加需求     | 低         |
| `f_reset()`     | ~30   | 重置状态     | 低         |
| `f_ig()`        | ~30   | 排除物品     | 低         |
| `settings` 管理 | ~200  | 设备设置     | 中         |
| `addXH/addOut`  | ~100  | 结果收集     | 中         |
| DOM操作(已删除) | ~1500 | UI渲染       | 已移除     |

**总行数**: ~6000行 (重构后~2000行)

#### 10.2.2 拆分目标结构

```
src/core/recipe/
├── types/
│   ├── recipe.ts            # IRecipe, IRecipeItem, IRawRecipe
│   ├── demand.ts            # IDemand, IDemandItem
│   ├── result.ts            # ICalculationResult, IResultItem
│   ├── settings.ts          # ISettings, IMachineSettings
│   └── index.ts             # 统一导出
├── data/
│   ├── recipes.json         # 配方数据
│   ├── schema.json          # JSON Schema
│   └── migrations/          # 数据迁移脚本
├── adapters/
│   ├── RecipeAdapter.ts     # 配方数据适配器 ✅
│   └── SettingsAdapter.ts   # 设置适配器
├── services/
│   ├── CalculatorService.ts # 计算服务 ✅
│   ├── CalculationContext.ts# 计算上下文 ✅
│   ├── DemandService.ts     # 需求管理服务
│   └── SettingsService.ts   # 设置管理服务
├── utils/
│   ├── recipeFinder.ts      # 配方查找工具
│   ├── unitConverter.ts     # 单位转换
│   └── validator.ts         # 数据校验
└── index.ts                 # 模块导出
```

#### 10.2.3 拆分步骤

| 步骤 | 任务                                       | 优先级 | 风险     | 依赖         |
| ---- | ------------------------------------------ | ------ | -------- | ------------ |
| 1    | 提取 `data` 数组 → `recipes.json`          | 高     | 低       | 无           |
| 2    | 创建 JSON Schema 校验                      | 中     | 低       | 步骤1        |
| 3    | 创建类型定义 `types/*.ts`                  | 高     | 低       | 步骤1        |
| 4    | 创建 `RecipeAdapter`                       | 高     | 低       | 步骤1-3      |
| 5    | 创建 `CalculationContext`                  | 高     | 中       | 步骤3        |
| 6    | 创建 `CalculatorService` 封装 `update_all` | 高     | 高(黑盒) | 步骤5        |
| 7    | 创建 `DemandService`                       | 中     | 低       | 步骤3        |
| 8    | 创建 `SettingsService`                     | 中     | 低       | 步骤3        |
| 9    | 创建 `recipeFinder.ts`                     | 中     | 中       | 步骤4        |
| 10   | 添加递归深度限制                           | 中     | 中       | 步骤6        |
| 11   | 添加空值安全检查                           | 高     | 中       | 步骤6        |
| 12   | 性能优化 (Map索引)                         | 中     | 低       | 步骤6        |
| 13   | 删除原 `data.js`                           | 低     | 高       | 步骤6-12验证 |

#### 10.2.4 关键模块详解

**A. RecipeAdapter (配方适配器)**

```typescript
// src/core/recipe/adapters/RecipeAdapter.ts

export class RecipeAdapter {
  private recipes: IRecipe[] = []
  private index: IRecipeIndex

  loadFromRawData(rawData: IRawRecipe[]): void
  loadFromJSON(jsonPath: string): Promise<void>

  findByProductName(name: string): IRecipe[]
  findByInputName(name: string): IRecipe[]
  findById(id: string): IRecipe | null
  getAllRecipes(): IRecipe[]

  getRecipeCount(): number
  isLoaded(): boolean
}
```

**B. CalculatorService (计算服务)**

```typescript
// src/core/recipe/services/CalculatorService.ts

export class CalculatorService {
  private context: CalculationContext
  private version: number = 0

  async calculate(demands: IDemand[], excludes: string[]): Promise<ICalculationResult>
  validate(demands: IDemand[]): IValidationResult
  getContext(): CalculationContext
  getVersion(): number
  reset(): void

  private extractResults(): IResultItem[]
  private syncToLegacy(demands: IDemand[], excludes: string[]): void
}
```

**C. DemandService (需求管理服务)**

```typescript
// src/core/recipe/services/DemandService.ts

export class DemandService {
  private demands: IDemand[] = []
  private excludes: Set<string> = new Set()

  addDemand(item: IDemandItem): void
  removeDemand(id: string): void
  updateDemand(id: string, updates: Partial<IDemandItem>): void
  clearDemands(): void

  addExclude(name: string): void
  removeExclude(name: string): void
  clearExcludes(): void

  getDemands(): IDemand[]
  getExcludes(): string[]
  hasDemand(name: string): boolean
}
```

**D. SettingsService (设置管理服务)**

```typescript
// src/core/recipe/services/SettingsService.ts

export class SettingsService {
  private settings: ISettings
  private speedSettings: Record<string, number>

  getMachineSetting(key: keyof IMachineSettings): string | number
  setMachineSetting(key: keyof IMachineSettings, value: string | number): void

  getSpeedSetting(machineType: string): number
  setSpeedSetting(machineType: string, speed: number): void

  loadFromStorage(): void
  saveToStorage(): void
  reset(): void

  getAllSettings(): ISettings
}
```

#### 10.2.5 风险评估

| 风险项                    | 级别 | 缓解措施                    |
| ------------------------- | ---- | --------------------------- |
| `loadNumber` 递归逻辑破坏 | 高   | 黑盒保护，仅外围封装        |
| 全局变量引用断开          | 高   | 保持 `window.settings` 引用 |
| 配方数据格式不一致        | 中   | JSON Schema 校验            |
| 计算结果格式变化          | 中   | Baseline 对比测试           |

---

### 10.3 拆分时间线

```
┌─────────────────────────────────────────────────────────────────┐
│                    类拆分时间线 (预计 4 周)                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  第1周: 数据层提取                                               │
│  ├── blueprint.js: itemMap/buildingMap → JSON                   │
│  ├── data.js: data数组 → recipes.json                           │
│  └── 创建类型定义文件                                            │
│                                                                 │
│  第2周: 工具层拆分                                               │
│  ├── blueprint.js: MD5/Binary工具 → utils/                      │
│  ├── data.js: RecipeAdapter/SettingsAdapter                     │
│  └── 单元测试覆盖                                                │
│                                                                 │
│  第3周: 服务层封装                                               │
│  ├── blueprint.js: BuildingGenerator/ConveyorGenerator          │
│  ├── data.js: CalculatorService/DemandService                   │
│  └── 集成测试                                                    │
│                                                                 │
│  第4周: 整合与清理                                               │
│  ├── BlueprintService/RecipeService 整合层                      │
│  ├── 删除原遗留文件                                              │
│  └── 回归测试 + 性能验证                                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 10.4 拆分验收标准

| 验收项     | 标准                          | 验证方式         |
| ---------- | ----------------------------- | ---------------- |
| 功能完整性 | 所有原有功能正常              | E2E测试覆盖      |
| 性能不退化 | 计算耗时 ≤ 原实现             | 性能基准测试     |
| 类型安全   | TypeScript strict mode 无错误 | CI构建检查       |
| 测试覆盖   | 核心模块覆盖率 ≥ 80%          | Vitest覆盖率报告 |
| 文档完整   | 所有公共接口有JSDoc           | 代码审查         |
