# DSP 工厂规划器 - 架构迁移方案

> **状态**：✅ 阶段 7 完成 - 99% 完成
>
> **更新日期**：2026-02-15
>
> **下一阶段**：E2E 测试 / PWA 支持

## 0. 迁移原则：架构师红线

- **零停机迁移**：每个阶段完成后，应用必须可正常运行。
- **增量替换**：新代码通过适配层与旧代码共存，逐步替换。
- **回滚能力**：每个阶段完成后打 Git Tag，确保可随时回退。
- **测试先行**：核心业务逻辑必须有 Baseline 测试覆盖。

---

## 1. 项目现状分析 (2026-02-15)

### 1.1 技术栈

| 层级     | 当前技术               | 目标技术               |
| -------- | ---------------------- | ---------------------- |
| 运行时   | Vue 3 + TypeScript     | Vue 3 + TypeScript     |
| 状态管理 | Pinia Store            | Pinia Store            |
| UI 框架  | Vue SFC 组件           | Vue SFC 组件           |
| 构建工具 | Vite                   | Vite                   |
| 类型安全 | TypeScript strict mode | TypeScript strict mode |

### 1.2 遗留代码清单

```
src/core/legacy/
├── data.js              # 核心流程 (1111行)
├── calculator.js        # 计算引擎导出层 (358行) ✅ 拆分
├── calculatorEngine.js  # 闭包封装计算引擎 (444行) ✅ 新增
├── iconLoader.js        # 图标加载 (134行) ✅ 拆分
├── storageManager.js    # 存储管理 (152行) ✅ 拆分
├── recipeHelper.js      # 配方辅助 (41行) ✅ 拆分
├── configData.js        # 配置数据 (230行) ✅ 拆分
├── settingsHelper.js    # 设置辅助 (82行) ✅ 拆分
└── blueprint.js         # 蓝图编码/解码 (3967行)

遗留代码总计: 6519行
```

### 1.3 新架构模块清单

```
src/core/
├── types/               # 类型定义 (9个文件, 721行)
│   ├── recipe.ts        # 配方类型 (172行)
│   ├── blueprint.ts     # 蓝图类型 (93行)
│   ├── itemMap.ts       # 物品映射 (28行)
│   ├── buildingMap.ts   # 建筑映射 (52行)
│   ├── settings.ts      # 设置类型 (109行)
│   ├── stack.ts         # 堆叠类型 (54行)
│   ├── legacy.ts        # 遗留代码类型 (128行)
│   ├── calculator.ts    # 计算器类型 (78行)
│   └── index.ts         # 类型导出 (7行)
├── data/                # 数据文件 (7个JSON文件)
│   ├── recipes.json     # 配方数据
│   ├── recipes.schema.json # 配方Schema
│   ├── itemMap.json     # 物品图标数据
│   ├── buildingMap.json # 建筑信息数据
│   ├── buildingType.json
│   ├── productionCategory.json
│   └── recipeMap.json
├── adapters/            # 适配器层 (4个文件, 557行)
│   ├── RecipeAdapter.ts # 配方适配器 (169行)
│   ├── SettingsAdapter.ts # 设置适配器 (240行)
│   ├── BlueprintAdapter.ts # 蓝图适配器 (145行)
│   └── index.ts         # 导出 (3行)
├── services/            # 服务层 (4个文件, 732行)
│   ├── CalculatorService.ts # 计算服务 (367行) - 含Worker支持
│   ├── CalculationContext.ts # 计算上下文 (145行)
│   ├── StackService.ts  # 堆叠服务 (217行)
│   └── index.ts         # 导出 (3行)
├── workers/             # Web Worker层 (4个文件, 699行) ✅ 新增
│   ├── types.ts         # Worker通信类型 (41行)
│   ├── calculator.worker.ts # 计算Worker (428行)
│   ├── CalculatorWorkerService.ts # Worker服务封装 (228行)
│   └── index.ts         # 导出 (2行)
├── blueprint/           # 蓝图生成模块 (19个文件) ✅ 新增
│   ├── types/           # 类型定义
│   ├── generators/      # 生成器服务
│   │   ├── BuildingGenerator.ts (106行)
│   │   ├── ConveyorGenerator.ts (198行)
│   │   ├── SorterGenerator.ts (387行)
│   │   ├── LayoutCalculator.ts (115行)
│   │   ├── ItemSummaryCalculator.ts (157行)
│   │   └── ConnectionBuilder.ts (158行)
│   ├── services/
│   │   └── BlueprintService.ts (244行)
│   └── __tests__/       # 测试文件
├── utils/               # 工具层 (10个文件, 1328行)
│   ├── BinaryReader.ts  # 二进制读取 (82行)
│   ├── BinaryWriter.ts  # 二进制写入 (68行)
│   ├── BlueprintDecoder.ts # 蓝图解码 (384行)
│   ├── BlueprintEncoder.ts # 蓝图编码 (414行)
│   ├── IndexMapper.ts   # 索引映射 (78行)
│   ├── md5.ts           # MD5工具 (109行)
│   ├── storage.ts       # 存储工具 (126行)
│   ├── unitConverter.ts # 单位转换 (61行)
│   ├── validator.ts     # 验证器 (92行)
│   └── index.ts         # 导出 (14行)
├── config/
│   └── app.config.ts    # 应用配置
├── __tests__/           # 测试文件 (12个文件, 3109行)
│   ├── recipe.test.ts (122行)
│   ├── calculatorService.test.ts (539行)
│   ├── calculatorEngine.test.ts (228行)
│   ├── calculationContext.test.ts (170行)
│   ├── dataConsistency.test.ts (105行)
│   ├── bridge.test.ts (619行)
│   ├── stackService.test.ts (384行)
│   ├── blueprint.test.ts (363行)
│   ├── binary.test.ts (228行)
│   ├── md5.test.ts (128行)
│   ├── indexMapper.test.ts (159行)
│   └── test-data.ts (64行)
└── bridge.ts            # 遗留代码适配层

新架构模块总计: 81个TypeScript文件 (含测试)
```

### 1.4 迁移进度总览

```
┌─────────────────────────────────────────────────────────────────┐
│                      迁移进度仪表盘 (2026-02-15)                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [█████████████████████████████████████████░] 99% 完成      │
│                                                                 │
│  ✅ index.html → Vue 3 组件化                                   │
│  ✅ 状态管理 → Pinia Store                                      │
│  ✅ bridge.ts 适配层                                            │
│  ✅ 核心组件迁移 (ControlPanel, ConfigPanel, ResultTable)       │
│  ✅ 添加需求对话框 (AddItemDialog)                               │
│  ✅ BlueprintGenerator 组件                                     │
│  ✅ LocaleSwitcher / ErrorBoundary 组件                         │
│  ✅ jQuery 完全移除                                             │
│  ✅ 构建配置优化 (Vite code splitting + esbuild)                │
│  ✅ 废弃资源清理 (~96KB)                                         │
│  ✅ data.js 核心逻辑解耦 (详见 data重构.md)                      │
│  ✅ Web Worker 计算迁移 (非阻塞UI)                               │
│  ✅ App.vue 迁移至 CalculatorService                             │
│  ✅ 性能优化 (图标缓存统一、v-memo、日志环境控制、搜索防抖)      │
│  ✅ blueprint.js 协议层解耦 (编码/解码器已完成)                  │
│  ✅ blueprint.js 生成器迁移 (BuildingGenerator/ConveyorGenerator)│
│  ✅ data.js 性能优化与空值安全                                   │
│  ✅ 堆叠模块服务化 (详见 堆叠流程重构.md)                        │
│  ✅ 单元测试覆盖 (637个测试全部通过)                             │
│  ✅ 状态同步机制 (demandVersion + 快照验证)                     │
│  ✅ 异常处理增强 (xhMap/outMap清理、边界校验)                    │
│  ✅ 开发工具链 (ESLint/Prettier/husky)                          │
│  ✅ 全局错误边界 (ErrorBoundary组件)                            │
│  ✅ CI/CD流水线 (GitHub Actions → GitHub Pages)                │
│  ✅ 数据流同步修复                                              │
│  ✅ SettingsAdapter 适配器                                      │
│  ✅ 配方数据JSON化 (recipes.json + schema)                      │
│  ✅ itemMap/buildingMap提取                                     │
│  ✅ 数据源统一                                                  │
│  ✅ 类型定义统一                                                │
│  ✅ 数据一致性测试 (637个测试全部通过)                           │
│  ✅ legacy itemMap注入修复                                      │
│  ✅ data.js模块化拆分 (calculator/iconLoader/storage等)         │
│  ✅ ES模块变量引用修复                                          │
│  ✅ 异步竞态修复                                                │
│  ✅ 全局状态迁移                                                │
│  ✅ 闭包封装计算引擎 (calculatorEngine.js)                      │
│  ✅ 双引擎支持 (遗留+新)                                        │
│  ✅ Web Worker 性能优化 (非阻塞计算)                            │
│  🔲 E2E 测试覆盖                                                │
│  🔲 PWA 支持                                                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 1.5 重构文档索引

| 文档                                   | 状态      | 说明                       |
| -------------------------------------- | --------- | -------------------------- |
| [index重构.md](./index重构.md)         | ✅ 已完成 | index.html 现代化重构      |
| [data重构.md](./data重构.md)           | ✅ 已完成 | data.js 核心数据与逻辑解耦 |
| [bluepirnt重构.md](./bluepirnt重构.md) | ✅ 已完成 | blueprint.js 协议层解耦    |
| [堆叠流程重构.md](./堆叠流程重构.md)   | ✅ 已完成 | 堆叠模块服务化重构         |
| [架构补齐文档.md](./架构补齐文档.md)   | ✅ 已完成 | 开发工具链/CI/CD配置       |

---

## 2. 目标架构

### 2.1 分层架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                         表现层 (UI Layer)                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │ControlPanel │  │ ConfigPanel │  │ ResultTable │  ...        │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘             │
└─────────┼────────────────┼────────────────┼─────────────────────┘
          │                │                │
          ▼                ▼                ▼
┌─────────────────────────────────────────────────────────────────┐
│                       状态管理层 (State Layer)                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Pinia Stores                          │   │
│  │  ┌───────────┐  ┌───────────┐  ┌───────────┐            │   │
│  │  │ blueprint │  │  recipe   │  │   ui      │            │   │
│  │  │  Store    │  │  Store    │  │  Store    │            │   │
│  │  └───────────┘  └───────────┘  └───────────┘            │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────────┐
│                      业务逻辑层 (Service Layer)                   │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │ Calculator  │  │ Blueprint   │  │   Stack     │             │
│  │  Service    │  │  Service    │  │  Service    │             │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘             │
└─────────┼────────────────┼────────────────┼─────────────────────┘
          │                │                │
          ▼                ▼                ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Worker层 (Worker Layer) ✅ 已完成           │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              CalculatorWorkerService                     │   │
│  │  • 非阻塞计算                                            │   │
│  │  • 主线程不阻塞UI                                        │   │
│  │  • 自动降级到遗留引擎                                    │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────────┐
│                      适配层 (Adapter Layer)                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              RecipeAdapter / SettingsAdapter            │   │
│  │  • 语义化数据访问                                        │   │
│  │  • 类型转换与安全检查                                    │   │
│  │  • 状态同步                                              │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────────┐
│                    遗留逻辑层 (Legacy Layer)                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │   data.js   │  │blueprint.js │  │   pako.js   │             │
│  │  (黑盒)     │  │  (黑盒)     │  │  (第三方)   │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 目录结构

```
src/
├── core/                          # 核心业务逻辑
│   ├── legacy/                    # 遗留代码 (只读)
│   │   ├── data.js                # 核心流程 (1111行)
│   │   ├── calculator.js          # 计算引擎导出层 (358行)
│   │   ├── calculatorEngine.js    # 闭包封装计算引擎 (444行)
│   │   ├── iconLoader.js          # 图标加载 (134行)
│   │   ├── storageManager.js      # 存储管理 (152行)
│   │   ├── recipeHelper.js        # 配方辅助 (41行)
│   │   ├── configData.js          # 配置数据 (230行)
│   │   ├── settingsHelper.js      # 设置辅助 (82行)
│   │   └── blueprint.js           # 蓝图编码/解码 (3967行)
│   ├── types/                     # 类型定义 ✅ (721行)
│   ├── data/                      # 数据文件 ✅ (7个JSON)
│   ├── adapters/                  # 适配器 ✅ (557行)
│   ├── services/                  # 业务服务 ✅ (732行)
│   ├── workers/                   # Web Worker ✅ (699行)
│   ├── blueprint/                 # 蓝图生成 ✅ (19个文件)
│   ├── utils/                     # 工具函数 ✅ (1328行)
│   ├── config/                    # 配置 ✅
│   ├── __tests__/                 # 测试 ✅ (3109行)
│   └── bridge.ts                  # 适配层 ✅
│
├── stores/                        # Pinia 状态管理 ✅
│   ├── blueprint.ts
│   └── __tests__/
│
├── components/                    # Vue 组件 ✅
│   ├── ControlPanel/
│   ├── ConfigPanel/
│   ├── ResultTable/
│   ├── AddItemDialog/
│   ├── BlueprintGenerator/
│   ├── Toast/
│   ├── LocaleSwitcher/
│   ├── ErrorBoundary/
│   └── __tests__/
│
├── App.vue                        # 根组件 ✅
└── main.ts                        # 入口文件 ✅
```

---

## 3. 迁移阶段详解

### 阶段 1：基础设施搭建 ✅ 已完成

**目标**：建立 Vue 3 + TypeScript + Vite 开发环境。

**完成项**：

- [x] Vite 项目初始化
- [x] TypeScript 配置
- [x] Pinia 状态管理集成
- [x] 基础组件结构

### 阶段 2：UI 组件迁移 ✅ 已完成

**目标**：将原生 DOM 操作迁移至 Vue SFC 组件。

**完成项**：

- [x] ControlPanel 组件
- [x] ConfigPanel 组件
- [x] ResultTable 组件
- [x] AddItemDialog 组件
- [x] Toast 消息组件

### 阶段 3：适配层建设 ✅ 已完成

**目标**：建立遗留代码与 Vue 3 的桥接层。

**完成项**：

- [x] bridge.ts 核心适配器
- [x] 状态同步机制
- [x] 类型安全封装

### 阶段 4：data.js 解耦 ✅ 已完成

**目标**：将配方计算逻辑从全局变量中解耦。

**详细方案**：参见 [data重构.md](./data重构.md)

#### 阶段 4A：jQuery 渐进式移除 ✅ 已完成

**完成日期**：2026-02-14

**已完成任务**：

- [x] 替换 `$.ajax` 为 `fetch()`
- [x] 替换 `$.extend` 为 `structuredClone()`
- [x] 替换 `$(arr).each()` 为 `arr.forEach()`
- [x] 替换 `$.cookie` 为 `localStorage` + `document.cookie`
- [x] 删除遗留 UI 代码块
- [x] 移除 jQuery 依赖
- [x] 删除废弃文件 (~96KB)
- [x] 构建验证通过

#### 阶段 4B：核心逻辑解耦 ✅ 已完成

**完成日期**：2026-02-14

**已完成任务**：

- [x] 类型定义 (`src/core/types/recipe.ts`)
- [x] 数据适配器 (`RecipeAdapter.ts`)
- [x] 计算上下文 (`CalculationContext.ts`)
- [x] 计算服务 (`CalculatorService.ts`)
- [x] Baseline 测试
- [x] 主入口迁移

#### 阶段 4C：性能优化与空值安全 ✅ 已完成

**完成日期**：2026-02-14

**已完成任务**：

- [x] addXH/addOut Map 优化
- [x] find() 空值检查 (8处)
- [x] loadNumber 递归深度限制
- [x] localStorage 异常保护
- [x] settings 引用保持

#### 阶段 4D-4I：数据流与类型统一 ✅ 已完成

**完成日期**：2026-02-14 ~ 2026-02-15

**已完成任务**：

- [x] 数据流同步修复
- [x] SettingsAdapter 适配器
- [x] 配方数据JSON化
- [x] itemMap/buildingMap提取
- [x] 数据源统一
- [x] 类型定义统一
- [x] 数据一致性测试
- [x] legacy itemMap注入修复

#### 阶段 8-12：模块化拆分 ✅ 已完成

**完成日期**：2026-02-15

**已完成任务**：

- [x] calculator.js 计算引擎拆分
- [x] iconLoader.js 图标加载拆分
- [x] storageManager.js 存储管理拆分
- [x] recipeHelper.js 配方辅助拆分
- [x] configData.js 配置数据拆分
- [x] settingsHelper.js 设置辅助拆分
- [x] ES模块变量引用修复

#### 阶段 13-15：异步与状态迁移 ✅ 已完成

**完成日期**：2026-02-15

**已完成任务**：

- [x] 异步竞态修复
- [x] 全局状态迁移
- [x] 闭包封装计算引擎 (`calculatorEngine.js`)
- [x] 双引擎支持 (遗留+新)

### 阶段 5：blueprint.js 解耦 ✅ 已完成

**目标**：将蓝图生成逻辑模块化。

**详细方案**：参见 [bluepirnt重构.md](./bluepirnt重构.md)

#### 阶段 5A：协议层解耦 ✅ 已完成

**完成日期**：2026-02-14

**已完成任务**：

- [x] 数据层解耦 (itemMap/buildingMap/recipeMap JSON)
- [x] 类型定义 (`src/core/types/blueprint.ts`)
- [x] 二进制工具 (`BinaryWriter.ts` / `BinaryReader.ts`)
- [x] MD5 工具 (`md5.ts`)
- [x] 编码器 (`BlueprintEncoder.ts`)
- [x] 解码器 (`BlueprintDecoder.ts`)
- [x] 单元测试覆盖

#### 阶段 5B：生成器迁移 ✅ 已完成

**完成日期**：2026-02-15

**已完成任务**：

- [x] BuildingGenerator 服务
- [x] ConveyorGenerator 服务
- [x] SorterGenerator 服务
- [x] LayoutCalculator 服务
- [x] ItemSummaryCalculator 服务
- [x] ConnectionBuilder 服务
- [x] BlueprintService 整合服务
- [x] 单元测试覆盖

### 阶段 6：堆叠模块服务化 ✅ 已完成

**目标**：将堆叠逻辑从 Blueprint 类中抽离为独立服务。

**详细方案**：参见 [堆叠流程重构.md](./堆叠流程重构.md)

**已完成任务**：

- [x] 类型定义 (`src/core/types/stack.ts`)
- [x] 索引重映射 (`IndexMapper.ts`)
- [x] 堆叠服务 (`StackService.ts`)
- [x] 单元测试覆盖

### 阶段 7：Web Worker 迁移 ✅ 已完成

**目标**：将计算密集型操作迁移至 Web Worker。

**完成日期**：2026-02-15

**已完成任务**：

- [x] Worker 通信协议类型定义 (`types.ts`)
- [x] 计算 Worker 实现 (`calculator.worker.ts`)
- [x] Worker 服务封装 (`CalculatorWorkerService.ts`)
- [x] CalculatorService Worker 支持
- [x] 回退机制实现
- [x] 单元测试覆盖

### 阶段 8：测试与质量保证 ✅ 已完成

**目标**：建立完整的测试体系。

**已完成任务**：

- [x] 单元测试 (637个测试全部通过)
- [x] 组件测试 (`@vue/test-utils`)
- [x] Store测试 (Pinia)
- [x] 测试覆盖率报告
- [x] 开发工具链 (ESLint/Prettier/husky)
- [x] CI/CD流水线 (GitHub Actions)

### 阶段 9：待实施

**目标**：性能优化与用户体验提升。

**待开始任务**：

- [ ] E2E 测试覆盖 (Playwright)
- [ ] PWA 支持 (vite-plugin-pwa)
- [ ] SEO 优化
- [ ] 骨架屏组件

---

## 4. 测试状态

```
Test Files  28 passed (28)
Tests       637 passed (637)
Duration    3.32s
```

### 测试文件清单

| 测试文件                            | 测试内容            | 行数 |
| ----------------------------------- | ------------------- | ---- |
| `recipe.test.ts`                    | 配方适配器测试      | 122  |
| `calculatorService.test.ts`         | 计算服务测试        | 539  |
| `calculatorEngine.test.ts`          | 计算引擎测试        | 228  |
| `calculationContext.test.ts`        | 计算上下文测试      | 170  |
| `dataConsistency.test.ts`           | 数据一致性测试      | 105  |
| `bridge.test.ts`                    | 桥接层测试          | 619  |
| `stackService.test.ts`              | 堆叠服务测试        | 384  |
| `blueprint.test.ts`                 | 蓝图编解码测试      | 363  |
| `binary.test.ts`                    | 二进制读写测试      | 228  |
| `md5.test.ts`                       | MD5工具测试         | 128  |
| `indexMapper.test.ts`               | 索引映射测试        | 159  |
| `test-data.ts`                      | 测试数据            | 64   |
| `ErrorBoundary.test.ts`             | 错误边界组件测试    | -    |
| `blueprint.store.test.ts`           | Blueprint Store测试 | -    |
| `buildingGenerator.test.ts`         | 建筑生成器测试      | 396  |
| `conveyorGenerator.test.ts`         | 传送带生成器测试    | 412  |
| `blueprintService.test.ts`          | 蓝图服务集成测试    | 345  |
| `calculatorWorker.test.ts`          | Worker服务测试      | 381  |
| `calculatorEngine.test.ts` (Worker) | Worker引擎测试      | 467  |
| `storage.test.ts`                   | 存储工具测试        | 146  |
| `unitConverter.test.ts`             | 单位转换测试        | 126  |
| `validator.test.ts`                 | 验证器测试          | 158  |
| `BlueprintAdapter.test.ts`          | 蓝图适配器测试      | 204  |
| `SettingsAdapter.test.ts`           | 设置适配器测试      | 154  |
| `SorterGenerator.test.ts`           | 分拣器生成器测试    | 236  |
| `ConnectionBuilder.test.ts`         | 连接构建器测试      | 246  |
| `ItemSummaryCalculator.test.ts`     | 物料统计测试        | 226  |

---

## 5. 配置文件清单

| 文件                            | 状态 | 说明           |
| ------------------------------- | ---- | -------------- |
| `.eslintrc.cjs`                 | ✅   | ESLint配置     |
| `.prettierrc`                   | ✅   | Prettier配置   |
| `.husky/pre-commit`             | ✅   | Git提交前钩子  |
| `.env.development`              | ✅   | 开发环境变量   |
| `.env.production`               | ✅   | 生产环境变量   |
| `vitest.config.ts`              | ✅   | Vitest测试配置 |
| `.github/workflows/ci.yml`      | ✅   | CI流水线       |
| `.github/workflows/release.yml` | ✅   | 发布流水线     |

---

## 6. 预估工时

| 阶段                     | 工时     | 状态      |
| ------------------------ | -------- | --------- |
| 阶段 1-3: 基础设施       | 5天      | ✅ 已完成 |
| 阶段 4: data.js解耦      | 11天     | ✅ 已完成 |
| 阶段 5A: blueprint协议层 | 4天      | ✅ 已完成 |
| 阶段 5B: blueprint生成器 | 4天      | ✅ 已完成 |
| 阶段 6: 堆叠服务化       | 1天      | ✅ 已完成 |
| 阶段 7: Web Worker迁移   | 2天      | ✅ 已完成 |
| 阶段 8: 测试与质量       | 5天      | ✅ 已完成 |
| 阶段 9: E2E/PWA          | 3天      | 🔲 待开始 |
| **总计**                 | **35天** | **99%**   |

### 6.1 代码统计 (2026-02-15)

| 模块                       | 文件数 | 代码行数 | 状态 |
| -------------------------- | ------ | -------- | ---- |
| 遗留代码 (legacy/\*.js)    | 9      | 6519行   | 保留 |
| 类型定义 (types/\*.ts)     | 9      | 721行    | ✅   |
| 数据文件 (data/\*.json)    | 7      | -        | ✅   |
| 适配器 (adapters/\*.ts)    | 4      | 557行    | ✅   |
| 服务层 (services/\*.ts)    | 4      | 732行    | ✅   |
| Worker层 (workers/\*.ts)   | 4      | 699行    | ✅   |
| 蓝图模块 (blueprint/)      | 19     | ~2400行  | ✅   |
| 工具层 (utils/\*.ts)       | 10     | 1328行   | ✅   |
| 测试代码 (**tests**/\*.ts) | 27     | ~5000行  | ✅   |
| **新架构总计**             | **81** | ~11437行 | -    |

### 6.2 测试覆盖

- **测试文件**: 28个
- **测试用例**: 637个
- **测试状态**: 全部通过 ✅

---

## 7. 架构约束

### 7.1 黑盒保护

- 🚫 禁止修改 `loadNumber` 函数内部的递归逻辑
- 🚫 禁止修改 MD5 中的位运算魔数
- 🚫 禁止尝试"优化"二进制读写逻辑
- 🚫 禁止全局搜索替换 `s` → `outputs`

### 7.2 兼容性要求

- ✅ 严格遵守小端序 (Little-Endian)
- ✅ 生成的蓝图字符串必须与原逻辑完全一致
- ✅ 所有对 `window.data` 的访问必须通过 `RecipeAdapter`
- ✅ 计算结果从 `window.app` 提取

### 7.3 代码规范

- ✅ 新增代码必须包含完整的 TypeScript 类型注解
- ✅ 每次修改后必须运行 Baseline 测试
- ✅ 遵循 ESLint/Prettier 配置

---

## 8. 下一阶段任务分析 (2026-02-15)

### 8.1 当前阻塞项

```
┌─────────────────────────────────────────────────────────────────┐
│                    下一阶段任务优先级                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  🟡 中优先级 (质量提升)                                          │
│  ├── E2E 测试覆盖 (Playwright)                                 │
│  ├── PWA 支持 (vite-plugin-pwa)                                │
│  └── SEO 优化 (动态meta标签)                                    │
│                                                                 │
│  🟢 低优先级 (体验优化)                                          │
│  ├── 骨架屏组件 (SkeletonTable)                                │
│  ├── 错误上报服务集成                                           │
│  └── 性能监控 (Web Vitals)                                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 8.2 E2E 测试方案

#### 8.2.1 测试框架选择

推荐使用 **Playwright**：

- 跨浏览器支持 (Chrome, Firefox, Safari)
- 原生支持 TypeScript
- 内置等待机制
- 强大的选择器引擎

#### 8.2.2 测试用例规划

| 测试场景     | 测试内容                       |
| ------------ | ------------------------------ |
| 基础计算流程 | 添加需求 → 计算 → 验证结果     |
| 蓝图生成     | 计算完成 → 生成蓝图 → 验证编码 |
| 设置修改     | 修改增产剂设置 → 重新计算      |
| 排除列表     | 添加排除项 → 验证计算结果      |
| 堆叠模式     | 启用堆叠 → 验证蓝图结构        |
| 错误处理     | 无效输入 → 验证错误提示        |
| 国际化       | 切换语言 → 验证UI文本          |

### 8.3 PWA 支持方案

#### 8.3.1 技术选型

- **vite-plugin-pwa**: Vite 官方 PWA 插件
- **workbox**: Service Worker 管理

#### 8.3.2 功能规划

| 功能     | 说明                         |
| -------- | ---------------------------- |
| 离线可用 | 缓存核心资源，支持离线访问   |
| 安装提示 | 支持 Add to Home Screen      |
| 缓存策略 | 配方数据优先缓存，UI动态更新 |
| 更新通知 | 检测新版本，提示用户刷新     |

---

## 9. 风险与缓解

### 9.1 已识别风险

| 风险项               | 影响 | 缓解措施               | 状态 |
| -------------------- | ---- | ---------------------- | ---- |
| 遗留代码依赖全局状态 | 高   | 适配层封装，逐步迁移   | ✅   |
| 蓝图编码格式变更     | 高   | 保持向后兼容，版本检测 | ✅   |
| 计算精度问题         | 中   | Baseline测试覆盖       | ✅   |
| Worker 不可用        | 中   | 自动降级到主线程计算   | ✅   |
| 大规模计算性能       | 中   | Web Worker 迁移        | ✅   |

### 9.2 技术债务

| 债务项                 | 优先级 | 计划解决时间 |
| ---------------------- | ------ | ------------ |
| 遗留代码类型定义不完整 | 中     | 持续改进     |
| 部分函数缺少单元测试   | 低     | 持续补充     |
| 文档与代码同步         | 低     | 持续维护     |

---

## 10. 变更日志

### 2026-02-15

- ✅ 完成 Web Worker 计算迁移
- ✅ 新增 CalculatorWorkerService 服务
- ✅ 新增 calculator.worker.ts Worker 实现
- ✅ 新增 Worker 通信协议类型定义
- ✅ 测试用例从 603 增加到 637
- ✅ 更新架构文档

### 2026-02-14

- ✅ 完成 blueprint.js 生成器迁移
- ✅ 新增 BuildingGenerator/ConveyorGenerator/SorterGenerator
- ✅ 新增 BlueprintService 整合服务
- ✅ 完成 data.js 模块化拆分
- ✅ 移除 jQuery 依赖

### 2026-02-13

- ✅ 完成 blueprint.js 协议层解耦
- ✅ 新增 BlueprintEncoder/BlueprintDecoder
- ✅ 完成 data.js 核心逻辑解耦
