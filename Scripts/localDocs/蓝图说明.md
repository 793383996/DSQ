# 蓝图模块说明文档

> **状态**：⏳ 重构进行中 - 协议层已完成，生成器待迁移
>
> **更新日期**：2026-02-15
>
> **关联文档**：[bluepirnt重构.md](./bluepirnt重构.md) | [架构迁移方案.md](./架构迁移方案.md)

## 目录

- [1. 模块概述](#1-模块概述)
- [2. 架构演进](#2-架构演进)
- [3. 核心数据结构](#3-核心数据结构)
- [4. Blueprint 类详解](#4-blueprint-类详解)
- [5. 蓝图生成流程](#5-蓝图生成流程)
- [6. 已迁移模块](#6-已迁移模块)
- [7. 待迁移模块](#7-待迁移模块)
- [8. 性能优化与问题修复](#8-性能优化与问题修复)

---

## 1. 模块概述

### 1.1 功能定位

蓝图模块是戴森球计划量产计算器的蓝图生成核心，承担以下职责：

| 功能           | 说明                                   | 迁移状态  |
| -------------- | -------------------------------------- | --------- |
| **数据映射**   | 维护游戏物品、建筑、配方的ID映射关系   | ✅ 已迁移 |
| **布局计算**   | 根据配方计算结果生成合理的建筑布局     | ⏳ 待迁移 |
| **传送带网络** | 自动连接输入输出分拣器，构建物流网络   | ⏳ 待迁移 |
| **增产剂系统** | 生成喷涂机和增产剂供应传送带           | ⏳ 待迁移 |
| **建筑堆叠**   | 支持垂直堆叠多layer建筑，减少占地面积  | ✅ 已迁移 |
| **蓝图编码**   | 将布局数据编码为游戏可识别的蓝图字符串 | ✅ 已迁移 |
| **蓝图解码**   | 解析游戏蓝图字符串为结构化数据         | ✅ 已迁移 |

### 1.2 当前依赖关系

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           蓝图模块架构 (2026-02-15)                      │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  新架构 (TypeScript)                    遗留代码 (JavaScript)            │
│  ───────────────────                    ────────────────────            │
│                                                                         │
│  src/core/types/blueprint.ts            blueprint.js (3967行)           │
│  ├── IBlueprintBuilding                 ├── itemMap (已迁移)             │
│  ├── IBlueprintData                     ├── buildingMap (已迁移)        │
│  └── IBlueprintHeader                   ├── recipeMap (已迁移)          │
│                                         ├── Blueprint类 (待迁移)        │
│  src/core/data/*.json                   │   ├── init()                  │
│  ├── itemMap.json                       │   ├── generateBuildings()     │
│  ├── buildingMap.json                   │   └── generateConveyorBelts() │
│  ├── recipeMap.json                     ├── toStr() (已迁移)            │
│  └── productionCategory.json            └── parse() (已迁移)            │
│                                                                         │
│  src/core/utils/                                                        │
│  ├── BlueprintEncoder.ts ✅                                             │
│  ├── BlueprintDecoder.ts ✅                                             │
│  ├── BinaryWriter.ts ✅                                                 │
│  ├── BinaryReader.ts ✅                                                 │
│  ├── md5.ts ✅                                                          │
│  └── IndexMapper.ts ✅                                                  │
│                                                                         │
│  src/core/services/                                                     │
│  └── StackService.ts ✅                                                 │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 1.3 核心设计理念

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           蓝图生成核心流程                              │
│                                                                         │
│  输入：配方数据（subRecipes）                                           │
│   ↓                                                                   │
│  Phase 1: init() - 初始化与预处理 ⏳待迁移                              │
│   - 配方ID映射                                                         │
│   - 堆叠层数处理（stackLayers）                                        │
│   - 蓝图面积计算                                                       │
│   ↓                                                                   │
│  Phase 2: generateBuildings() - 建筑生成 ⏳待迁移                       │
│   - 生产设备布局                                                       │
│   - 分拣器关联                                                         │
│   - 研究站垂直堆叠（maxLabLayers）                                     │
│   ↓                                                                   │
│  Phase 3: generateConveyorBelts() - 传送带网络 ⏳待迁移                 │
│   - 物料流量计算                                                       │
│   - 传送带生成                                                         │
│   - 分拣器连接                                                         │
│   ↓                                                                   │
│  Phase 4: generateConveyorBeltsForSprayCoater() - 增产剂系统 ⏳待迁移   │
│   - 喷涂机布局                                                         │
│   - 增产剂供应网络                                                     │
│   ↓                                                                   │
│  Phase 5: cloneToStackLayers() - 建筑堆叠 ✅已迁移                      │
│   - 克隆设备到高层                                                     │
│   - Index引用重映射                                                   │
│   - 共享传送带网络                                                     │
│   ↓                                                                   │
│  Phase 6: toStr() - 编码导出 ✅已迁移                                   │
│   - 二进制序列化                                                       │
│   - MD5签名                                                            │
│   - Gzip压缩 + Base64编码                                              │
│   ↓                                                                   │
│  输出：蓝图字符串（游戏可识别）                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 架构演进

### 2.1 重构进度总览

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    blueprint.js 重构路线图                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐                 │
│  │ 阶段 1      │    │ 阶段 2      │    │ 阶段 3      │                 │
│  │ 类型定义    │───▶│ 数据提取    │───▶│ 编解码封装  │                 │
│  │ types.ts    │    │ data/*.json │    │ Encoder/Dec │                 │
│  │ ✅ 已完成   │    │ ✅ 已完成   │    │ ✅ 已完成   │                 │
│  └─────────────┘    └─────────────┘    └─────────────┘                 │
│                                                                         │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐                 │
│  │ 阶段 4      │    │ 阶段 5      │    │ 阶段 6      │                 │
│  │ 堆叠服务    │───▶│ 生成器迁移  │───▶│ 完全解耦    │                 │
│  │ StackService│    │ BuildingGen │    │ 独立模块    │                 │
│  │ ✅ 已完成   │    │ ⏳ 进行中   │    │ 🔲 待开始   │                 │
│  └─────────────┘    └─────────────┘    └─────────────┘                 │
│                                                                         │
│  当前进度: ████████████████████░░░░░░░░ 56%                            │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 文件结构

```text
src/core/
├── legacy/
│   └── blueprint.js           # 蓝图生成逻辑 (3967行) - 待迁移
├── types/
│   ├── blueprint.ts           # ✅ 蓝图类型定义 (107行)
│   ├── stack.ts               # ✅ 堆叠类型定义 (54行)
│   ├── itemMap.ts             # ✅ 物品映射类型 (28行)
│   └── buildingMap.ts         # ✅ 建筑映射类型 (52行)
├── data/
│   ├── itemMap.json           # ✅ 物品映射数据
│   ├── buildingMap.json       # ✅ 建筑映射数据
│   ├── recipeMap.json         # ✅ 配方映射数据
│   ├── buildingType.json      # ✅ 建筑类型
│   └── productionCategory.json # ✅ 生产分类
├── utils/
│   ├── BinaryReader.ts        # ✅ 二进制读取器 (82行)
│   ├── BinaryWriter.ts        # ✅ 二进制写入器 (68行)
│   ├── BlueprintDecoder.ts    # ✅ 蓝图解码器 (384行)
│   ├── BlueprintEncoder.ts    # ✅ 蓝图编码器 (414行)
│   ├── IndexMapper.ts         # ✅ 索引重映射 (78行)
│   └── md5.ts                 # ✅ MD5工具 (109行)
├── services/
│   └── StackService.ts        # ✅ 堆叠服务 (217行)
└── __tests__/
    ├── blueprint.test.ts      # ✅ 编解码测试 (363行)
    ├── binary.test.ts         # ✅ 二进制测试 (228行)
    ├── md5.test.ts            # ✅ MD5测试 (128行)
    ├── indexMapper.test.ts    # ✅ 索引映射测试 (159行)
    └── stackService.test.ts   # ✅ 堆叠服务测试 (384行)
```

### 2.3 遗留代码分析 (blueprint.js)

| 模块                 | 行数  | 状态      | 说明                                         |
| -------------------- | ----- | --------- | -------------------------------------------- |
| `itemMap`            | ~200  | ✅ 已迁移 | → `src/core/data/itemMap.json`               |
| `buildingMap`        | ~150  | ✅ 已迁移 | → `src/core/data/buildingMap.json`           |
| `productionCategory` | ~10   | ✅ 已迁移 | → `src/core/data/productionCategory.json`    |
| `buildingType`       | ~10   | ✅ 已迁移 | → `src/core/data/buildingType.json`          |
| `recipeMap`          | ~200  | ✅ 已迁移 | → `src/core/data/recipeMap.json`             |
| `Blueprint` 类       | ~1500 | ⏳ 待迁移 | init/generateBuildings/generateConveyorBelts |
| `Blueprint.toStr()`  | ~300  | ✅ 已迁移 | → `BlueprintEncoder.encode`                  |
| `Blueprint.parse()`  | ~300  | ✅ 已迁移 | → `BlueprintDecoder.decode`                  |
| MD5 实现             | ~300  | ✅ 已迁移 | → `src/core/utils/md5.ts`                    |
| 二进制读写           | ~400  | ✅ 已迁移 | → `BinaryReader/BinaryWriter`                |

**总行数**: 3967行

---

## 3. 核心数据结构

### 3.1 类型定义 (blueprint.ts)

```typescript
export interface ICoordinate {
  x: number
  y: number
  z: number
}

export interface IBlueprintBuilding {
  index: number
  areaIndex?: number
  localOffset: ICoordinate[] | null
  yaw: number[]
  itemId: number
  modelIndex?: number
  outputObjIdx: number
  inputObjIdx: number
  outputToSlot: number
  inputFromSlot: number
  outputFromSlot: number
  inputToSlot: number
  outputOffset: number
  inputOffset: number
  recipeId: number
  filterId: number
  parameters: unknown
}

export interface IBlueprintHeader {
  layout: number
  icons: number[]
  time: Date
  gameVersion: string
  shortDesc: string
  desc: string
}

export interface IBlueprintData {
  version: number
  cursorOffset: { x: number; y: number }
  cursorTargetArea: number
  dragBoxSize: { x: number; y: number }
  primaryAreaIdx: number
  areas: IBlueprintArea[]
  buildings: IBlueprintBuilding[]
  header: IBlueprintHeader
}
```

### 3.2 物品映射表 (itemMap.json)

```json
{
  "sand": { "name": "sand", "iconId": 1099, "remark": "沙土" },
  "water": { "name": "water", "iconId": 1000, "remark": "水" },
  "ironOre": { "name": "ironOre", "iconId": 1001, "remark": "铁矿" },
  "proliferatorMkI": {
    "name": "proliferatorMkI",
    "iconId": 1141,
    "extra_rate": 0.125,
    "accelerate": 0.25,
    "remark": "增产剂Mk.Ⅰ"
  }
}
```

**字段说明**：

| 字段         | 类型   | 说明                   |
| ------------ | ------ | ---------------------- |
| `name`       | String | 物品内部标识符         |
| `iconId`     | Number | 游戏内图标ID           |
| `extra_rate` | Number | 增产模式下额外产出比例 |
| `accelerate` | Number | 加速模式下生产速度加成 |
| `remark`     | String | 中文名称               |

### 3.3 建筑映射表 (buildingMap.json)

```json
{
  "arcSmelter": {
    "remark": "电弧熔炉",
    "name": "arcSmelter",
    "itemId": 2302,
    "modelIndex": 62,
    "productionSpeed": 1,
    "size": { "x": 3, "y": 3 },
    "type": 0,
    "category": 0,
    "slotMaxIndex": 7
  },
  "assemblingMachineMk3": {
    "remark": "制作台Mk.III",
    "itemId": 2305,
    "modelIndex": 67,
    "productionSpeed": 1.5,
    "size": { "x": 3, "y": 3 },
    "type": 0,
    "category": 1,
    "slotMaxIndex": 8
  }
}
```

**建筑类型 (category)**：

| 类型         | 值  | 包含建筑                     |
| ------------ | --- | ---------------------------- |
| `smelter`    | 0   | 电弧熔炉、位面熔炉、负熵熔炉 |
| `assembling` | 1   | 制作台Mk.Ⅰ/Ⅱ/Ⅲ、重组式制造台 |
| `plant`      | 2   | 化工厂、量子化工厂           |
| `refinery`   | 3   | 原油精炼厂                   |
| `collider`   | 4   | 微型粒子对撞机               |
| `lab`        | 5   | 矩阵研究站、自演化研究站     |

---

## 4. Blueprint 类详解

### 4.1 构造函数

```javascript
class Blueprint {
  constructor(title, iconId, recipe, config) {
    this.recipe = recipe // 配方计算结果
    this.config = config // 用户配置

    // 内部状态
    this.buildingIndex = -1 // 建筑索引计数器
    this.blueprintSize = { x: 0, y: 0 } // 蓝图尺寸
    this.occupiedArea = [] // 占用区域记录
    this.buildings = [] // 建筑列表
    this.buildingArray = [] // 建筑二维数组
    this.sorters = {} // 分拣器信息
    this.sprayCoaterOffsetList = [] // 喷涂机位置列表
    this.itemSummary = {} // 物料统计
  }
}
```

### 4.2 核心方法概览

| 方法                                    | 功能                                  | 迁移状态              |
| --------------------------------------- | ------------------------------------- | --------------------- |
| `init()`                                | 初始化（配方映射、num缩减、面积计算） | ⏳ 待迁移             |
| `generateBuildings()`                   | 生成所有生产建筑                      | ⏳ 待迁移             |
| `generateConveyorBelts()`               | 生成物料传送带网络                    | ⏳ 待迁移             |
| `generateConveyorBeltsForSprayCoater()` | 生成增产剂传送带                      | ⏳ 待迁移             |
| `cloneToStackLayers()`                  | 克隆建筑到高层（堆叠功能）            | ✅ → StackService     |
| `toStr()`                               | 导出蓝图字符串                        | ✅ → BlueprintEncoder |
| `parse()`                               | 解析蓝图字符串                        | ✅ → BlueprintDecoder |

### 4.3 Blueprint 配置项

```javascript
const defaultConfig = {
  stackLayers: 1, // 建筑堆叠层数（1=不堆叠）
  maxLabLayers: 15, // 研究站最大垂直堆叠层数
  maxSorterNumOneBelt: 8, // 单传送带最大分拣器数量
  conveyorBeltStackLayer: 4, // 传送带物品堆叠层数
  onlyConveyorBeltMk3: false, // 仅使用三级传送带
  onlySorterMk3: false, // 仅使用三级分拣器
  selfSpray: true, // 自喷涂模式
  generateTeslaTower: true // 生成电力感应塔
}
```

---

## 5. 蓝图生成流程

### 5.1 完整调用链

```
用户点击生成蓝图
    ↓
new Blueprint(title, iconId, recipe, config)
    ↓
init() - 初始化 ⏳待迁移
    ├── mapRecipeID() - 配方ID映射
    ├── stackLayers处理 - num缩减
    └── calculateBlueprintArea() - 面积计算
    ↓
generateBuildings() - 生成建筑 ⏳待迁移
    ├── 遍历subRecipes
    ├── newProductionBuilding() - 生成生产建筑
    └── 记录分拣器信息
    ↓
generateConveyorBelts() - 生成传送带 ⏳待迁移
    ├── 计算itemSummary
    ├── 生成传送带网络
    └── 连接分拣器
    ↓
generateConveyorBeltsForSprayCoater() - 增产剂系统 ⏳待迁移
    ↓
cloneToStackLayers() - 堆叠克隆 ✅已迁移
    ├── 快照baseBuildings
    ├── 生成地基
    ├── 过滤不可克隆建筑
    ├── 逐层克隆
    └── index引用重映射
    ↓
toStr() - 编码导出 ✅已迁移
    ├── 二进制序列化
    ├── MD5签名
    ├── Gzip压缩
    └── Base64编码
    ↓
输出蓝图字符串
```

### 5.2 生成阶段详解

| 阶段 | 方法                                    | 输入                  | 输出               | 状态      |
| ---- | --------------------------------------- | --------------------- | ------------------ | --------- |
| 1    | `init()`                                | recipe, config        | 初始化状态         | ⏳ 待迁移 |
| 2    | `generateBuildings()`                   | subRecipes            | buildings (z=0)    | ⏳ 待迁移 |
| 3    | `generateConveyorBelts()`               | buildings, sorters    | 传送带网络         | ⏳ 待迁移 |
| 4    | `generateConveyorBeltsForSprayCoater()` | sprayCoaterOffsetList | 增产剂系统         | ⏳ 待迁移 |
| 5    | `cloneToStackLayers()`                  | buildings (z=0)       | buildings (所有层) | ✅ 已迁移 |
| 6    | `toStr()`                               | buildings             | 蓝图字符串         | ✅ 已迁移 |

---

## 6. 已迁移模块

### 6.1 二进制读写器

**文件**: `src/core/utils/BinaryReader.ts` / `BinaryWriter.ts`

```typescript
export class BinaryWriter {
  writeByte(value: number): void
  writeInt16(value: number): void
  writeInt32(value: number): void
  writeFloat32(value: number): void
  writeString(str: string): void
  getBuffer(): Uint8Array
}

export class BinaryReader {
  readByte(): number
  readInt16(): number
  readInt32(): number
  readFloat32(): number
  readString(length: number): string
}
```

**关键特性**：

- 严格遵守小端序 (Little-Endian)
- Float32 精度保证
- 完整单元测试覆盖

### 6.2 蓝图编解码器

**文件**: `src/core/utils/BlueprintEncoder.ts` / `BlueprintDecoder.ts`

```typescript
export function encodeBlueprint(data: IBlueprintData): string {
  // 1. 写入二进制数据
  // 2. 计算 MD5 签名
  // 3. Gzip 压缩
  // 4. Base64 编码
  // 5. 添加 BLUEPRINT: 前缀
}

export function decodeBlueprint(blueprintStr: string): IBlueprintData {
  // 1. 移除 BLUEPRINT: 前缀
  // 2. Base64 解码
  // 3. Gzip 解压
  // 4. 验证 MD5 签名
  // 5. 解析二进制数据
}
```

### 6.3 堆叠服务

**文件**: `src/core/services/StackService.ts`

```typescript
export class StackService {
  constructor(config: Partial<IStackConfig>)

  cloneToStackLayers(buildings: IBlueprintBuilding[], layers: number): ICloneResult
  createFoundationBuildings(zOffset: number, count: number): IBlueprintBuilding[]
  reduceBuildingNum(subRecipes: ISubRecipe[]): void
  scaleItemSummary(itemSummary: IItemSummary): void

  getConfig(): IStackConfig
  getBuildingIndex(): number
  setBuildingIndex(index: number): void
}
```

### 6.4 索引重映射

**文件**: `src/core/utils/IndexMapper.ts`

```typescript
export function buildIndexMap(buildings: IBlueprintBuilding[]): Map<number, number>

export function cloneBuildingWithRemap(
  building: IBlueprintBuilding,
  indexMap: Map<number, number>,
  indexOffset: number,
  zOffset: number
): IBlueprintBuilding
```

### 6.5 MD5 工具

**文件**: `src/core/utils/md5.ts`

```typescript
export function md5Hex(message: string): string
```

---

## 7. 待迁移模块

### 7.1 BuildingGenerator (待实现)

```typescript
export interface IBuildingGeneratorConfig {
  conveyorBeltStackLayer: number
  onlyConveyorBeltMk3: boolean
  onlySorterMk3: boolean
  useSorterMk4: boolean
  selfSpray: boolean
}

export class BuildingGenerator {
  initLayout(recipes: ISubRecipe[]): void
  generateFromRecipe(recipe: ISubRecipe, position: ICoordinate): IBlueprintBuilding[]
  generateAssemblingMachine(recipe: ISubRecipe, pos: ICoordinate): IBlueprintBuilding
  generateSmelter(recipe: ISubRecipe, pos: ICoordinate): IBlueprintBuilding
  generateLab(recipe: ISubRecipe, pos: ICoordinate): IBlueprintBuilding

  getBuildings(): IBlueprintBuilding[]
  reset(): void
}
```

### 7.2 ConveyorGenerator (待实现)

```typescript
export interface IConveyorGeneratorConfig {
  transportSpeed: number
  stackLayer: number
  maxSorterNumOneBelt: number
}

export class ConveyorGenerator {
  generateBelt(segment: IBeltSegment): IBlueprintBuilding[]
  connectBuildings(source: IBlueprintBuilding, target: IBlueprintBuilding): void
  optimizeLayout(): void

  getBuildings(): IBlueprintBuilding[]
  reset(): void
}
```

### 7.3 BlueprintService (待实现)

```typescript
export class BlueprintService {
  private buildingGenerator: BuildingGenerator
  private conveyorGenerator: ConveyorGenerator
  private stackService: StackService

  generate(recipes: ISubRecipe[], options: IBlueprintGenerateOptions): string
  parse(blueprintStr: string): IBlueprintData
  validate(blueprintStr: string): boolean
}
```

---

## 8. 性能优化与问题修复

### 8.1 Pako 兼容性修复

```typescript
function getPakoImpl(): PakoModule {
  const pakoModule = (pako as unknown as { default?: PakoModule }).default || pako
  return {
    ...pakoModule,
    gzip: pakoModule.deflate, // 兼容性封装
    ungzip: pakoModule.inflate // 兼容性封装
  } as PakoModule
}
```

### 8.2 浮点数精度处理

```typescript
const tolerance = 0.0001
if (Math.abs(read - expected) > tolerance) {
  console.warn('Float32 precision loss detected')
}
```

### 8.3 性能瓶颈预警

大型蓝图（几千个建筑）的生成过程可能阻塞主线程：

```typescript
// 推荐方案：Web Worker (待实现)
const worker = new Worker('blueprint-worker.js')
worker.postMessage({ type: 'generate', data: largeData })
```

---

## 9. 测试覆盖

| 模块       | 测试文件             | 测试数量   | 状态 |
| ---------- | -------------------- | ---------- | ---- |
| 二进制读写 | binary.test.ts       | 228行      | ✅   |
| MD5        | md5.test.ts          | 128行      | ✅   |
| 编解码     | blueprint.test.ts    | 363行      | ✅   |
| 索引映射   | indexMapper.test.ts  | 159行      | ✅   |
| 堆叠服务   | stackService.test.ts | 384行      | ✅   |
| **总计**   | -                    | **1262行** | ✅   |

---

## 10. 预估工作量

| 阶段                | 预估时间 | 状态      |
| ------------------- | -------- | --------- |
| Phase 1-3: 协议层   | 3天      | ✅ 已完成 |
| Phase 4: 堆叠服务   | 1天      | ✅ 已完成 |
| Phase 5: 生成器迁移 | 3天      | ⏳ 进行中 |
| Phase 6: 测试与清理 | 2天      | 🔲 待开始 |
| **总计**            | **9天**  | **56%**   |

---

**最后更新**：2026-02-15  
**维护者**：DSQ 计算器团队
